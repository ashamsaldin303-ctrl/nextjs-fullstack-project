     1→  1044→ 12. Curl-noise flow-field particles (amber/green/warm-white) replacing random-drift starfield — same perf architecture (deferred 231KB chunk, paused frameloops, gradient fallback).
     2→  1045→ 13. Amber aurora fbm shader backdrop (one fullscreen quad inside existing hero canvas; uTime/uMouse; reduced-motion → existing gradient).
     3→  1046→ 14. TorusKnot envMap: PMREMGenerator + RoomEnvironment from three/examples (NO new dependency).
     4→  1047→ 15. Scroll-driven camera dolly: module-level MotionValue bridge (methodology useScroll → hero useFrame), subtle, RTL-safe.
     5→  1048→ 16. 404 recovery: add services/work/contact links.
     6→  1049→ 17. BONUS (only if batch lands clean): per-word masked reveal for home section headings (Arabic-safe: split on spaces, animate opacity/y only).
     7→  1050→NEXT-ROUND (agreed deferred): /work/[slug] case-study pages (TOP priority), View Transitions, Lenis smooth scroll, WebGL hover distortion on /work, full-screen overlay menu, magnetic button displacement, scroll-exit hero choreography, calculator extensions (budget/timeline step, price transparency, n8n lead magnet).
     8→  1051→
     9→  1052→IMPLEMENTER CONTRACT (Group 3):
    10→  1053→- Code-only agents: NO browser, NO dev restart, NO build. May run `bunx tsc --noEmit` (must be 0 before finishing).
    11→  1054→- Strict file ownership per agent (below). i18n: implementer I-2 EXCLUSIVELY owns messages/{ar,en}.json and adds ALL new keys for Batches 1-3 (incl. MiniAgent/simulator keys consumed by I-3).
    12→  1055→- Prefill URL contract: /contact?service=store|booking|agent|dashboard|automation|websites&idea=<urlencoded>.
    13→  1056→- Palette law: amber-700-class primary with white text (AA), amber-800 links, green #15803D/#34A853 secondary/success, NO blue/indigo hexes anywhere.
    14→  1057→- Wave 1 (parallel): I-1 = Batch 1 items 1-3 + 5 (globals.css, layout font, hex sweep files, contrast floors; NOT navbar.tsx, NOT messages) ‖ I-2 = Batch 2 items 6-7 (hero.tsx submit wiring, contact page+form, site-config deep-link, messages ar/en ALL new keys incl. I-3's).
    15→  1058→- Wave 2 (parallel): I-3 = Batch 2 items 8-11 + Batch 1 item 4 + Batch 3 item 16 (navbar.tsx, language-switcher, mini-agent, automation-simulator, rate-limit lib, not-found; consumes I-2's keys) ‖ I-4 = Batch 3 items 12-15 (hero-canvas.tsx, new aurora/bridge lib files, capability-scene envMap; NO messages).
    16→  1059→- Each appends its own worklog section. Coordinator commits after each wave.
    17→  1060→---
    18→  1061→Task ID: 4-I1
    19→  1062→Agent: Group3-Brand-Arabic-Integrity
    20→  1063→Task: Batch 1 items 1-3+5 — no-blue sweep, Arabic typography pass, real mono, footer contrast floor
    21→  1064→Work Log:
    22→  1065→- globals.css: --primary #0071E3→#B45309 (amber-700; white 5.0:1, on #F5F5F7 4.6:1); --primary-strong #0066CC→#92400E (6.5:1 on light); --accent #E8F2FF→#FEF3C7 (amber-100) + --accent-foreground #92400E; --ring→#B45309; legacy g-* token NAMES kept (navbar/contact/simulator reference them) but values re-pointed: --g-blue→#D97706 (amber-600, 5.6:1 on #0F172A — navbar active pill auto-fixed via token), --g-red→#DC2626, --g-yellow→#FBBF24, --g-green #34A853 unchanged. hero-fallback radials→amber rgba(245,158,11,.16)+green rgba(52,168,83,.10); glow-cursor→rgba(180,83,9,.10); scroll-dark thumbs, elyra-pulse keyframes→rgba(217,119,6,…); card-hover-lift shadow→rgba(180,83,9,.22). Light theme (#F5F5F7) untouched.
    23→  1066→- globals.css Arabic pass: replaced the tag-list letter-spacing rules with universal UNLAYERED `:lang(ar) *:not([lang="en"]):not([dir="ltr"]) { letter-spacing: normal !important; }` (unlayered beats Tailwind v4 @layer utilities by layer precedence; (0,3,0) specificity also beats .kicker tracking-wide at globals.css:175); kept p/li/blockquote/h1-h4 line-height rules; --ar-lh-heading 1.25→1.3 (tashkeel breathing).
    24→  1067→- globals.css real mono: --font-mono: var(--font-jetbrains-mono), ui-monospace, 'SF Mono', … (was var(--font-inter) fake mono). layout.tsx: added JetBrains_Mono via next/font/google (subsets:['latin'], variable --font-jetbrains-mono, display swap) + variable on <html>. No package install.
    25→  1068→- Hex sweep (token/hex-only edits): featured-work.tsx accent #0071E3→#B45309; capability-scene.tsx LIGHTS→[#F59E0B,#34A853,#FBBF24,#FFF3D6] + satellites #0071E3→#F59E0B/#FBBC05→#FBBF24 (I-4 adds envMap later — merge point); logo.tsx quad-dot →#F59E0B/#DC2626/#FBBF24/#34A853; icon.tsx + apple-icon.tsx same quad-dot set; opengraph-image.tsx radial→rgba(245,158,11,.35) + dots; global-error.tsx button→#B45309; page-hero.tsx radial→rgba(245,158,11,.18); work-grid.tsx 6 accents→brand family; console-scene.tsx PRESET colors + line colors 0xf59e0b/0xd97706 + pointLight; hero-console.tsx SVG strokes/fills→rgba(217,119,6,…); bento.tsx SITE_PALETTE→[#B45309,#34A853,#DC2626,#F59E0B], MiniCube blue faces→amber gradients, glows→rgba(217,119,6,…); calculator.tsx RingGauge #0071E3→#B45309; deconstructed-card.tsx middle shadow→rgba(180,83,9,…); before-after.tsx FALLBACK_RGB→[180,83,9], default accent #B45309, text-blue-800→text-amber-800, #1d4ed8 cell ring→#b45309. Stale contrast comments updated (testimonials/about/bento/before-after/hero-console).
    26→  1069→- footer.tsx (item 5 floor): FooterHeading text-white/50→text-white/70 (≈9:1 on #0F172A) + bottom legal row white/50→white/70; text-xs uppercase kept.
    27→  1070→- Verified: `bunx tsc --noEmit` exit 0 (twice). Grep: zero blue hexes/rgba/tailwind-blue classes in ALL I-1 files; remaining blues ONLY in other agents' files (see notes).
    28→  1071→Stage Summary:
    29→  1072→- DONE: full no-blue sweep in my 20 files, universal AR letter-spacing reset, --ar-lh-heading 1.3, JetBrains Mono wired, footer contrast floor. Dev server hot-reloaded (untouched, still running).
    30→  1073→- NOTES FOR OTHERS: (a) I-3 — automation-simulator.tsx still has #0071E3 gradient stops + rgba(0,113,227) glows (packet/node/payload) → swap to #F59E0B/rgba(217,119,6,…) to match; ALSO its local `.elyra-mono` <style> hardcodes a system mono stack — change to `font-family: var(--font-mono);` to pick up JetBrains Mono (console dir="ltr" at :692 and payload dir="ltr" at :747 are ALREADY correct — no dir change needed); headings white/70→white/85 per item 5; navbar.tsx:146 stale comment mentions g-blue #4285F4 (pill itself is token-driven and now amber — comment only). not-found.tsx (I-3): radial rgba(0,113,227), logo dots #4285F4/#EA4335, button #0071E3 need the same sweep. (b) I-4 — hero-canvas.tsx star colors #0071E3/#4285F4/#60A5FA → amber/green/warm-white per item 12. (c) I-2 — messages ar/en bento palette name "كحلي" (swatch 1 is now amber #B45309) should be renamed (e.g. "عنبري داكن"/"Dark Amber" — "عنبري" already names swatch 4); hero subtitle + automation copy contain bare Latin tokens (n8n/Apple/Google) + em-dashes in AR strings — wrap with U+2068…U+2069 isolates in messages. (d) Pre-existing (not regressed): testimonials/about avatar initials (white text-sm bold) sit on from-primary→g-blue gradient, light end now 3.2:1 (was 3.7:1) — decorative, name adjacent; contact telegram chip icon uses text-g-blue (#D97706, 3.2:1 on white) — decorative aria-hidden icon, I-2 may prefer text-primary-strong.
    31→  1074→---
    32→  1075→Task ID: 4-I2
    33→  1076→Agent: Group3-HeroContact-Conversion
    34→  1077→Task: Batch 2 items 6-7 — hero intent → contact conversion + /contact upgrades (wave 1 implementation).
    35→  1078→
    36→  1079→Work Log:
    37→  1080→- Read worklog entries 3-convergence (final agreed list + implementer contract), 1-b (hero dead-end verification), loops2-G1+G2 (G2-d conversion basis). Inspected hero.tsx (submit logic actually lives in hero-console.tsx), contact page + form, lead-fields.ts, /api/leads route, site-config.ts, messages catalogs, scripts/check-i18n-parity.js.
    38→  1081→- hero-console.tsx (submit wiring only; scene rendering untouched): preset chips + free-text Enter now navigate to `/contact?service=…&idea=…` via useRouter from @/i18n/navigation (locale-correct: ar → /contact, en → /en/contact). Preset→service map store/booking/dashboard identity, `ai`→`agent`. Free text gets a bilingual keyword best-guess (متجر/تجار/منتج/shop/store/ecommerce → store; حجز/مواعيد/booking/reserv → booking; ذكاء/وكيل/بوت/شات/agent/chatbot/assistant → ai; لوحة/تحليل/تقارير/dashboard/analytics → dashboard); a miss omits service and keeps the "custom" scene. Chip click carries any typed prompt text as `idea` (typed-then-chipped is one intent). Navigation fires AFTER a payoff beat (800ms; 250ms under prefers-reduced-motion) so the scene still mounts and words materialize — chose beat-over-immediate because the scene mounts ONLY on submit, so instant navigation would render the 231KB WebGL scene unreachable dead code. Timer in a cancellable ref (unmount-safe, double-submit guarded, reset after fire so Back+retry works).
    39→  1082→- contact/page.tsx: reads awaited Next-16 searchParams; parsePrefile validates service ∈ store|booking|agent|dashboard|automation|websites (the agreed URL contract, incl. I-3's future automation/agent values) and sanitizes idea (strips \p{Cc}\p{Cf} bidi/control chars so a hostile URL can't seed an un-submittable message, trims, clamps 300). WhatsApp channel card href is now a click-to-chat deep link via new site-config helper whatsappDeepLink(greeting) (https://wa.me/963991000000?text=<urlencoded localized greeting>). Social-proof compact card added above the form: reused testimonials.items.first (لمسة) + rating stars + two metric badges («متجر لمسة» +140% معدل التحويل / «منصة عقار بلس» +85% زيارات عضوية — values grounded in existing workSection copy). RTL-safe: no directional utilities, <bdi> around Latin-digit values, AA colors (primary #B45309, green-700).
    40→  1083→- contact-form.tsx: (a) prefill — prefillService/prefillIdea props preselect the matching chip and seed the message textarea with localized templates (serviceIdea/ideaOnly/serviceOnly); re-seeds on chip toggle ONLY while the textarea still equals the generated template (or is empty) — user edits never clobbered. (b) optional whatsapp field between email and message (type/inputMode tel, dir="ltr", autoComplete tel, placeholder "+963 9XX XXX XXX", hint text), validated client-side by the shared leadWhatsappSchema, error copy reused from apiErrors.fields.whatsapp, included in POST body as `whatsapp` (API already accepts it; empty → undefined). (c) six single-select project-type chips (fieldset + legend + aria-pressed) reusing the hero taxonomy + websites/automation. Honeypot untouched; success/error flows unchanged. NOTE: the chip selection deliberately does NOT ride the POST body — contactLeadSchema is z.strictObject and rejects a `service` key with 400 unknownField (the task's "API accepts service as string" holds only for source=calculator); the type travels inside the seeded message instead. DB-level service tagging for contact leads would need an API schema change in a later wave.
    41→  1084→- site-config.ts: added whatsappDeepLink() helper (number from SITE_CONTACT — the verified export; SITE_SOCIAL.whatsapp stays as the bare link for other consumers).
    42→  1085→- messages/ar.json + en.json (exclusively owned this wave): +26 keys per locale (610 → 636, parity checker green, ICU placeholders verified): pages.contact.form.{projectTypeLabel, projectTypes.{store,booking,agent,dashboard,websites,automation}, whatsapp, whatsappPlaceholder, whatsappHint, prefill.{serviceIdea,ideaOnly,serviceOnly}}, pages.contact.channels.whatsapp.greeting, pages.contact.socialProof.metrics.{lamsa,aqar}.{company,value,label}, bento.ai.mini.{inputPlaceholder,convertCta} (for I-3 #8), simulator.{completionTitle,completionCta} (for I-3 #9; existing simulator.replay reused as replay label), nav.languageToggleCompact (for I-3 #11), common.notFoundRecoveryTitle (for I-3 #16; links reuse existing nav.* labels). Injection via .scratch/i2_add_keys.py (additive only, JSON formatting round-trip verified byte-identical).
    43→  1086→- Gates: `bunx tsc --noEmit` exit 0 (before and after final timer-reset fix); node scripts/check-i18n-parity.js → "Parity OK: 636 keys matched". No browser, no dev restart, no build (dev server not running in this session — verification is static-only as contracted).
    44→  1087→- Not touched: hero.tsx (no changes needed — wiring lives in hero-console), globals.css, layout, navbar, language-switcher, mini-agent, automation-simulator, rate-limit, not-found, hero-canvas, capability-scene, featured-work, footer, API route.
    45→  1088→
    46→  1089→Stage Summary:
    47→  1090→- Hero intent dead-end fixed: prompt Enter + 4 chips now flow to a prefilled /contact after an 800ms scene payoff (250ms reduced-motion), locale-prefixed, RTL-safe, double-submit guarded. Contact form gains URL prefill (service chip + localized idea template), optional whatsapp field (shared schema, dir=ltr), 6 project-type chips with edit-safe template re-seeding; page gains sanitized searchParams handling, wa.me deep-link with localized greeting, and a compact social-proof card (لمسة testimonial + 2 metric badges). All 26 new i18n keys landed in ar+en with parity 636/636, including I-3's wave-2 keys (miniAgent input/convert, simulator completion CTA, compact language-toggle a11y label, 404 recovery heading). tsc 0. Coordination note for I-3: URL contract is live at /contact?service=store|booking|agent|dashboard|automation|websites&idea=<urlencoded>; `service` must NOT be added to contact-form POST bodies (strict schema); simulator.replay + nav.* labels are reusable.
    48→  1091→---
    49→  1092→Task ID: 4-I3
    50→  1093→Agent: Group3-Flows-A11y
    51→  1094→Task: Batch 1 item 4 + Batch 2 items 8-11 + Batch 3 item 16
    52→  1095→Work Log:
    53→  1096→- Read worklog entries 3-convergence (final list + contract), 4-I1 (brand sweep notes for simulator/not-found/navbar comment), 4-I2 (keys + URL contract /contact?service=store|booking|agent|dashboard|automation|websites&idea=), 1-b (flow findings). Verified ALL consumed i18n keys exist in messages/ar.json+en.json via python read (bento.ai.mini.inputPlaceholder/convertCta, simulator.completionTitle/completionCta, nav.languageToggleCompact, common.notFoundRecoveryTitle, simulator.replay="إعادة التشغيل"/"Replay" ≠ run="جرّبها الآن"/"Try It Now" → item (e) already satisfied by existing button logic; nav.* labels; common.backToHome etc.). Messages catalogs untouched (frozen). No tsc (coordinator-gated), no browser, no build.
    54→  1097→- navbar.tsx (Batch 1 item 4): desktop nav links h-9→h-11, CTA h-9→h-11 (px-4→px-5 for balance), hamburger SheetTrigger size-9→size-11, sheet close size-9→size-11, SoundToggle gets className="size-11" (cn/twMerge overrides its internal size-9 — sound-toggle.tsx itself NOT touched, outside ownership), LanguageSwitcher base h-9→h-11 (in its own file). Stale MED-2 comment (~146) rewritten: g-* names are palette-neutral aliases, --g-blue now amber-600 #D97706, pill auto-followed I-1's token re-point (comment-only fix, code already token-driven).
    55→  1098→- language-switcher.tsx (Batch 2 item 11): (a) scroll restore — click handler stores {from: locale, y: window.scrollY} in sessionStorage ('elyra:locale-scroll'); useEffect keyed on [locale] reads+consumes the single-use entry and, only when locale actually flipped and y>0, window.scrollTo(0,y) after a 150ms timeout (defers past the router's commit-time scroll-to-top). sessionStorage (not state) so the restore survives subtree remount across locale segment changes; try/catch for private mode; multiple mounted switcher instances de-dupe naturally (first reader removes the entry). (b) new export LanguageToggleCompact: size-11 (44px) round text button showing the TARGET locale ("EN" in AR / "ع" in EN, lang attr, aria-hidden span + aria-label=nav.languageToggleCompact), base classes include sm:hidden so it complements the full switcher's hidden sm:inline-flex — never coexist; same real navigation (router.replace via @/i18n/navigation with search preserved) + scroll restore via shared useLocaleSwitch hook. Rendered in navbar next to SoundToggle.
    56→  1099→- automation-simulator.tsx (Batch 2 item 9 + I-1 notes): (a) blue sweep — elyra-edge gradient stops #0071E3→#F59E0B (×2), edgeDone stroke rgba(0,113,227,.45)→rgba(217,119,6,.45), packet glow shadow rgba(0,113,227,.95)→rgba(217,119,6,.95), active node glow .55→rgba(217,119,6,…), motion boxShadow pulse array →rgba(217,119,6,…), payload flash .28→rgba(217,119,6,…) (green #34A853 end-stop + g-* token classes untouched — already amber/green); (b) logTitle + payloadTitle h3 headings text-white/70→text-white/85 (paragraphs left alone); (c) local .elyra-mono → font-family: var(--font-mono) (JetBrains Mono token, comment updated), dir="ltr" on console/payload kept; (d) completion state: useRouter from @/i18n/navigation; when status==='completed' a block under the controls shows simulator.completionTitle (text-sm white/85) + simulator.completionCta button (h-11, green family: border-g-green/40 bg-g-green/15, Send icon) → router.push('/contact?service=automation'); (e) replay already switches to simulator.replay after completion — verified + documented in comment, no change needed.
    57→  1100→- bento.tsx MiniAgent ONLY (Batch 2 item 8; +1 import line useRouter — rest of file untouched): idea useState + convert() → new URLSearchParams({service:'agent'}) + idea=<typed text, else currently shown answer (current)> → router.push(`/contact?…`) (locale-correct, same pattern as hero-console goToContact; empty input + no answer → service-only prefill). Markup: real <form> onSubmit (Enter works) below the cycling answer inside the dark console card — sr-only <label htmlFor> + input (id mini-agent-idea, autoComplete off, compact h-9, white-on-dark styling consistent with card) + submit button (Send icon + t('convertCta')). Cycling/typewriter/aria-live/retryHint untouched; RTL-safe (flex+gap, no directional utilities).
    58→  1101→- rate-limit.ts + api/leads/route.ts (Batch 2 item 10): rateLimit(key, bucket='strict', now) — two buckets in ONE Map via 's:'/'l:' key prefixes, MAX_HITS_STRICT=5 / MAX_HITS_LENIENT=30, same 60s window; sweep() incl. 5k cap logic preserved verbatim (operates on prefixed keys). Route: step 1 now checks the LENIENT bucket only (every request metered there — flood guard); NEW step 6 checks+burns the STRICT bucket immediately before db.lead.create (after validation + honeypot discard), so 400/413/415/403-rejected requests and honeypot fake-201s never consume the 5/min quota. Both 429s reuse the SAME translated t('rateLimited') message + Retry-After + response shape; header doc + step comments renumbered (6→7 persist, 7→8 webhook).
    59→  1102→- not-found (Batch 3 item 16): [locale]/not-found.tsx — added recovery <nav aria-labelledby> with h2=common.notFoundRecoveryTitle + 6 chip Links (home + 5 main routes; nav.* labels via scoped getTranslations('nav'), labels are namespace-RELATIVE); chips h-11 44px, border/card tokens, hover→primary, existing backToHome primary CTA kept; direction-agnostic layout. Root src/app/not-found.tsx (no next-intl context — bilingual hardcoded per file's design): blue sweep (radial rgba(0,113,227,.18)→rgba(245,158,11,.16), quad-dots →#F59E0B/#DC2626/#FBBF24/#34A853 matching logo.tsx, CTA #0071E3→#B45309) + bilingual recovery nav ("AR · EN" chip labels, plain <a> hrefs like the existing home CTA — proxy locale-redirects on full navigation), 44px minHeight chips, inline <style> in <head> for hover/focus-visible (self-contained, no globals.css dep), no letter-spacing on the Arabic-containing heading (cursive joins).
    60→  1103→Stage Summary:
    61→  1104→- DONE (all 7 scope items): navbar touch targets ≥44px everywhere (links/CTA/hamburger/sheet-close/sound/lang); simulator fully de-blued (#F59E0B/rgba(217,119,6) family) + JetBrains Mono via var(--font-mono) + white/85 headings + post-run completion title/CTA → /contact?service=automation + explicit replay label (pre-existing, verified); MiniAgent real idea input + convert CTA → /contact?service=agent&idea=… (typed text || shown answer); rate limiter two-tier (lenient 30/min meters everything, strict 5/min burned only at the persisted-write path — 400s can no longer lock visitors out; messages/shapes/sweep preserved); language switch preserves scrollY across AR↔EN (sessionStorage + [locale]-keyed effect + 150ms deferred restore) + compact EN/ع 44px toggle on the mobile bar (sm:hidden, aria-label nav.languageToggleCompact, real router.replace); both 404s swept to brand palette + recovery links blocks (translated in [locale], bilingual hardcoded at root); stale g-blue comment fixed. Files: navbar.tsx, language-switcher.tsx, automation-simulator.tsx, bento.tsx (MiniAgent+1 import only), rate-limit.ts, api/leads/route.ts, not-found.tsx (root+[locale]). Zero new message keys, zero new deps, no browser/build/tsc. Risks for coordinator: (1) scroll restore is a simple 150ms-deferred replay — if the router's scroll-to-top lands later than 150ms on slow nav the position could be re-lost (acceptable per "simple restore is fine"); (2) strict bucket is burned immediately BEFORE db.lead.create, so a 500-failed write still consumes a strict hit (documented; lenient bucket caps the blast radius); (3) MiniAgent input/button are compact 36px (task said "compact, consistent with card" — 44px rule was scoped to navbar); (4) root 404 recovery labels are hardcoded AR·EN pairs (no i18n context exists there by design).
    62→  1105→---
    63→  1106→Task ID: 4-I4
    64→  1107→Agent: Group3-Signature-3D
    65→  1108→Task: Batch 3 items 12-15 — curl-noise flow field, aurora backdrop, envMap, scroll dolly
    66→  1109→Work Log:
    67→  1110→- Read worklog tail (3-convergence contract, 4-I1, 4-I2, loops2-G1+G2 G1-d/G2-c findings) + all four target files + hero.tsx (deferred mount / IO-gated active), hero-fallback & --background-deep tokens (#08080A), eslint config, three@0.185.1 exports map, @types/three RoomEnvironment.d.ts.
    68→  1111→- src/components/home/hero-canvas.tsx (items 12+13+15, full motion rework): replaced per-particle sin/cos jitter with a proper 3D simplex CURL field — Ashima snoise (canonical, MIT) + snoiseVec3 + finite-difference curl (6 samples, safe-normalized, divergence-free => neighbouring particles share velocity => streaming ribbons). Vertex math: q = seed*0.16 + time-translating offset (field structures sweep past volume), p += curl*amp (amp = 1.1+aSpeed*1.6, swelled ×(1+uScroll*0.4)); slow global drift = embers rising on wrapped y-domain (mod, DOMAIN 20) with edge fade smoothstep(8.5,10) masking the recycle seam; kept uMouse*0.18 attraction, distance fade, soft-round additive sprites, point-size clamp. Count 3500→4500 (still 1 draw call, same buffer attributes + lazy useState initializer). COLORS → #F59E0B/#FBBF24/#34A853/#10B981/#FFF3D6 (weighted, zero blues). Uniforms: uTime/uMouse/uPixelRatio/uScroll (4).
    69→  1112→- Aurora backdrop INSIDE the same Canvas (no second Canvas, mounts with the chunk): 64×32 plane at z=-9, renderOrder -1, depthWrite/depthTest false, 4-octave value-noise fbm (rotated octaves), two drifting amber/green curtains + warm overlap core + broad cursor wash (world-space projected uMouse, aspect-correct) + vignette on the light. CRITICAL blend fix: transparent canvas + plain AdditiveBlending would accumulate alpha≈1 over the fullscreen plane and HIDE the CSS stage — used CustomBlending (blendSrc/blendDst ONE/ONE, blendSrcAlpha/blendDstAlpha ZERO/ONE) so canvas alpha stays 0 = true additive light OVER the page: deep bg, hero-fallback gradient AND the WS-3 blueprint spotlight grid stay visible beneath.
    70→  1113→- Scroll dolly (item 15): new src/lib/hero-scroll.ts — module-level bridge { bindHeroScroll(mv): unbind, getHeroScroll(): number } (getter guards undefined → 0). methodology.tsx: ONE useEffect line feeds its existing useScroll scrollYProgress into the bridge (+2 import tweaks; no restructure). hero-canvas: ScrollDolly component eases camera z 6→6.8 via state.camera (useFrame param — avoids the useThree-return immutability error), RTL-safe (view-axis only); Particles lerp uScroll from the same bridge.
    71→  1114→- capability-scene.tsx (item 14): RoomEnv component — import { RoomEnvironment } from 'three/examples/jsm/environments/RoomEnvironment.js' (verified: file exists in three@0.185.1, './examples/jsm/*' in exports map, @types/three ships the .d.ts; no new dependency). useEffect: PMREMGenerator(gl).fromScene(new RoomEnvironment(), 0.04).texture → scene.environment (eslint-disable react-hooks/immutability, canonical three IBL wiring); disposes room + target + pmrem on unmount. Lights untouched (I-1 amber/green/warm-white). envMapIntensity already 1 on knot/satellites.
    72→  1115→- Also added ContextLossGuard to hero-canvas (mirrors capability-scene — KEEP list).
    73→  1116→- Verification: scoped `bunx eslint` on all 4 files → 0 errors (1 benign react-compiler warn, same class as existing convention). NO tsc (coordinator gates centrally). Structural GLSL check (balanced braces/parens, terminators) via node script. Grep: zero blue hexes/rgba in my files. No browser/dev/build touched. Git scope: exactly hero-canvas.tsx, methodology.tsx (+7 lines), capability-scene.tsx (+32), new src/lib/hero-scroll.ts.
    74→  1117→Stage Summary:
    75→  1118→- DONE 12: curl-noise flow field (4500 particles, brand palette, single draw call, full perf architecture preserved: deferred chunk, IO-gated frameloop, visibility pause, dpr clamp, pointer tilt+attraction, gradient fallback, additive soft sprites, JSX disposal, context-loss guard).
    76→  1119→- DONE 13: aurora plane behind particles in the same canvas — fbm curtains amber/green, uTime drift, uMouse world-space light, vignette; CustomBlending keeps CSS stage visible; reduced-motion covered by parent skip.
    77→  1120→- DONE 14: /services/websites knot has PMREM RoomEnvironment IBL (envMapIntensity 1), fully disposed.
    78→  1121→- DONE 15: methodology useScroll → module bridge → hero camera dolly (+0.8) + flow swell; guarded, RTL-safe.
    79→  1122→- RISKS for coordinator: (1) GLSL not compile-tested (browser forbidden) — Ashima snoise is canonical and structurally verified, but a browser console check for shader warnings is advised; (2) blind-tuned amplitudes (curl amp, aurora brightness, dolly 0.8) may want a visual pass; (3) tsc not run — TS patterns mirrored from existing files (React.MutableRefObject, bufferAttribute args, useThree selectors, Material blend props).
    80→  1123→---
    81→  1124→Task ID: 5-group4-verification
    82→  1125→Agent: coordinator (Group 4 — comprehensive verification)
    83→  1126→Task: Verify everything Group 3 implemented; fix what verification found; close the loops plan.
    84→  1127→
    85→  1128→Work Log:
    86→  1129→- Gates: bunx tsc --noEmit = 0 · bun run lint = 0 (after fixing a react-compiler warning in capability-scene.tsx by extending the eslint-disable comment to the correct rule).
    87→  1130→- Live E2E (agent-browser, one session): home palette live (CTA rgb(180,83,9) amber-700), zero console/page errors; hero prompt → /contact?service=store&idea=… prefill verified end-to-end (message "أرغب ببناء متجر ذكي: …"); contact form shows whatsapp field (dir=ltr) + 6 project chips + social proof (لمسة/عقار) + wa.me deep-link; MiniAgent input + "حوّل هذا إلى ططلب" → service=agent prefill; full simulator (button "جرّبها الآن", NOT the bento mini "شغّل التدفق") runs ~15s → completion title + visible CTA → /contact?service=automation; 404 recovery links present; EN locale intact (dir/lang/placeholders); mobile 390px: no horizontal overflow, compact EN toggle visible+44px, nav targets 44px (hamburger/sound/lang).
    88→  1131→- API/rate-limit: 8 consecutive invalid payloads → all 400 (no lockout), then valid → 201. Form submit E2E → toast + persisted row (whatsapp stored). NOTE: a rapid first click right after page load can be swallowed pre-hydration in dev automation — recovers on next click; judged a dev-mode timing artifact, not a product bug.
    89→  1132→- BUG FOUND & FIXED (1): locale-switch scroll restore silently failed — root cause StrictMode double-effect consumed the single-use sessionStorage entry before the timeout fired (console-instrumented proof: "restore scheduled" logged, "restore firing" never). Fix: consume the entry INSIDE the timeout + 5s expiry (language-switcher.tsx). Re-verified: scrollY 1100 preserved across AR↔EN.
    90→  1133→- BUG FOUND & FIXED (2, MAJOR, pre-existing): the 3D hero canvas was rendering at the HTML default 300×150 inside a 0-height wrapper — .elyra-spotlight > * {position:relative} (unlayered, beats layered Tailwind utilities) collapsed the hero background layer div. The pretty CSS fallback (dark bg + spotlight grid) had masked it since the spotlight rule landed. Fix: scope the lifting rule with :not([data-bg-layer]) + data-bg-layer attr on the hero bg div. Had to wipe a stale .next cache (Turbopack served old CSS chunk after edits, even across a server restart) — after wipe the fix served.
    91→  1134→- BUG FOUND & FIXED (3): stale Turbopack watcher/cache after OOM-kill history — documented workaround: rm -rf .next + restart via double-fork (bash -c '(( exec bun run dev > dev.log 2>&1 ) &)') which also survives across bash tool calls (plain nohup/setsid variants were reaped).
    92→  1135→- Visual gates (VLM): hero now shows glowing particle field (white/amber/green) + soft aurora wash behind text, no defects (g4-hero4); /services/websites envMap scene renders actively (canvas 1150×574, heavy software-GPU load starved CDP screenshots — code-verified + zero errors; shininess confirmed only by active render, not VLM).
    93→  1136→- Routes sweep: /, /en, /services/websites, /services/automation, /work, /about, /contact, /en/contact, robots.txt, sitemap.xml all 200; /ghost-404 → 404.
    94→  1137→- Cleanup: 6 test leads deleted (sara×2, omar, laila, nour, valid-after-rejections) → DB count 0.
    95→  1138→- Commits: 2a282dc (wave 1) · 9fbd8a0 (wave 2 + verification fixes).
    96→  1139→
    97→  1140→Stage Summary:
    98→  1141→- ALL agreed Batch 1/2/3 items implemented and verified live, plus 3 verification-discovered bugs fixed (StrictMode scroll restore, hero canvas collapse, stale cache handling).
    99→  1142→- Known deferred (next round, agreed): /work/[slug] case-study pages (top priority), View Transitions, Lenis, WebGL hover distortion, overlay menu, magnetic displacement, calculator extensions.
   100→  1143→- Environment lessons recorded: never run concurrent tsc with the dev server (OOM), double-fork for server survival, single browser session at a time.
   101→  1144→---
   102→  1145→Task ID: 4
   103→  1146→Agent: 3D Visuals Specialist
   104→  1147→Task: Upgrade capability-scene (liquid-glass iridescent blob + orbitals + halo) and console-scene (glow sprites, curved tubes, traveling pulses)
   105→  1148→
   106→  1149→Work Log:
   107→  1150→- Read worklog tail (Task 0 digest, 4-I4 3D conventions, 5-group4 verification + environment lessons) + both target files + hero-canvas.tsx (NOISE_GLSL source, point-sprite pattern, uniform-mutation eslint-disable convention) + eslint.config.mjs (react-hooks/immutability/purity = error). Confirmed both scenes are dynamic ssr:false imports (three-d-section.tsx / hero-console.tsx) → document access at component scope is safe.
   108→  1151→- src/components/three/capability-scene.tsx (full visual rework, component API `CapabilityScene({ active })` unchanged): replaced the TorusKnot with a custom-shader "liquid glass / iridescent" blob — IcosahedronGeometry(1.1, 5) displaced in the vertex shader by two time-translated simplex octaves (Ashima NOISE_GLSL copied from hero-canvas, GLSL1 attribute/varying style), amplitude uAmp 0.12 with slow sin breathing, normals recomputed per-frame via tangent-frame finite differences on the GPU (seam-safe: duplicated icosphere verts share positions). Fragment shader: Fresnel rim pow 2.4, iridescent gradient #0071E3→#4285F4→#60A5FA driven by the noise field, #34A853 thin-film tint only at grazing angles (×fresnel 0.30), soft deep-blue inner glow pow(ndv,2.5), animated sheen + one fake view-space key-light glint.
   109→  1152→- Added 3 glass satellites (sphere + MeshPhysicalMaterial roughness .05 / clearcoat 1 / envMapIntensity 1.6 — glassy under the kept RoomEnvironment IBL without a transmission pass) on tilted elliptical orbits inside the drag group; each orbit path marked by a thin TorusGeometry ring (r .006, additive, opacity .16, depthWrite off) non-uniformly scaled to match the ellipse; satellite positions driven in useFrame via ref-callback mesh array (cos/sin in the tilted local plane).
   110→  1153→- Added 600-point particle halo in a spherical shell (r 2.1–3.2): ShaderMaterial points with soft-round additive sprites (hero-canvas fragment pattern), brand-blue palette + green/white counterpoints, per-point scale/phase/color attributes, gentle shell breathing + twinkle in the vertex shader, slow independent rotation (not driven by drag). Math.random confined to a lazy useState initializer (react-hooks/purity).
   111→  1154→- Kept verbatim: RoomEnv PMREM IBL (+ its eslint-disable line), ambient + 4 brand point lights, ContextLossGuard, WebGL feature detection, DPR [1,2], frameloop={active?'always':'never'}, camera [0,0,5] fov 50, drag-to-rotate with pointer capture + 60Hz throttle + auto-spin, mouse parallax lerp (now on a parent group so blob+satellites+halo drift together). Disposal: useEffect frees blobGeo/blobMat/haloGeo/haloMat (prop-passed — R3F skips those); satellites/rings are JSX-declared so R3F disposes them. Uniform time updates carry the repo's eslint-disable react-hooks/immutability comments; satellite positions mutate only through ref.current.
   112→  1155→- src/components/home/console-scene.tsx (exported API `ConsoleScene({ preset, active = true })` + PresetId unchanged; PRESET_CONFIG + buildPositions + InitialRenderSafety + rotation drift + reduced-motion frameloop gating all untouched): nodes upgraded to 0.32 radius / 32×32 spheres with an additive glow sprite behind each (one shared 64×64 white radial-gradient CanvasTexture generated in a component-scope useMemo, reused by every SpriteMaterial; depthTest off + renderOrder 2 so it reads as bloom instead of being clipped by its own sphere). Node emissiveIntensity pulses 0.9±0.5·sin(t·1.7 + i·1.9) in useFrame via material ref-callback array.
   113→  1156→- Replaced straight THREE.Line connections with QuadraticBezierCurve3 arcs (midpoint lifted ±0.6 perpendicular to the chord via cross(dir, UP) with a parallel fallback) rendered as TubeGeometry (radius 0.012, 24 segments, 8 radial) meshes with additive transparent MeshBasicMaterial (~0.5 opacity, cross-chords 0.22) — still built imperatively + mounted via <primitive> (same `<line>` JSX/SVG bypass as P0-1). Ring neighbor arcs alternate bow direction, flow arcs bow consistently toward camera, orbit spokes radial.
   114→  1157→- Added traveling pulses: one bright whitened sphere (r 0.05, additive) per main connection, position = curve.getPointAt((t·speed + phase) % 1) with opacity envelope 0.2+0.8·sin(πu) (bright mid-path, fading at endpoints); speeds 0.22–0.36 loops/s, phases spread ×0.37; static initial pose baked at mount so the reduced-motion/offscreen frameloop='never' state still looks intentional. Pulse meshes/materials are JSX-declared (R3F-disposed); curves + timing live in pure memo metadata.
   115→  1158→- Perf budget respected: tubes ≤ 12 per preset (ring 6-node = 12 exactly), pulses ≤ node count, no post-processing, no drei, no new deps. Disposal useEffect extended to tubes (geometry + material, array-safe) + the shared glow texture (disposed once — Material.dispose never frees textures); comment documents the accepted StrictMode re-upload semantics.
   116→  1159→- Verification: `bunx tsc --noEmit` → exit 0. `bun run lint` → exit 0, zero errors AND zero warnings (no new react-compiler warns — all useFrame mutations go through ref.current or carry the repo's immutability disable). Scoped eslint on both files → 0. GLSL structural check (brace/paren balance, no GLSL3 syntax) via node script → PASS. dev.log shows clean Turbopack recompiles after the edits (no server restart). No other files touched (git scope: exactly the two scene files).
   117→  1160→
   118→  1161→Stage Summary:
   119→  1162→- capability-scene: metallic knot → liquid-glass iridescent noise-displaced blob (GPU-side displacement + recomputed normals, Fresnel rim, brand-blue iridescence, green grazing tint, inner glow, fake glint) + 3 IBL glass satellites on tilted elliptical additive-ring orbits + 600-point brand-palette halo; full interaction/perf/disposal architecture preserved.
   120→  1163→- console-scene: emissive-pulsing nodes with shared-canvas glow-sprite bloom, curved bezier tube connections, and traveling energy pulses per link; preset layouts/colors/rotational drift/reduced-motion + disposal patterns preserved.
   121→  1164→- RISKS for coordinator: (1) GLSL not compile-tested in a browser (forbidden) — noise block is the already-live hero-canvas verbatim, blob shaders structurally verified; a visual pass for amplitude/brightness tuning is advised; (2) glow sprites use depthTest:false (bloom-style overlay) — if rear-node halos feel too strong through front nodes, lower sprite opacity 0.5→0.35 or restore depthTest; (3) tube/pulse counts sit exactly at the agreed budget for the 6-node ring preset — do not add more links there without raising the budget.
   122→  1165→
   123→  1166→---
   124→  1167→Task ID: R1-R6 (user round: blue revert + scroll animations + 3D + intro)
   125→  1168→Agent: main (Full Stack Agent) + 3D Visuals Specialist (Task 4)
   126→  1169→Task: User request — (1) revert orange to the blue palette, (2) bigger/clearer scroll-animated work cards + enable the scroll animation on mobile, (3) scroll animations site-wide, (4) better 3D models, (5) cinematic homepage entry animation.
   127→  1170→
   128→  1171→Work Log:
   129→  1172→- Task 1 (blue revert): restored the original blue token set in globals.css (--primary #0071E3, --primary-strong #0066CC, --accent #E8F2FF, --g-blue #4285F4, --g-red #EA4335, --g-yellow #FBBC05) + all derived rgba()s (glow-cursor, scroll-dark, elyra-pulse, card-hover-lift, hero-fallback radials). Swept 20+ files: hero-canvas COLORS/aurora → blue family; capability/console scene lights+lines+presets; bento SITE_PALETTE + cube gradients; work-grid/featured-work accents; before-after FALLBACK_RGB + amber utilities; calculator slider; automation-simulator gradient; navbar/testimonials/hero-console/about comments; logo + icons + OG images restored to the EXACT original quad-dot (#4285F4/#EA4335/#FBBC05/#34A853) and #0071E3 CTA in not-found/global-error. Commit 8a56762.
   130→  1173→- Task 2 (bigger card + mobile): rewrote DeconstructedCard — ONE unified sticky scene for all viewpoints (the md:hidden static stack is gone; phones now get the scroll-driven explosion). Card is fluid: width min(30rem, 88vw), aspect 5:3 (480×288 desktop vs old fixed 320×192). Geometry is measurement-driven (ResizeObserver): frontSep=0.5h+40, backSep=0.52h+42, X=±4.5%w, plus a vh-damp scale (0.72–1) for short viewports. Label moved INSIDE the back layer (top:100%) — glued to the cascade with zero projected-edge math. Bolder layer content (bigger mock UI, size-8 icons, text-4xl metric, text-2xl label).
   131→  1174→- Task 3 (site-wide scroll animations): Reveal grew variants (up/zoom/blur/left/right/clip, all scripting-safe + reduced-motion-safe) + new Parallax component (rAF-coalesced, IO-armed, transform-only). Applied: work-grid cards (zoom + stagger, gap-10, larger type), featured-work teaser, trust-bar stat cells, bento GlowCards (zoom stagger, h-full stretch preserved), testimonials (blur + parallax glow), about values/team (zoom), contact (right slide + zoom), services pages (zoom + alternating slides), CTA (zoom). New global ScrollProgress bar (3px brand gradient, RTL-aware origin, in [locale]/layout). Commit ea9951d.
   132→  1175→- Task 4 (3D upgrade, delegated to 3D Visuals Specialist agent): capability-scene → custom-shader liquid-glass iridescent blob (simplex-displaced icosahedron, fresnel rim, blue iridescence, green thin-film at grazing angles) + 3 glass satellites on tilted elliptical orbits with thin additive orbit rings + 600-point particle halo; console-scene → glowing pulse nodes with additive glow sprites + QuadraticBezier tube connections + traveling bright pulses. Same APIs, full disposal, no new deps. (Agent appended its own worklog entry.)
   133→  1176→- Task 5 (entry animation): IntroOverlay — cinematic once-per-session intro: pre-paint beforeInteractive gate script in the root layout arms data-intro (first visit) / data-intro-off (repeat); CSS pauses hero entrance while the veil shows so it plays INTO the curtain lift; mark scales in with glow, Arabic-safe clip-path wordmark wipe (joining intact), tagline + gradient line, curtain lifts at 1.7s (0.85s expo), skip on any intent, no-JS CSS failsafe at 4.5s, reduced-motion never renders. New i18n key meta.tagline (AR/EN parity). Commit 9b4f19a.
   134→  1177→- Task 6 (verification): server restarted after an infra stall (git checked clean first). Browser E2E (single sequential session): intro sequence VLM 10/10; deconstructed card desktop 9.5/10 + MOBILE 9/10 (scroll explosion confirmed working on 390×844); /work cards 10/10; 3D blob scene confirmed rendering (glossy blue blob, orbit rings, satellites, particle halo); hero console 8/10 (glowing nodes + traveling pulses); mobile bento 9/10 + footer 10/10 sticky; full-scroll section sweep 8–10/10 with ZERO blank viewports (the one-shot --full screenshot "gap" is the known lazy-load IO artifact — real scrolling renders everything); EN locale + LTR ✓; scroll progress bar live ✓; console errors: none. Gates: tsc=0, lint=0. DB leads: 0 (no E2E writes this round).
   135→  1178→
   136→  1179→Stage Summary:
   137→  1180→- Blue palette fully restored (token-level, zero amber hex left; exact original quad-dot in logo/icons/OG).
   138→  1181→- The signature scroll card is now bigger, clearer, and works on mobile — one unified implementation, measurement-driven geometry.
   139→  1182→- Site-wide scroll-motion language: Reveal variants + stagger everywhere, Parallax glow, global scroll progress bar.
   140→  1183→- 3D scenes upgraded to premium visuals (liquid-glass blob + living constellation) within the no-new-deps constraint.
   141→  1184→- Cinematic once-per-session intro with hero coordination, skip intents, no-JS/reduced-motion safety.
   142→  1185→- All commits: 8a56762 → ea9951d → 9b4f19a (+ this round's final commit).
   143→  1186→
   144→  1187→---
   145→  1188→Task ID: 3
   146→  1189→Agent: 3D Visuals Specialist
   147→  1190→Task: Wave 1-c — replace hero aurora backdrop with a domain-warped SILK/flow-field shader (hero-canvas.tsx only)
   148→  1191→
   149→  1192→Work Log:
   150→  1193→- Read worklog tail (3D conventions from 4-I4 / Task 4 / Task 1: GLSL1 attribute/varying style, eslint-disable react-hooks/immutability on uniform mutation, no concurrent tsc with dev server, single-browser-session, node structural GLSL check) + full hero-canvas.tsx. Verified AURORA_* symbols are referenced nowhere outside the file (safe rename).
   151→  1194→- src/components/home/hero-canvas.tsx — renamed AURORA_FRAGMENT→SILK_FRAGMENT, AURORA_VERTEX→SILK_VERTEX, AuroraBackdrop→SilkBackdrop, AURORA_PLANE_Z/DIST→SILK_PLANE_Z/DIST; file export surface unchanged (HeroCanvas); Particles (curl system), ScrollDolly, ContextLossGuard, WebGL detection, pointer tracking, Canvas props (dpr/frameloop/camera), CustomBlending (ONE,ONE / ZERO,ONE), plane 64×32 @ z=-9, renderOrder -1, depthTest false, frustumCulled false, vertex shader — all preserved verbatim.
   152→  1195→- SILK_FRAGMENT: hash/vnoise kept verbatim; fbm 4→5 octaves (same inter-octave rotation matrix). Two-level IQ domain warp: q = fbm(p + vec2(0.6t, −0.4t)), r = fbm(p·1.3 + vec2(q·1.7, q·1.1) + vec2(−0.5t, 0.3t) + 2.7), f = fbm(p + r·2.2), t = uTime·0.1 → effective offsets 0.06/−0.04/−0.05/0.03 units/s counter-drifting (fields shear → folds re-form, never loop). Exactly 3 fbm calls, only loop is fbm's, no derivatives, GLSL1 only.
   153→  1196→- Mouse vortex: pos = uMouse + rot(0.35·exp(−md²/24))·(vPos−uMouse) — gaussian stir radius ≈6 world units, ≤0.35 rad at centre, total displacement < ~0.7 units (composition stays calm); uMouse still CPU-lerped world-space projection (0.05). Broad cursor wash kept but now blue-family only (mix(uColorC,uColorA,0.75)·exp(−md²/220)·0.10) since green is reserved for filaments.
   154→  1197→- Colour: uColorA #4285F4 body hue-graded by q through derived midBlue mix(C,A,0.55)≈#0071E3; lightBlue mix(A,white,0.22)≈#60A5FA ridge sheen; NEW uColorC #0A2A5E undertone in low-energy zones (deep² weighting, coeff 0.30); uColorB #34A853 confined to thin filaments = narrow level band of the r field (smoothstep(0.46,0.55)·(1−smoothstep(0.60,0.70))) × body × sheen. Uniforms object: ADDED uColorC + uScroll only (backwards-compatible shape).
   155→  1198→- Intensity discipline: hard per-channel governor col = min(col, vec3(0.42)); body coeff 0.16+0.12·ridge, ridge sheen 0.20, filaments 0.40, undertone 0.30, wash 0.10. Old aurora's uncapped peak was ~0.54 linear blue on double-curtain cores — the new cap is strictly darker; large-area average ≈0.04–0.06 linear blue (moody). vert melt window kept verbatim; radial vignette kept (centre nudged 0.5→0.44) + lift 0.85–1.15 vertical bias so the silk is brightest in the upper-middle band behind the headline.
   156→  1199→- uScroll wired in SilkBackdrop useFrame from getHeroScroll() with the same 0.08 lerp + eslint-disable react-hooks/immutability convention as Particles; shader swell = 1.0 + uScroll·0.25.
   157→  1200→- Doc comments updated: file-header "Aurora backdrop (item 13)" paragraph rewritten to the silk model; SILK_FRAGMENT header comment describes warp structure/vortex/filaments; blending comment "beneath the silk".
   158→  1201→- Verification (NO tsc/lint/browser per constraints): (1) node structural check /tmp/task3/silk-check.mjs — delimiter balance + GLSL1-only (no in/out at global scope, no #version/layout/texture) + all called fns defined for all 4 shaders, exactly 3 fbm() calls + 5 octaves, GLSL uniform set == SilkBackdrop TS uniform keys (uColorA/B/C, uMouse, uScroll, uTime), 17 preservation anchors (curlNoise, PARTICLE_COUNT 4500, CustomBlending factors, renderOrder −1, plane 64×32, frameloop gating, dpr [1,2], HeroCanvas export…), zero stale aurora naming → ALL PASS. (2) Single-file SWC transpile via `bun build <file> --external …` (syntax-only, not tsc) → exit 0. (3) Diff re-read twice for TS strictness: no unused vars, `state`/`delta` both used, uniform literal shape matches shader, React.MutableRefObject pattern unchanged. (4) dev.log tail: no compile crash, zero errors mentioning hero-canvas; a pre-existing Turbopack tokio panic ("inner_of_upper_lost_followers") predates my edit (log had no new lines across the edit window; subsequent GET / are 200).
   159→  1202→- Git scope: exactly src/components/home/hero-canvas.tsx (115 insertions, 48 deletions). No other files touched; no new deps.
   160→  1203→
   161→  1204→Stage Summary:
   162→  1205→- Hero backdrop is now a domain-warped silk field (3×5-octave fbm warp chain, IQ q/r/f structure) with a cursor vortex stir + broad wash, graded brand blues (#4285F4 body / ≈#0071E3 depth / ≈#60A5FA sheen / #0A2A5E undertone), green #34A853 only in thin warp-crossing filaments, uScroll swell, hard 0.42 linear per-channel cap guaranteeing the additive layer stays premium-dark beneath the giant typography.
   163→  1206→- Tuning risks for coordinator visual pass (each a single knob in SILK_FRAGMENT): (1) overall too bright → body coeff 0.16→0.12 (and 0.12·ridge→0.09); (2) feels too static → t scale 0.1→0.14; (3) vortex too subtle → 0.35→0.5 rad or falloff /24→/36; (4) green filaments too rare/faint → widen lvl band to (0.42–0.74) or raise 0.40→0.5; (5) undertone invisible → 0.30→0.40; (6) headline zone too busy → lift 0.30→0.18 or vignette centre 0.44→0.40. Peak is capped at 0.42 linear per channel (old aurora peaked ~0.54 uncapped) so worst case is DARKER than the proven previous look.
   164→  1207→- Not done by design: scalar q/r warp (vec2 warps would need 5 fbm calls — over the 3-call budget), no GLSL3, no new deps. GLSL not browser-compiled (browser forbidden this round) — structural verification only, same as previous 3D rounds; a 30-second visual pass on / is advised before sign-off.
   165→  1208→
   166→  1209→---
   167→  1210→Task ID: 2
   168→  1211→Agent: Sensory Polish Specialist
   169→  1212→Task: Wave 1-b — site-wide sensory layer: custom cursor, animated film grain, magnetic buttons
   170→  1213→
   171→  1214→Work Log:
   172→  1215→- Read worklog tail (environment lessons: no concurrent tsc with dev server, single browser session, eslint purity/immutability/set-state-in-effect at error level, Math.random only in lazy useState) + use-reduced-motion.ts, use-cursor-velocity.ts (rAF-coalesced pattern), existing Phase 2 sensory files (magnetic-cursor.tsx / film-grain.tsx), hero.tsx, [locale]/layout.tsx, globals.css (z-order plan: intro 80 / grain 90 / skip-link 100 / cursor 200; global reduced-motion override COLLAPSES durations rather than removing animations).
   173→  1216→- NEW src/components/sensory/custom-cursor.tsx ('use client'): 7px dot (lerp 0.4) + 34px ring (lerp 0.16), single permanent rAF loop writing translate3d via refs ONLY when a value moved >0.01px (documented choice over cancel/reschedule). Enabled = useSyncExternalStore('(pointer: fine)') (exact use-reduced-motion pattern, module-level MQL singleton — swapped in after eslint flagged set-state-in-effect for the useState version) AND !usePrefersReducedMotion(); SSR renders null. Each layer = POSITIONER (per-frame translate3d + mix-blend-mode: difference — same proven placement as Phase 2) wrapping a visual CORE (CSS-transitioned ~220ms scale) + label span; splitting position from scale is what lets hover growth ease via CSS without easing the per-frame position writes. Hover via pointerover/pointerout delegation with closest('a, button, [role="button"], [data-cursor], input, textarea, select, label') + relatedTarget same-hit check; ring 1.9×/dot 0.4×; label mode (data-cursor-label, falling back to common.cursor.* for WS-7 contexts zoom/inspect/external/rotate/drag/preview — magnet stays unlabeled) grows ring to ~72px with white/10 fill + centered 11px dir=auto text; pointerdown squeeze 0.85×; is-hidden until first pointermove (snap-to-pointer, no corner fly-in) and on documentElement mouseleave; non-mouse pointerdown tears the engine down for the session (native cursor returns). Adds html.elyra-cursor-active; full listener cleanup.
   174→  1217→- NEW src/components/sensory/grain-overlay.tsx (server component, zero JS): single inert .grain-overlay div; CSS in globals.css = feTurbulence fractalNoise baseFrequency 0.72 / numOctaves 2 / saturate 0 data-URI SVG (%23-encoded), 240px tile, opacity 0.045, z-90, 8-step background-position flicker at 0.4s steps(1); 100% keyframe returns to base 0 0 so the reduced-motion duration-collapse lands on the static base state.
   175→  1218→- NEW src/lib/use-magnetic.ts: useMagnetic(ref, {strength=0.3, radius=28}) — rAF-coalesced (pointermove stores position, on-demand loop reads getBoundingClientRect once per frame), pull clamped ±14px, settles within 0.2px then STOPS and removes the inline transform entirely (Tailwind hover:scale governs at rest). Suppresses the element's OWN transform transition (inline transition:none) for exactly the duration it writes — only if computed transition-property includes transform/all (primary CTA's transition-transform), so color transitions (secondary CTA) are untouched; restores on release + cleanup. armed-guard so the {0,0} default never magnetizes corner elements; scroll/resize listeners keep the offset honest under a resting pointer; no-op on reduced-motion/coarse pointer (reactive via usePrefersReducedMotion).
   176→  1219→- Wired layout.tsx: <FilmGrain/>+<MagneticCursor/> → <GrainOverlay/>+<CustomCursor/> (same spot inside NextIntlClientProvider — label fallback needs the catalog; comment rewritten). Old sensory files left on disk unmounted (their CSS above is inert; deleting them is a coordinator call).
   177→  1220→- Wired hero.tsx (surgical: 1 import, 1 ref decl + ref attr, 3 hook calls): useMagnetic on ctaPrimaryRef (shared with useCursorVelocity — var vs transform, no conflict), new ctaSecondaryRef, new badgeRef inside OrbitBadge.
   178→  1221→- Appended `/* ===== R7-b — Sensory layer (cursor / grain) ===== */` to END of globals.css via heredoc (append-only; file 1028→1195 lines): .grain-overlay + @keyframes grain-flicker, .elyra-cursor-layer/-dot/-ring (with explicit width/height/background/border RESETS because Phase 2 rules share the -dot/-ring names above), cores, is-hover/is-label/is-press/is-hidden states, .elyra-cursor-label (sibling of the scaled core so text never inherits ring scale), html.elyra-cursor-active native-cursor hiding (cursor:none !important under pointer:fine), print display:none for both layers.
   179→  1222→- Verification: scoped `bunx eslint` on all 5 touched TS files → exit 0 (fixed 2 errors it caught: set-state-in-effect → useSyncExternalStore; unused transitionsTransform → gated suppressTransition on it). NO tsc/build/browser (coordinator-gated). Dev server untouched; dev.log shows clean / and /en 200 recompiles after the edits; SSR HTML contains .grain-overlay and no old .elyra-grain/.elyra-cursor markup (cursor is client-only by design).
   180→  1223→- Scope: exactly 3 new files + layout.tsx + hero.tsx + globals.css append. NOT touched: hero-canvas.tsx, kinetic-heading.tsx, package.json, existing globals.css content, messages catalogs.
   181→  1224→
   182→  1225→Stage Summary:
   183→  1226→- DONE: difference-blend custom cursor (dot+trailing ring, delegation-based hover growth, ring-centered label mode incl. WS-7 fallback labels, press squeeze, leave-hide, touch teardown), animated 8-step film grain (reduced-motion-safe), magnetic pull hook on hero primary CTA + secondary CTA + orbit badge with transform-ownership discipline (inline transform + transition suppression released at rest).
   184→  1227→- Design decisions worth knowing: (1) old Phase 2 MagneticCursor/FilmGrain were REPLACED in the layout (two cursor engines would double-render) — files remain on disk unmounted, delete at will; (2) hover selector uses [data-cursor] (any value) not just "magnet" so work-grid zoom / footer external / simulator inspect keep their affordances, with common.cursor.* fallback labels preserving WS-7 chips; (3) cursor motion is difference-blend on the JS-transformed positioner (proven Phase 2 placement); label mode uses a plain white/10 fill, NO backdrop-filter (unreliable inside a blended+transformed group).
   185→  1228→- RISKS for coordinator: (1) mix-blend-mode: difference inside the new .elyra-cursor-layer wrapper (fixed + z-index, no transform/opacity/filter — spec-safe, but a visual check that the cursor INVERTS over light sections is advised; if it ever renders plain white, drop the wrapper or hoist the layers to body siblings like Phase 2 did); (2) tsc not run (React.RefObject without import follows use-cursor-velocity convention); (3) grain flicker is 20 jumps/s at 0.045 opacity — tune opacity/duration in one place (.grain-overlay) if it reads too nervous; (4) cursor is hidden over Sonner toasts if Sonner's own z-index exceeds 200 (same as Phase 2 — no regression); (5) useMagnetic runs one listener per magnetized element (3 on the hero) — negligible, but don't blanket-apply site-wide without batching.
   186→  1229→
   187→  1230→---
   188→  1231→Task ID: R7 (user round: "make the homepage extraordinary — not generic AI-made")
   189→  1232→Agent: main (Full Stack Agent) + Sensory Polish Specialist (Task 2) + 3D Visuals Specialist (Task 3)
   190→  1233→Task: User request — the homepage first impression felt ordinary/generic; wanted something distinctive with big animations and aesthetics that amaze.
   191→  1234→
   192→  1235→Work Log:
   193→  1236→- W1-a (main) Editorial hero redesign: hero.tsx rewritten as an asymmetric start-aligned editorial column — kicker row (pulse dot + agency line + MapPin + live Damascus clock, Intl.DateTimeFormat ar-SY/en-GB with Asia/Damascus timezone, Arabic-Indic digits on AR, hydration-safe placeholder), giant display h1 via rewritten KineticHeading (clamp 2.7rem→8.5rem, per-word mask rise .hr-line/.hr-word with overflow-hidden + padding/margin clip trick for Arabic diacritics, accent line = brand gradient via logical [dir] rules + spinning Sparkles star, staggered inline-start indents ms-[7vw]/ms-[1.5vw]), outlined ELYRA watermark (text-stroke, bleeding far corner), vertical scroll rail (writing-mode vertical-rl, ltr-only tracking, traveling light drop), rotating orbit badge (SVG textPath Latin seal, links /work), infinite service marquee (LTR track + per-item RTL shaping, ×2 duplication, -50% loop, edge mask, hover pause), console start-aligned with justify-start chips. i18n keys: hero.location/watermark/marquee[7] + manifesto namespace (AR/EN).
   194→  1237→- W1-b (subagent Task 2) Sensory layer: custom-cursor.tsx (7px dot + 34px trailing ring, mix-blend difference, single rAF lerp loop, hover growth via pointerover/out delegation incl. [data-cursor]/[data-cursor-label] label mode, press squeeze, document-leave hide, touch teardown, html.elyra-cursor-active + CSS native-cursor hiding), grain-overlay.tsx (server component, feTurbulence data-URI, 8-step steps(1) flicker), use-magnetic.ts (rAF-coalesced magnetic pull, ±14px clamp, settles and clears inline transform), wired into [locale]/layout + hero CTAs/orbit badge. Coordinator fixed 1 TS error (hoisted function declaration broke const narrowing in kill()).
   195→  1238→- W1-c (subagent Task 3) WebGL: aurora → SILK shader (Inigo Quilez 2-level domain warp, 3×fbm@5 octaves, mouse vortex rotation gaussian ~6wu, green thin filaments at warp-field crossings, deep-blue undertone, vertical melt window + vignette + upper-band lift, uScroll swell, CustomBlending additive-over-page preserved). Coordinator tuned intensity after pixel measurements (undertone .30→.42, body .16→.26/.12→.20, sheen .20→.30, filament .40→.48, cursor wash .10→.14, cap .42→.50, time scale .1→.13).
   196→  1239→- W2 (main) Manifesto section: manifesto.tsx — scroll-linked word-by-word lighting (26 AR/32 EN words, rAF-coalesced scroll handler armed by IO 25% margins, progress measured on the h2 NOT the section, direct style writes, 160ms opacity transitions, server-renders fully lit for no-JS/reduced-motion), inserted TrustBar→Manifesto→Bento.
   197→  1240→- W3 verification (single browser session): found & fixed MAJOR layout bug — .elyra-spotlight > * content-lift rule forced position:relative on the watermark/rail/marquee → 437px phantom in-flow height pushing the hero column below the fold (section 1686px on 900px viewport). Fix: data-bg-layer exemption on the three decorative absolutes. Second fix: h1 flex-col (flex containers don't collapse .hr-line negative margins — recovered ~80px). Post-fix geometry: kicker 96→CTA row 886 of 900 viewport, console at fold.
   198→  1241→- Verified: DOM (h1 3 lines correct, 6 masked words opacity 1, clock ticking ١٥:١٦:٣٨, 14 marquee items, watermark stroke, rail, orbit textPath, grain, no overflowX); manifesto progression (all dim at 86% scroll entry → 23/26 lit mid-read with fading tail + hidden signature → all lit + signature 1.0 at completion — after fixing the section-vs-title progress rect bug); console conversion path (typed "متجر عطور فاخرة" + Enter → /contact?service=store&idea=… → message prefilled "أرغب ببناء متجر ذكي: متجر عطور فاخرة" + whatsapp field + chip pressed); mobile 390×844 (no overflow, h1 43.2px full width, marquee 48px, console in fold, orbit badge correctly hidden); EN locale (dir ltr, "We Craft | Digital | Experiences", latin clock, gradient to right, 32 manifesto words); silk rendering (pixel-measured: blue-dominant 18%→61% after boost, right-upper mean (41,48,67), animation confirmed via 8s frame diff 30.3% subtle drift); VLM verdicts: hero wow 9/10 "Awwwards Site of the Day caliber, Arabic joining flawless, no diacritic clipping", manifesto 8/10 premium/intentional, mobile 8/10, background atmosphere 8/10 readable. Gates: tsc=0, lint=0. DB leads: 0.
   199→  1242→- Custom cursor code-verified only (headless env reports pointer:none so it correctly stays disabled there; activates on real pointer:fine devices).
   200→  1243→- Cleanup: deleted orphaned Phase-2 files (sensory/film-grain.tsx, sensory/magnetic-cursor.tsx — unmounted dead code superseded by R7-b components).
   201→  1244→
   202→  1245→Stage Summary:
   203→  1246→- Hero transformed from centered generic SaaS → asymmetric editorial-cinematic composition with masked word reveals, live Damascus clock, outlined watermark, vertical rail, orbit seal badge, service marquee edge, blend-difference cursor, film grain, magnetic CTAs, domain-warped silk WebGL backdrop, and a scroll-lit manifesto statement.
   204→  1247→- Key architectural lessons: (1) .elyra-spotlight's content-lift rule forces position:relative on ALL non-bg-layer children — any new decorative absolute in spotlight sections MUST carry data-bg-layer; (2) mask-reveal line wrappers need flex-col parents to avoid negative-margin collapse; (3) additive shader intensity must be pixel-verified (VLM undersells subtle luminance; measure means + blue-dominance).
   205→  1248→- Commits: this round's work committed as R7.
   206→  1249→
   207→  1250→---
   208→  1251→Task ID: L1-A
   209→  1252→Agent: Security Audit Specialist (Group 1 — Analysis)
   210→  1253→Task: LOOP 1 deep security audit of API/leads pipeline, rate limiting, webhook, config, headers, robots/sitemap
   211→  1254→
   212→  1255→Work Log:
   213→  1256→- Read worklog tail (project conventions: no concurrent tsc with dev server, eslint at error level, analyst-only role, worklog is the only writable file).
   214→  1257→- Read IN FULL: src/app/api/leads/route.ts (482 L), src/lib/rate-limit.ts (87 L), src/lib/n8n-webhook.ts (198 L), src/lib/lead-fields.ts (85 L), src/lib/api-i18n.ts (35 L), src/proxy.ts (13 L), src/app/robots.ts (14 L), src/app/sitemap.ts (46 L), src/app/[locale]/opengraph-image.tsx (189 L), src/app/[locale]/apple-icon.tsx (86 L), src/app/icon.tsx (86 L), next.config.ts (82 L), .env.example (32 L), src/components/pages/contact-form.tsx (321 L).
   215→  1258→- Cross-referenced the full request lifecycle: contact-form.tsx / calculator.tsx fetch → route.ts POST → header gates → CLIENT_ECHO strip → Zod strict parse → honeypot → strict bucket → Prisma create → fire-and-forget webhook → response. Also read supporting files: src/lib/db.ts, src/lib/calculator.ts (computeEstimate — bounded, no ReDoS), src/lib/seo.ts, src/lib/site-config.ts, prisma/schema.prisma, scripts/clean-leads.ts, the root reverse-proxy file, Dockerfile, package.json, messages/{ar,en}.json apiErrors block (all keys present), src/app/[locale]/contact/page.tsx (searchParams path), src/components/seo/home-json-ld.tsx, examples/websocket/server.ts, README (Deployment + webhook recipe sections).
   216→  1259→- Environment checks: `ls -la .env*` (.env exists, 50 B, untracked; .env.example tracked). `git ls-files | rg env` → only .env.example. Git history audit of .env* with VALUES REDACTED (key names only, never printed): commit 6d367b6 "Initial commit" ADDED .env (single key: DATABASE_URL — a local SQLite path, not a credential); commit b659e9b deleted it; current .env untracked & ignored (.gitignore:35 `.env*`). `git remote -v` → origin URL embeds an `x-access-token:<token>@github.com/...` credential (token value auto-redacted by tooling; existence noted, never printed).
   217→  1260→- Grep checks: `dangerouslySetInnerHTML` → exactly one usage (home-json-ld.tsx, static build-time data only). `N8N_WEBHOOK|NEXT_PUBLIC_` in src → N8N_* only in server-only src/lib/n8n-webhook.ts; NEXT_PUBLIC_ only SITE_URL (seo.ts:5, layout.tsx:99). No other API route exists (glob src/app/api → leads/route.ts only; exports POST + OPTIONS, no GET → no lead-read endpoint).
   218→  1261→- Deps skim: zod resolved to 4.3.5 (bun.lock:1153); all packages mainstream, no red flags. dev.log / db/*.db gitignored. tool-results/ untracked-but-not-ignored (was purged from tracking once before, commit 887c4dc).
   219→  1262→- Explicit mandated verifications: (a) rate-limit Map cleanup EXISTS — rate-limit.ts:36-54 sweep every 60 s + 5 000-key hard cap dropping oldest 1 000; (b) ALL string lengths bounded — lead-fields.ts (name 2–100, email ≤254, whatsapp 5–30 + charset regex, message 10–5000, honeypot ≤200) + route.ts strictObject + integrations .max(6); (c) webhook error paths log to SERVER console only (n8n-webhook.ts:112,189-193) and route.ts:463-470 returns a generic translated 500 — no URLs/stacks/Prisma details reach the client; (d) NO secret in client-visible code — N8N_WEBHOOK_URL/SECRET referenced only in the server-side webhook module imported solely by the API route; (e) searchParams rendering SAFE — contact/page.tsx:51-60 whitelists service, strips Cc/Cf, clamps idea to 300 chars, and rendering goes through next-intl ICU interpolation + React text nodes (auto-escaped); no XSS sink on any user-controlled path.
   220→  1263→
   221→  1264→Stage Summary:
   222→  1265→FINDINGS:
   223→  1266→
   224→  1267→[P1] .git/config (environment; repo remote) — Live GitHub access token embedded in the origin remote URL
   225→  1268→  Evidence: `git remote -v` → `https://x-access-token:<redacted>@github.com/ashamsaldin303-ctrl/nextjs-fullstack-project.git` (credential detected in the stored remote URL; value never printed).
   226→  1269→  Impact: Any process, backup, CI log, or pasted `git remote -v` output that reads .git/config yields a usable GitHub credential for that repo (push/fetch scope unverified). It is a plaintext secret at rest on disk.
   227→  1270→  Recommended fix: `git remote set-url origin https://github.com/ashamsaldin303-ctrl/nextjs-fullstack-project.git` and authenticate via credential helper or SSH deploy key instead; rotate the token if it was ever copied anywhere.
   228→  1271→
   229→  1272→[P2] prisma/schema.prisma:15-17 + scripts/clean-leads.ts:20 + README Deployment — Lead PII (ipAddress, userAgent, email, whatsapp, message) retained indefinitely; promised retention policy does not exist
   230→  1273→  Evidence: schema comment says "Define a retention policy before real production use (e.g., purge Lead rows older than 90 days) — see README 'Deployment'"; README has NO retention/purge text (rg 'retention|purge|90 days' → 0 hits) and clean-leads.ts only does `deleteMany({})` as a pre-launch full wipe; nothing is scheduled.
   231→  1274→  Impact: Privacy/compliance exposure (IP + UA + full contact text are personal data under GDPR-like regimes); SQLite grows forever; a leaked DB file exposes the complete contact history.
   232→  1275→  Recommended fix: Add an age-based purge job (cron/GitHub Action running `prisma.lead.deleteMany({ where: { createdAt: { lt: <cutoff> } } })`, or null out ipAddress/userAgent after 30–90 days keeping the business fields), and document the policy in README Deployment where the schema comment points.
   233→  1276→
   234→  1277→[P2] Root reverse-proxy file :6-18 — Shipped proxy config contains an intentional open-proxy/SSRF primitive (?XTransformPort)
   235→  1278→  Evidence: `reverse_proxy localhost:{query.XTransformPort}` proxies any request carrying `?XTransformPort=<port>` to an arbitrary localhost port; header block explicitly warns "SANDBOX-ONLY … NEVER deploy this publicly".
   236→  1279→  Impact: An operator copy-pasting the repo's only provided proxy config into production exposes an SSRF relay into every localhost service on the host (databases, n8n, admin panels). The app's own TRUST_PROXY/XFF-overwrite guidance points users at this exact file.
   237→  1280→  Recommended fix: Move the sandbox file to `sandbox/` (or gate the transform behind a build flag) and add a production-safe `deploy/` variant (TLS block, no XTransformPort, XFF overwrite only); mention the split in README Deployment.
   238→  1281→
   239→  1282→[P2] route.ts:149-155 + rate-limit.ts:23-25 + Dockerfile:52-61 + README docker-run — Documented production deployment runs with TRUST_PROXY unset → single GLOBAL rate-limit bucket → trivial total lockout of the only conversion endpoint
   240→  1283→  Evidence: `if (process.env.TRUST_PROXY !== 'true') return 'anonymous'` — with TRUST_PROXY unset/false every caller shares one bucket (30 lenient / 5 strict per MINUTE, globally). The Dockerfile ENV block does not set TRUST_PROXY and the README `docker run` example passes only DATABASE_URL/N8N_* vars; TRUST_PROXY appears in no deployment doc except a comment in the root proxy file (rg confirms: that comment + .env.example:31=false).
   241→  1284→  Impact: ~30 junk POSTs/min from one curl loop keeps the shared lenient bucket permanently exhausted → every legitimate visitor gets 429 on BOTH forms site-wide, continuously. Availability of the lead pipeline hinges on an env var the canonical deploy recipe never sets.
   242→  1285→  Recommended fix: Set `TRUST_PROXY=true` in the Dockerfile/README docker-run example (deployment is by definition behind the overwriting proxy), or reconsider the fail-closed default for the standalone Docker target; at minimum add TRUST_PROXY to the README run command with one explanatory line.
   243→  1286→
   244→  1287→[P2] next.config.ts:26-38 (and 41-55) — Production CSP keeps 'unsafe-inline' in script-src; no COOP/CORP headers
   245→  1288→  Evidence: `"script-src 'self' 'unsafe-inline'"` with the documented rationale (static prerender embeds inline bootstrap; nonce CSP incompatible). securityHeaders list has no Cross-Origin-Opener-Policy / Cross-Origin-Resource-Policy. (HSTS is emitted while the sandbox serves plain HTTP — harmless, browsers ignore it there; correct once TLS terminates at the proxy.)
   246→  1289→  Impact: Any future HTML-injection bug escalates to full XSS with no CSP backstop (unsafe-inline voids script-src protection); missing COOP leaves the site without window.opener isolation. Current exploitability is LOW (React auto-escaping everywhere, no user-content HTML sink), so this is a hardening gap, not an active hole.
   247→  1290→  Recommended fix: Add `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Resource-Policy: same-origin` now; evaluate hash-based CSP for the stable inline bootstrap chunks, or make the shell dynamic to enable nonce CSP. Keep the documented tradeoff note updated with whatever is chosen.
   248→  1291→
   249→  1292→[P3] route.ts:287-305 — Body-size gate depends on content-length/transfer-encoding presence; length-less h2 forwarding would bypass it
   250→  1293→  Evidence: 413 fires only when `content-length > 64 KB` or `transfer-encoding` is present; a request with NEITHER header (possible over HTTP/2 DATA frames) skips both gates and `req.json()` buffers the full body before Zod runs.
   251→  1294→  Impact: Unreachable in the shipped stack (Node standalone = HTTP/1.1; the fronting proxy speaks h1 to upstream with CL/chunked), but a future h2c/h2-direct exposure turns this into a memory-DoS primitive.
   252→  1295→  Recommended fix: Document "never expose the app over h2 directly / proxy must forward length framing", or wrap the parse with a size-checked stream read (e.g. read the body via a capped reader before JSON.parse).
   253→  1296→
   254→  1297→[P3] git history (commits 6d367b6 → b659e9b) — .env was committed then deleted; history still contains it
   255→  1298→  Evidence: `git log --all --name-status -- .env` → `A .env` in 6d367b6, `D .env` in b659e9b. Key names verified WITH VALUES REDACTED: it contained exactly one key, DATABASE_URL (local SQLite path — not a credential).
   256→  1299→  Impact: Minimal today (no secret value), but the pattern is a footgun and the file remains retrievable from history if the repo goes public.
   257→  1300→  Recommended fix: No action needed for a file path; if the repo will be public, rewrite history (git filter-repo/BFG) for hygiene and keep `.env*` ignored (already .gitignore:35).
   258→  1301→
   259→  1302→[P3] n8n-webhook.ts:94-99 / :179 — deliver() takes a `secret` parameter it never uses
   260→  1303→  Evidence: `async function deliver(url, secret, body, headers)` — the body of deliver() never reads `secret`; called as `deliver(config.url, config.secret, body, headers)`.
   261→  1304→  Impact: No vulnerability (server-side only), but it needlessly propagates the HMAC secret through a call stack that doesn't need it — the kind of parameter that later gets logged by mistake.
   262→  1305→  Recommended fix: Drop the `secret` parameter from deliver() and its call site.
   263→  1306→
   264→  1307→[P3] rate-limit.ts:36-54 — Transient Map growth possible between throttled sweeps
   265→  1308→  Evidence: `sweep()` executes at most once per 60 s (`if (now - lastSweep < SWEEP_INTERVAL_MS) return`); within that interval every NEW distinct key inserts without any size check; the 5 000-key cap only applies when the sweep body actually runs.
   266→  1309→  Impact: Requires TRUST_PROXY=true AND direct reachability past the overwriting proxy (i.e., misdeployment) — an attacker minting fresh XFF keys can grow the Map unbounded for up to 60 s before the cap fires. Steady state is capped; realistic risk is low.
   267→  1310→  Recommended fix: Cheap hardening — when inserting a key that didn't previously exist, also check `hits.size > 10_000` and run the eviction inline (or skip inserting the new key entirely).
   268→  1311→
   269→  1312→[P3] robots.ts:9 — Disallow list advertises a nonexistent /admin path
   270→  1313→  Evidence: `disallow: ['/api', '/admin']` — no admin route exists anywhere in the app (single API route, no GET, no admin pages).
   271→  1314→  Impact: Cosmetic recon hint that an admin surface might exist; /api disallow is likewise cosmetic (endpoint is POST-only). No real leak — sitemap contains only the 12 public locale×path URLs.
   272→  1315→  Recommended fix: Optional — drop `/admin` until an admin surface actually exists (counter-argument: keeping it pre-blocks a future panel from indexing; either choice is defensible, just be aware it advertises the path name).
   273→  1316→
   274→  1317→[P3] src/components/seo/home-json-ld.tsx:14-55 — JSON-LD embedded via dangerouslySetInnerHTML without `</script>` escaping
   275→  1318→  Evidence: `dangerouslySetInnerHTML={{ __html: JSON.stringify({...}) }}` — JSON.stringify does NOT escape `</script>`, so any value containing it would break out of the script element. All current inputs are build-time constants (SITE_URL env, SITE_CONTACT/SITE_SOCIAL literals).
   276→  1319→  Impact: Safe as shipped (no user-controlled data reaches it), but the pattern breaks silently the day a dynamic value is added.
   277→  1320→  Recommended fix: Wrap with `.replace(/</g, '\\u003c')` on the stringified output (standard JSON-LD hardening).
   278→  1321→
   279→  1322→[P3] Repo tree — Internal agent guides/prompts and a wildcard-CORS example are tracked in git
   280→  1323→  Evidence: tracked: `upload/elyra-fullstack-prompt-01..05.md`, `upload/FULLSTACK_AGENT_GUIDE (2).md`, `upload/elyra-phase1-audit.md`, `download/README.md`, `examples/websocket/{server,frontend}.ts` (server.ts:8-11 uses `cors: { origin: "*" }`, no auth — inert example, not imported by the app, absent from the Docker runtime image). Also `tool-results/` is currently untracked but NOT ignored (same artifact class was purged before in commit 887c4dc).
   281→  1324→  Impact: Combined with the P1 remote credential: if this repo is or ever becomes public, internal architecture/audit documentation is exposed; the example could be copy-pasted as-is into a real service.
   282→  1325→  Recommended fix: Verify repo visibility is private; move internal docs out of the tracked tree (or a private docs repo); add `tool-results/` to .gitignore; label examples/ clearly as non-production.
   283→  1326→
   284→  1327→Verified-safe (no action): CSRF layering (Sec-Fetch-Site cross-site/same-site 403 + Origin/Host match + 415 content-type gate blocks form-post spam before the origin check); honeypot returns an indistinguishable fake 201 before the strict bucket (bots can't fingerprint or burn quota); server-side estimate recompute with client-echo field stripping; webhook URL is env-only (no SSRF via user input), HTTPS-enforced in production, secret-length-gated, HMAC-signed with fresh timestamp/nonce per attempt, 5 s AbortController timeout, one network-only retry, failures server-log-only and never alter the 201; Prisma usage is a single typed create (no raw SQL, no read endpoint); production Prisma logging is error-only (no PII query logs); rate limiter is synchronous on a single-threaded runtime (no sliding-window race), blocked requests don't extend the window, Retry-After computed from the oldest hit; reference IDs use crypto.randomInt (collision-proof, no row-id leak); client forms surface only translated catalog strings; API error responses never echo values, stacks, or internal paths (unknown-field NAMES are echoed as JSON keys — inert); zod 4.3.5 schemas are strict, bounded, and ReDoS-free (all regexes linear/anchored); OG/apple icons render static content on cached metadata routes (no per-request render cost); Dockerfile runs non-root with no .env in the runtime image and secrets via -e.
   285→  1328→
   286→  1329→VERDICT: 0 P0, 1 P1, 4 P2, 7 P3 — The leads pipeline itself is genuinely well-engineered (strict bounded validation, layered CSRF, honeypot, signed webhook, clean error hygiene); the real risks are operational: a plaintext GitHub token in the git remote, an undocumented PII retention gap, a sandbox proxy config carrying an open-proxy primitive, and a default deployment posture (TRUST_PROXY unset) that lets one trivial script globally 429 the only conversion endpoint.
   287→  1330→
   288→  1331→---
   289→  1332→Task ID: L1-B
   290→  1333→Agent: Backend/Data/i18n Audit Specialist (Group 1 — Analysis)
   291→  1334→Task: LOOP 1 deep audit of Prisma/DB, business logic, i18n parity & catalogs, configs
   292→  1335→
   293→  1336→Work Log:
   294→  1337→- Read worklog tail (last ~300 lines) for conventions: blue-palette revert is CURRENT law, i18n single-writer discipline, no concurrent tsc with dev server, parity script is the i18n gate.
   295→  1338→- Read IN FULL: prisma/schema.prisma, src/lib/{db,calculator,seo,site-config,sound,utils,hero-scroll,api-i18n,rate-limit,lead-fields,n8n-webhook}.ts, src/i18n/{routing,request,navigation}.ts, src/proxy.ts (Next 16 middleware), src/app/api/leads/route.ts, src/components/seo/home-json-ld.tsx, src/app/{sitemap,robots}.ts, src/app/[locale]/{layout,page,opengraph-image,not-found}.tsx, src/app/not-found.tsx, src/app/[locale]/[...rest]/page.tsx, scripts/{check-i18n-parity.js,clean-leads}.ts, messages/ar.json + messages/en.json (both complete, 1291 lines each), tsconfig.json, eslint.config.mjs, package.json, next.config.ts, postcss.config.mjs, components.json, .env.example. Also read contact/page.tsx prefill contract + calculator.tsx consumption + scoped sections of navbar/footer/custom-cursor/automation-simulator/hero-console.
   296→  1339→- Ran `node scripts/check-i18n-parity.js` (verified read-only first): → "Parity OK: 643 keys matched across ar.json and en.json / ICU placeholders verified on 582 string keys · array lengths verified on 38 array keys", exit 0.
   297→  1340→- Ran `bunx eslint` scoped on all 19 audited lib/api/seo/i18n files → 0 errors, 0 warnings.
   298→  1341→- Business-logic verification: exhaustively re-implemented computeEstimate in node and swept the ENTIRE input space (3 services × 20 pages × 2 languages × 2 threeD × 64 integration subsets × 2 automation levels = 30,720 combos): 0 invariant violations (min≤max, weeksMin≤weeksMax, weeksMax≥weeksMin+2 everywhere); boundary checks pages NaN/−5/999 clamp correctly; max estimate 14,600–23,400 USD / 11–20 weeks (sane).
   299→  1342→- ICU validation with the project's own intl-messageformat: formatted all message templates in both locales — Arabic pagesValue plural renders correct zero/one/two/few/many/other forms (0→"لا صفحات", 1→"صفحة واحدة", 2→"صفحتان", 3→"3 صفحات", 11→"11 صفحة", 100→"100 صفحات"); step/weeks/successReference/rights/completed/prefill.serviceIdea all format correctly.
   300→  1343→- Zod 4.3.5 runtime probe: confirmed `unrecognized_keys` issues carry `keys: string[]` + empty path (fieldErrors handles them) and discriminated-union missing-discriminator issues carry path ['source'] → maps to fields.source (matches audit-1b live observation).
   301→  1344→- Catalog deep scans (read-only node scripts): 42 identical ar/en values — ALL legitimate (numbers, contact values, brand names, CRM acronym, 2 empty numeric suffixes, intentional cursor.magnet ""); zero Arabic text inside en.json except by-design nav.switchLocale="عربي"; zero untranslated English sentences in ar.json; zero unbalanced ICU braces; max EN/AR length ratio 2.36 (below the 3× layout-risk threshold).
   302→  1345→- Key-usage cross-reference (namespace-aware t() literal scan across src/): all consumed keys resolve in both catalogs; found 8 never-referenced catalog keys (P3 below). Verified hero/manifesto/nav/footer/contact-form/services/work/about/common-cursor/metadata namespaces line-by-line against their consuming components.
   303→  1346→- Verified reference generator stats: 36^9 space, 200k draws → 0 collisions; format ^c[0-9a-z]{9}$.
   304→  1347→- DB usage map: only db.lead.create (route) + standalone scripts/clean-leads.ts (dry-run supported); no app read paths. prisma/ has NO migrations dir (db push workflow; db:migrate/db:reset scripts exist but would baseline fresh).
   305→  1348→- Verified palette revert completeness via grep: --primary #0071E3 / --g-blue #4285F4 confirmed current; exactly 3 amber remnants found (opengraph-image.tsx:44, not-found.tsx:48,60) + 2 stale "no blue" palette-law comments.
   306→  1349→- Verified proxy.ts matcher semantics (/opengraph-image, /apple-icon, /en/* all resolve; /api excluded from intl middleware); sitemap's 12 URLs (6 paths × 2 locales) all exist as routes; fixed LAST_MODIFIED 2026-08-26 is not future-dated (today 2026-08-27).
   307→  1350→- Verified sound.ts SSR-safety (no module-level window access, lazy AudioContext on gesture, onended teardown, storage-event cross-tab sync), hero-scroll.ts bind/unbind identity guard, api-i18n.ts safe key-fallback.
   308→  1351→- No `any` leaks in src/lib/** or src/app/api/** (grep-verified).
   309→  1352→
   310→  1353→Stage Summary:
   311→  1354→FINDINGS:
   312→  1355→
   313→  1356→[P2] src/app/api/leads/route.ts:430 + prisma/schema.prisma — Customer reference is never persisted
   314→  1357→  Evidence: `const reference = randomReference()` is generated AFTER `db.lead.create(...)`, included only in the 201 response + webhook payload; the Lead model has no `reference` column (only id/name/email/whatsapp/message/service/pages/... — schema.prisma:22-46). The success UI shows "رقمك المرجعي: {reference}" (calculator.form.successReference), and .env.example ships with N8N_WEBHOOK_URL empty (webhook disabled by default).
   315→  1358→  Impact: The reference shown to every customer is untraceable — the team cannot map "c5tr8p13xb" to a stored row (CRM lookup fails; with the webhook disabled the reference exists NOWHERE except the user's toast). Support/repeat-contact workflows break.
   316→  1359→  Recommended fix: Add `reference String @unique` to the Lead model; generate the reference BEFORE `db.lead.create` and include it in `data` (keep the 'c'+9 base36 format); optionally catch P2002 unique violation and regenerate (odds ~1e-14, one retry is plenty). While touching the schema, document the `service:'contact'` sentinel (route.ts:246) in a schema comment.
   317→  1360→
   318→  1361→[P2] src/app/[locale]/opengraph-image.tsx:11-13,44 + src/app/not-found.tsx:48,60 — Blue-palette revert leftovers: amber remnants + comments asserting the abolished "no blue" law
   319→  1362→  Evidence: opengraph-image.tsx:11-12 comment claims "quad-dot brand language (amber/gold/red/green — palette law: no blue)" while :80/:156 draw #4285F4 blue dots and :44 still has the amber-era radial `rgba(245, 158, 11, 0.35)`; root not-found.tsx:48 chip hover border `rgba(245,158,11,.5)` + :60 radial `rgba(245,158,11,0.16)` sit next to the blue CTA #0071E3 (:125) and focus ring #4285F4 (:49). Current global law is blue (--primary #0071E3, --g-blue #4285F4 — globals.css:71,86; user round R1-R6 reverted the amber palette).
   320→  1363→  Impact: The social-shared OG card and the last-resort 404 mix amber glow/hover with blue brand accents (off-brand vs the restored identity); the stale "palette law: no blue" comments actively mislead the next editor into "fixing" the blue dots back to amber.
   321→  1364→  Recommended fix: Decide the accent once: either swap the three rgba(245,158,11,*) to the blue family (e.g. rgba(0,113,227,0.35)/.5/.16) for full consistency, or keep a deliberate warm secondary — then update BOTH comments to state the actual current decision. (1-line CSS + 2 comment edits per file.)
   322→  1365→
   323→  1366→[P3] messages/ar.json + en.json — 8 dead catalog keys (present in both locales, consumed by nothing)
   324→  1367→  Evidence: namespace-aware t()-literal scan + direct grep across src/ shows zero references to: `nav.services` (navbar consumes websites/automation/work/about/contact/home/cta/ariaLabel/openMenu/closeMenu only), `common.back`, `common.next` (calculator uses its own calculator.back/next), `common.close`, `common.getStarted`, `common.explore`, `common.reduceMotionNote`, `hero.canvasFallback`.
   325→  1368→  Impact: Catalog noise; copy edits to never-rendered strings waste review time; inflates the parity count (643 vs 635 live keys).
   326→  1369→  Recommended fix: Delete the 8 keys from BOTH catalogs in one edit (parity stays green), or wire them if a surface was planned (e.g. common.reduceMotionNote for a reduced-motion notice).
   327→  1370→
   328→  1371→[P3] scripts/check-i18n-parity.js:1-111 — Parity gate has three blind spots
   329→  1372→  Evidence: The script verifies presence/type/array-length/placeholder-set only. It cannot distinguish intentional empties from accidents (`common.cursor.magnet` = "" in both files passes silently — intentional per custom-cursor.tsx:42, but nothing encodes that), never flags ar==en values ( untranslated-copy risk — currently all 42 identical values are legitimate numbers/brand strings), and has no usage-vs-catalog check (the 8 dead keys above exist today).
   330→  1373→  Impact: False confidence — an accidentally blanked or untranslated string ships silently.
   331→  1374→  Recommended fix: Extend the script: (a) empty-string check with allowlist ['common.cursor.magnet'], (b) warn-list of identical ar/en string values, (c) optional unused-key scan (same t()-literal heuristic used in this audit).
   332→  1375→
   333→  1376→[P3] src/lib/n8n-webhook.ts:94-99 — `deliver()` declares an unused `secret` parameter
   334→  1377→  Evidence: `async function deliver(url: string, secret: string, body: string, headers: ...)` — `secret` is never read inside the function body (signing happens in attemptDelivery); caller passes config.secret (:179).
   335→  1378→  Impact: Reader confusion — the signature implies the transport layer authenticates with the secret, which it does not. (Passes lint only because of after-used arg semantics.)
   336→  1379→  Recommended fix: Drop the parameter (and the argument at :179) or rename to `_secret`.
   337→  1380→
   338→  1381→[P3] src/lib/seo.ts:50 vs src/app/[locale]/layout.tsx:88 — Inconsistent OG image alt text
   339→  1382→  Evidence: seo.ts declares alt "Elyra — Stunning Websites · n8n Automation · Digital Studio"; layout.tsx:88 and [locale]/page.tsx:41 declare "Elyra — Stunning Websites & n8n Automation Systems" for the SAME image.
   340→  1383→  Impact: Scrapers see two different alt texts for one asset; maintenance drift.
   341→  1384→  Recommended fix: Export a single OG_ALT constant (e.g. from site-config.ts) and use it in all three places.
   342→  1385→
   343→  1386→[P3] src/app/[locale]/layout.tsx:98-100 — SITE_URL re-derived instead of reusing the import
   344→  1387→  Evidence: Line 8 imports SITE_URL from '@/lib/seo' (used for ogImage), but metadataBase is built from a duplicate expression `new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000')`.
   345→  1388→  Impact: Two sources of truth for the same value (drift risk if the fallback ever changes in seo.ts).
   346→  1389→  Recommended fix: `metadataBase: new URL(SITE_URL)`.
   347→  1390→
   348→  1391→[P3] src/app/not-found.tsx:135-138 — Stale comment: "the proxy locale-detects and redirects"
   349→  1392→  Evidence: Comment says plain <a> hrefs work because "the proxy locale-detects and redirects on the subsequent full navigation" — but src/i18n/routing.ts:10 sets `localeDetection: false` (deterministic; /work serves the default Arabic page directly, no detection, no redirect).
   350→  1393→  Impact: Future maintainers may build locale-sensitive behavior on a false invariant.
   351→  1394→  Recommended fix: Reword to "paths serve the default Arabic locale (localeDetection is disabled — see routing.ts)".
   352→  1395→
   353→  1396→[P3] package.json:5-15 — No typecheck script
   354→  1397→  Evidence: scripts = dev/build/start/lint/db:*; the worklog shows every agent manually runs `bunx tsc --noEmit` as the actual type gate.
   355→  1398→  Impact: The project's primary safety gate is tribal knowledge, not a script (CI- unfriendly).
   356→  1399→  Recommended fix: Add `"typecheck": "tsc --noEmit"`.
   357→  1400→
   358→  1401→[P3] components.json:18 — shadcn `hooks` alias points to a nonexistent directory
   359→  1402→  Evidence: `"hooks": "@/hooks"` while src/hooks/ does not exist (LS-verified).
   360→  1403→  Impact: A future `bunx shadcn@latest add` that installs a hook would fail until the dir is created.
   361→  1404→  Recommended fix: Create src/hooks/ (empty) or point the alias at an existing dir.
   362→  1405→
   363→  1406→[P3] src/app/api/leads/route.ts:386-426 — Strict quota is burned before a write that may still fail
   364→  1407→  Evidence: Step 6 checks+records the 5/min strict bucket, THEN `db.lead.create` runs inside try/catch — a storage failure (500) still consumed a strict slot.
   365→  1408→  Impact: A user whose lead failed to persist loses one of only 5 submissions/min through no fault of their own (rare: SQLite write failures are exceptional).
   366→  1409→  Recommended fix: Acceptable as-is (simplicity); if desired, on catch remove the just-recorded strict timestamp or move the burn after a successful create with a pre-check for early 429.
   367→  1410→
   368→  1411→[P3] src/i18n/navigation.ts:4 — `redirect` and `getPathname` exports are unused
   369→  1412→  Evidence: createNavigation destructures 5 exports; grep shows only Link/usePathname/useRouter consumed anywhere in src/.
   370→  1413→  Impact: None (standard boilerplate surface); noted for the dead-code ledger.
   371→  1414→  Recommended fix: Keep (next-intl convention) or trim to the three used exports.
   372→  1415→
   373→  1416→VERIFIED-CLEAN (for the record — checked and NOT broken): db.ts globalThis singleton (hot-reload-safe, correct Prisma pattern; dev 'query' logging acknowledged as dev-only diagnostic with PII note); route error handling (500 generic + translated message, details only to server log, webhook strictly after commit and failure-isolated); rate-limit sliding window + sweep + 5k cap; honeypot fake-201 indistinguishability; CSV-injection neutralizer + per-attempt HMAC re-signing; chunked-transfer 413 gate; calculator rounding invariant (breakdown lines sum to headline, verified exhaustively); formatMoney ar/en outputs ("‏5,200 US$" / "$5,200" — Latin digits by design); sitemap ↔ routes ↔ hreflang ↔ canonical consistency (12 URLs, as-needed prefixes, x-default→ar); JSON-LD validity (Organization+WebSite @graph, no phantom SearchAction); api-i18n safe fallback + compile-time shape check on ar.apiErrors; sound.ts/hero-scroll.ts/utils.ts SSR-safety & teardown; tsconfig strict+noUncheckedIndexedAccess+noImplicitOverride; dependency alignment (next 16.1.1 / react 19 / next-intl 4.3.4 / eslint-config-next 16.1.1 / zod 4.3.5 / react-compiler plugin wired); next.config standalone+reactCompiler+CSP split dev/prod; proxy matcher coverage for icon/OG/apple-icon routes; i18n catalogs: full key-tree parity, valid ICU in both locales, zero language leakage, zero accidental empties, no 3× truncation-risk strings.
   374→  1417→
   375→  1418→VERDICT: 0 P0, 0 P1, 2 P2, 10 P3 — Backend, data and i18n core is production-grade; the only real gaps are the unpersisted customer reference (data-model completeness) and 3 amber palette remnants + stale comments from the blue revert, everything else is polish.
   376→  1419→
   377→  1420→---
   378→  1421→Task ID: L1-D
   379→  1422→Agent: WebGL/Sensory/Layout/Hooks Audit Specialist (Group 1 — Analysis)
   380→  1423→Task: LOOP 1 deep audit of WebGL scenes, sensory layer, layout/shared components, hooks, globals.css
   381→  1424→
   382→  1425→Work Log:
   383→  1426→- Read worklog tail (R7 lessons: GLSL1 attribute/varying convention, eslint-disable react-hooks/immutability on uniform mutation, no concurrent tsc with dev server, single-browser-session, .elyra-spotlight content-lift / data-bg-layer exemption, z-ladder 80/90/100/200).
   384→  1427→- Read EVERY scope file in full: hero-canvas.tsx (599 ln, TS + all 5 GLSL blocks), capability-scene.tsx (604), console-scene.tsx (434), three-d-section.tsx, custom-cursor.tsx, grain-overlay.tsx, sound-toggle.tsx, navbar.tsx, footer.tsx, language-switcher.tsx, live-clock.tsx, scroll-progress.tsx, logo.tsx, cta.tsx, page-hero.tsx, reveal.tsx, section-heading.tsx, service-prose.tsx, home-json-ld.tsx, all 7 use-*/lib hooks (use-reduced-motion, use-cursor-velocity, use-magnetic, use-near-viewport, use-rtl, hero-scroll, sound), all 7 shadcn ui files, globals.css (all 1213 lines). Plus consumers: hero.tsx, hero-console.tsx (full), [locale]/layout.tsx, methodology.tsx (bindHeroScroll wiring), testimonials.tsx (spotlight interplay), site-config.ts.
   385→  1428→- Ran structural GLSL check via node (/tmp): delimiter balance + GLSL1-only (no #version/in/out/texture()) PASS on all 4 hero-canvas shaders; JS uniform objects match GLSL declarations EXACTLY (Particles: uTime float/uMouse vec3(Vector3)/uPixelRatio float/uScroll float; Silk: uTime/uMouse vec2(Vector2)/uColorA-C vec3(Color)/uScroll); fbm call sites = 3 (budget respected; 5 octaves).
   386→  1429→- Verified R3F v9.7 internals in node_modules: commitUpdate reconstructs primitives when the `object` prop changes (console-scene tube swap on preset change is SAFE without key changes); removeChild never disposes primitives (manual tube disposal is required + correct); events hijack BOTH e.target and e.currentTarget with {hasPointerCapture,setPointerCapture,releasePointerCapture} — decisive for P1-1 below; captured pointers keep receiving move events off-object (capability drag works); Canvas has an internal ErrorBoundary for children but WebGL-creation failure rejects an un-awaited async configure() (silent blank, not a route crash).
   387→  1430→- Scoped `bunx eslint` on all 33 scope files → 0 errors, 0 warnings (two batches). i18n parity script → 643/643 keys; common.cursor.* (magnet/rotate/preview/drag/zoom/inspect/external) present in ar+en with magnet="" by design.
   388→  1431→- Grep verification: dead CSS classes (elyra-grain, elyra-cursor base/chip/--visible, glass-light, blend-difference, prose-ar-max) have ZERO TSX references; amber rgba(245,158,11)/rgba(217,119,6) remnants found in 6 files; sonner 2.0.7 toaster z-index 999999999 (above cursor 200 — coherent ladder: sheet/nav 50 < progress 60 < intro 80 < grain 90 < skip 100 < cursor 200 < toasts); body > header print selector valid (providers render no DOM); footer.localTime = "بتوقيتك المحلي" (visitor-tz semantics correct).
   389→  1432→
   390→  1433→Stage Summary:
   391→  1434→FINDINGS:
   392→  1435→
   393→  1436→[P1] capability-scene.tsx:462 — Mouse parallax can never activate (R3F hijacks e.currentTarget)
   394→  1437→  Evidence: `const rect = (e.currentTarget as HTMLElement).getBoundingClientRect?.()` … `if (rect) { mouse.current.tx = … }` — in @react-three/fiber v9 the raycast event overrides BOTH target and currentTarget with `{ hasPointerCapture, setPointerCapture, releasePointerCapture }` (verified in node_modules/@react-three/fiber/dist/events-156d8d12.esm.js:765-773); that object has NO getBoundingClientRect, so `rect` is always undefined and the only writes to mouse.current.tx/ty (lines 464-465) never execute. The useFrame parallax (lines 416-421) then lerps toward (0,0) forever.
   395→  1438→  Impact: The documented "mouse parallax lerp (now on a parent group so blob+satellites+halo drift together)" is a dead feature — the scene never responds to hover position (only auto-spin + drag still work).
   396→  1439→  Recommended fix: Drop manual rect math entirely — read the R3F-maintained NDC pointer inside useFrame: `const { gl, pointer } = state; parallax.current.position.x += (pointer.x * viewport.width / 4 - parallax.current.position.x) * 0.04` (state.pointer is updated by R3F's event pass; no listener needed). Delete lines 462-466 and the mouse ref.
   397→  1440→
   398→  1441→[P2] page-hero.tsx:37 + hero-console.tsx:307/315/316 (+ cross-scope: opengraph-image.tsx:44, not-found.tsx:48/60, automation-simulator.tsx:441/511/544/553/743, bento.tsx:418/447/600) — Amber palette remnants survive the blue revert
   399→  1442→  Evidence: page-hero: `radial-gradient(60% 60% at 50% 0%, rgba(245,158,11,0.18), transparent 70%)` (amber behind EVERY inner-page hero); hero-console SVG fallback (mobile + reduced-motion!): `stroke="rgba(217,119,6,0.3)"`, `fill="rgba(217,119,6,0.15)"`; opengraph-image: `rgba(245, 158, 11, 0.35)` on the SOCIAL SHARE image; simulator/bento: five rgba(217,119,6,…) glow shadows. R1-R6 claimed "zero amber hex left" — the rgba() forms were missed. (L1-C independently found 3 of these; my sweep adds hero-console + simulator + bento = 16 instances in 6 files.)
   400→  1443→  Impact: Off-palette amber glows/nodes against the restored blue brand — visible on all inner-page heroes, the mobile hero-console diagram, the simulator/bento glows, the root 404, and externally on og:image social cards.
   401→  1444→  Recommended fix: Swap rgba(245,158,11,x)→rgba(66,133,244,x) (#4285F4) and rgba(217,119,6,x)→rgba(0,113,227,x) (#0071E3) in all 16 spots (keep the green #34A853 counterpoints, e.g. hero-console center node).
   402→  1445→
   403→  1446→[P2] globals.css:750-827 (+170-175, 204-207, 653-656, 835-841) — ~95 lines of dead CSS from the unmounted Phase-2 sensory layer + unused utilities
   404→  1447→  Evidence: Grep across src/ returns ZERO TSX references for `.elyra-grain` (750), `.elyra-cursor` base (764), `.elyra-cursor-chip` (792), `.elyra-cursor--visible` (813), `.glass-light` (170), `.blend-difference` (204), `.prose-ar-max` (654); the print block still lists `.elyra-grain/.elyra-cursor/.elyra-cursor-chip` (836-838); the `cursor:none` rule at 820-827 duplicates R7-b's 1182-1187. The Phase-2 `.elyra-cursor-dot/-ring` rules (775-789) survive only because R7-b re-declares the same class names with explicit resets (1087-1100) — fragile shared-name coupling.
   405→  1448→  Impact: ~8% of the stylesheet is inert; every future cursor tweak must keep neutralizing rules nobody remembers. R7 deleted film-grain.tsx/magnetic-cursor.tsx but left their styles.
   406→  1449→  Recommended fix: Delete lines 750-827 entirely (keep 820-827's reduced-motion nesting only if you want belt-and-braces, else rely on the R7-b !important rule), the three dead print selectors, and `.glass-light`/`.blend-difference`/`.prose-ar-max` blocks.
   407→  1450→
   408→  1451→[P2] console-scene.tsx:16-17 + hero-console.tsx:265-266 — "Drag to rotate" affordance is false
   409→  1452→  Evidence: Header comment: "Drag to rotate (clamped). No wheel zoom / no scroll hijack (§4)." — but console-scene.tsx contains NO pointer handlers anywhere (verified: git log -S "onPointerDown" on the file returns nothing — drag never existed); the only motion is the useFrame rotation drift (lines 274-279). Meanwhile hero-console renders `data-cursor="rotate"` + label chip and the Canvas has `cursor: 'grab'` (line 423).
   410→  1453→  Impact: Users are told "اسحب للتدوير 360°" and see a grab cursor, attempt to drag, and nothing responds — a broken promise on the hero's conversion payoff scene.
   411→  1454→  Recommended fix: Either add the interaction (drag delta on the group with clamped rotation, mirroring capability-scene's throttled pattern) or fix the affordance: drop data-cursor="rotate" + change the chip label + remove `cursor: 'grab'`, and reword the header comment.
   412→  1455→
   413→  1456→[P2] hero-canvas.tsx:39 + 583-598 — No mobile tier: 4500 curl-noise particles + 15-vnoise silk fragment ship to phones
   414→  1457→  Evidence: `const PARTICLE_COUNT = 4500` (fixed); SILK_FRAGMENT runs 3× fbm×5 octaves = 15 value-noise samples (~60 sin) per pixel; hero.tsx gates only on `!reduced && load3D` — no viewport/mobile matchMedia (unlike hero-console, which replaces WebGL with SVG below 768px); the only mobile concession is `dpr={[1,2]}`.
   415→  1458→  Impact: Low-end phones render ~1.3M px × heavy fragment math + 4500-particle vertex curl (6 snoise/vertex) at up to 60fps — jank/battery risk; prior rounds never reduced the count.
   416→  1459→  Recommended fix: Add a coarse-pointer or <768px tier: PARTICLE_COUNT 4500→1800 and dpr [1,1.5] (or a uniform-driven fbm octave drop to 4 is harder — count+dpr is enough); alternatively reuse hero-console's `matchMedia('(max-width: 767px)')` to skip the silk plane on phones (particles alone read fine over the CSS fallback).
   417→  1460→
   418→  1461→[P3] live-clock.tsx:21-27 + 58-63 — English users see the timezone twice
   419→  1462→  Evidence: `new Intl.DateTimeFormat(…, { …, timeZoneName: 'short' })` already yields "15:16 GMT+3" (verified via node), then the component appends the manually built `{tzLabel}` → "15:16 GMT+3 (GMT+3)". Arabic shows "١٥:١٦ غ (GMT+3)" (no visual dup, cryptic abbreviation).
   420→  1463→  Impact: Duplicated tz string in EN footer; mildly confusing.
   421→  1464→  Recommended fix: Remove `timeZoneName: 'short'` from the formatter (keep the explicit GMT±N label — it's locale-stable), or drop tzLabel and keep the Intl one.
   422→  1465→
   423→  1466→[P3] logo.tsx:11-13 — Stale palette comment contradicts the code
   424→  1467→  Evidence: "a 4-color brand dot in the amber/green family (palette law: no blue)" — the circles below are #4285F4/#EA4335/#FBBC05/#34A853 (the Google quad, correct after the blue revert).
   425→  1468→  Impact: Misleads the next palette sweep.
   426→  1469→  Recommended fix: Reword to "Google-brand quad-dot (#4285F4/#EA4335/#FBBC05/#34A853) — intentionally outside the token system".
   427→  1470→
   428→  1471→[P3] console-scene.tsx:404-434 + hero-console.tsx:197 — WebGL guard checks API presence, not context creation
   429→  1472→  Evidence: `!('WebGLRenderingContext' in window)` gates the SVG fallback; hero-canvas and capability-scene both do full `createElement('canvas').getContext('webgl2' ?? 'webgl')` creation probes. If the API exists but creation fails (driver blocklist), R3F v9's configure() rejects an un-awaited async promise (verified in fiber dist) → silent 280px empty box, no SVG fallback, no crash.
   430→  1473→  Impact: Rare edge case (blocked GPUs / hardened browsers) renders an empty hero-console scene.
   431→  1474→  Recommended fix: Reuse the same rAF-deferred context-creation probe in ConsoleScene (return null → let the parent fall through to the SVG diagram), or lift the probe result into hero-console's showSvg condition.
   432→  1475→
   433→  1476→[P3] sheet.tsx:73-79 + navbar.tsx:124-196 — Mobile sheet has no Description/aria-describedby
   434→  1477→  Evidence: SheetDescription is exported but never used; the navbar sheet passes none, and the stock built-in close was removed (documented LOW-10). Radix Dialog logs a dev warning for dialogs without Description or `aria-describedby={undefined}`.
   435→  1478→  Impact: Dev-console noise; minor a11y (dialog purpose not described to SR).
   436→  1479→  Recommended fix: Pass `aria-describedby={undefined}` on SheetContent in navbar (one-line), or add a visually-hidden SheetDescription with the nav summary.
   437→  1480→
   438→  1481→[P3] footer.tsx:154/166/178/190/202 — Social icon buttons are 36px touch targets
   439→  1482→  Evidence: `size-9` (36px) on all 5 social anchors; the Batch-1 item-4 44px pass covered the navbar only (size-11), footer was out of its scope.
   440→  1483→  Impact: Below WCAG 2.5.8 (24px minimum, AA) — passes the minimum, but below the 44px target the project standardized on for navbar/CTAs (G1-c finding class).
   441→  1484→  Recommended fix: size-9→size-11 with compensating gap-3→gap-2.5, or wrap in a hit-area expander.
   442→  1485→
   443→  1486→[P3] hero-canvas.tsx:492 — Silk plane edges visible on super-ultrawide viewports
   444→  1487→  Evidence: `planeGeometry args={[64, 32]}` (±32 wide) at z=-9 with fov 70/z=6 covers visible width 21×aspect → fully covered up to aspect ≈ 3.05; 32:9 monitors (3.56) / 5120×1440 show uncovered side bands (the additive layer just ends).
   445→  1488→  Impact: Cosmetic seam on extreme aspect ratios only.
   446→  1489→  Recommended fix: Widen to args={[80, 32]} (cost-free — one fullscreen quad regardless) or clamp via the existing vignette.
   447→  1490→
   448→  1491→[P3] three-d-section.tsx:70-72 — aria-label on a role-less div + non-keyboard drag
   449→  1492→  Evidence: `aria-label={t('hint')}` sits on a plain div (ignored by most AT without a role); the capability scene's drag interaction has no keyboard equivalent and the visible hint text duplicates the label.
   450→  1493→  Impact: Minor a11y semantics gap on the 3D showcase.
   451→  1494→  Recommended fix: `role="img"` + keep the visible hint as the accessible description (drop aria-label), or add tabIndex={0} + arrow-key rotation (10 lines, mirrors simulator stage pattern).
   452→  1495→
   453→  1496→VERIFIED-CLEAN (for the record — checked and NOT broken): hero-canvas GLSL (GLSL1-only, uniform sets match exactly, 3×fbm budget, CustomBlending ONE,ONE/ZERO,ONE preserves the CSS stage, aspect-correct mouse projection, scroll-dolly via state.camera, context-loss guard, full disposal via JSX, rAF-deferred WebGL probe); hero visibility pause lives correctly in hero.tsx (IO + visibilitychange threaded into active); hero-console's 800ms payoff timer is unmount-safe and double-submit-guarded; console-scene primitive tube swap is SAFE in R3F v9 (commitUpdate reconstructs on object change — index keys fine) and its manual disposal is required + correct (primitives are never auto-disposed), StrictMode double-dispose semantics documented; capability-scene drag works off-object (R3F captured-pointer dispatch verified) and pointer capture uses R3F's hijacked target.setPointerCapture correctly; custom-cursor (single rAF loop with proper teardown, delegation + relatedTarget check, all 7 label keys present in both catalogs, is-hidden initial, touch kill-switch, elyra-cursor-active class cleanup, z-200 under toasts); grain-overlay truly server-rendered, %23-encoded data-URI valid, reduced-motion collapses to the base 0 0 keyframe; sound engine (gesture-gated AudioContext, storage-backed uSES with stable primitive snapshots, node teardown via onended, pointer-only delegation with cleanup); all 7 hooks (uSES patterns canonical — module MQL singletons, primitive snapshots, cleanup in every branch; use-magnetic's transition suppression/settle/restore logic correct incl. capture-flag-matched scroll listener; Parallax IO arm/disarm; useNearViewport one-shot disconnect); language-switcher scroll restore (StrictMode-safe consume-inside-timeout + 5s expiry); navbar sheet a11y (aria-expanded, Radix focus/Escape, SheetClose-wrapped links); footer (wa.me number format correct, rel noopener everywhere, mt-auto sticky-footer with body flex-col); home-json-ld (@graph Organization+WebSite valid, home-only); all 7 shadcn files stock + the one documented sheet modification; globals.css z-ladder coherent incl. sonner 999999999, spotlight data-bg-layer exemptions present on all 4 hero decorative absolutes, @theme/inline TW4 syntax correct, universal :lang(ar) letter-spacing reset sound, print `body > header` matches (providers render no DOM).
   454→  1497→
   455→  1498→VERDICT: 0 P0, 1 P1, 4 P2, 7 P3 — The WebGL/sensory/hooks architecture is genuinely production-grade (every disposal/rAF/uSES discipline verified clean); the real work is one dead parallax feature (R3F event-hijack gotcha), finishing the blue-revert sweep (16 amber rgba remnants in 6 files), deleting ~95 lines of Phase-2 dead CSS, and the false drag affordance on the console scene.
   456→  1499→
   457→  1500→---
   458→  1501→Task ID: L1-C
   459→  1502→Agent: App Pages & Home Components Audit Specialist (Group 1 — Analysis)
   460→  1503→Task: LOOP 1 deep audit of app-router pages, home sections, page components
   461→  1504→
   462→  1505→Work Log:
   463→  1506→- Read worklog tail (R7 + R1-R6 blue-revert + 3-convergence + I1-I4 entries) for conventions: data-bg-layer exemption from .elyra-spotlight's content-lift rule, flex-col parents for mask-reveal wrappers, Math.random only in lazy useState, blue palette is the CURRENT mandate (#0071E3/#4285F4 family), 44px touch targets scoped to navbar by decision.
   464→  1507→- Read ALL scoped files completely: pages layer ([locale] layout/page/error/not-found, global-error, root not-found, [...rest], about/contact/services-websites/services-automation/work pages + all 5 loading.tsx), all 19 home components (hero, hero-console, intro-overlay, kinetic-heading, manifesto, trust-bar, bento, featured-work, testimonials, before-after, deconstructed-card, calculator(+lazy), automation-simulator(+lazy), console-scene, ring-gauge, methodology(+lazy), three-d-section), pages components (contact-form, work-grid).
   465→  1508→- Cross-verified supporting infra: globals.css (universal :lang(ar) letter-spacing reset :649, reduced-motion kill-switch :246, spotlight lifting rule :722, hero-marquee/hr-word/intro/manifesto CSS), messages/ar.json+en.json (stats/about numbers values are real Numbers — no NaN risk; hero.console/workSection.scenes/simulator/bento keys all present; parity script → "Parity OK: 643 keys"), shared components (section-heading, reveal/parallax, page-hero), lib hooks (use-rtl, use-near-viewport, use-reduced-motion, use-cursor-velocity, use-magnetic, sound, calculator, seo, site-config), sitemap.ts, proxy.ts, eslint.config.mjs.
   466→  1509→- Verified with Grep across src/: amber-remnant sweep (217,119,6/#b45309/245,158,11 found in 5 files + page-hero), Math.random/Date.now/new Date confined to effects/callbacks/lazy-initializers in scoped files, physical-direction utilities (only deliberate ltr:right/rtl:left hero rail + documented honeypot -left-[9999px]), --color-elyra-deep token existence (absent — only hand-written .bg-elyra-deep class), ContextLossGuard presence per WebGL scene (hero-canvas ✓, capability-scene ✓, console-scene ✗), testimonial Parallax is NOT a direct spotlight child (no R7 lifting-rule bug).
   467→  1510→- Ran scoped `bunx eslint` on all 33 scoped files in 4 batches → 0 errors, 0 warnings. Ran read-only parity script (643/643). No tsc/build/browser/DB writes (analyst-only contract).
   468→  1511→
   469→  1512→Stage Summary:
   470→  1513→FINDINGS:
   471→  1514→[P1] hero-console.tsx:307,315,316 + automation-simulator.tsx:441,511,544,553,743 + bento.tsx:418,447,600 + before-after.tsx:938 + shared/page-hero.tsx:37 — Incomplete blue-palette revert: 13 amber remnants survived the R1-R6 sweep
   472→  1515→  Evidence: hero-console ConsoleSvgDiagram `stroke="rgba(217,119,6,0.3)"` / `fill="rgba(217,119,6,0.15)"` / `stroke="rgba(217,119,6,0.6)"`; simulator `edgeDone ? 'rgba(217,119,6,0.45)'`, `shadow-[0_0_12px_rgba(217,119,6,0.95)]`, `shadow-[0_0_28px_rgba(217,119,6,0.55)]`, `animate={{ boxShadow: ['0 0 0 0 rgba(217,119,6,0.5)'…` , `shadow-[0_0_32px_rgba(217,119,6,0.28)]`; bento `boxShadow: '0 0 0 4px rgba(217,119,6,0.35)'`, `shadow-[0_0_6px_rgba(217,119,6,0.9)]`, `radial-gradient(130px 90px … rgba(217,119,6,0.28)`; before-after `boxShadow: 'inset 0 0 0 1.5px #b45309'`; page-hero `radial-gradient(60% 60% at 50% 0%, rgba(245,158,11,0.18)…)`.
   473→  1516→  Impact: WORST at hero-console: `showSvg = mounted && (isMobile || reduced || !('WebGLRenderingContext' in window))` means EVERY phone (<768px) and every reduced-motion user who submits a hero prompt sees an AMBER node diagram, while desktop users see the blue WebGL constellation (console-scene PRESET colors were reverted). Simulator shows blue `border-primary` nodes with amber glow shadows on the same element (home + /services/automation). All 5 inner-page heroes carry a faint amber top glow stacked over the blue .hero-fallback. Violates the user's explicit blue-revert directive; prior convergence round classed palette integrity as top severity.
   474→  1517→  Recommended fix: one sweep restoring the pre-amber values — hero-console SVG → rgba(0,113,227,0.3/0.15/0.6); simulator glows → rgba(0,113,227,…) family (match elyra-edge #4285F4 stops); bento MiniFlow/MiniCube glows → rgba(0,113,227,…); before-after cell ring #b45309 → #1d4ed8 (pre-amber value); page-hero radial → rgba(0,113,227,0.18). Then re-grep `217,119,6|b45309|245,158,11` in src/ → must be zero.
   475→  1518→[P2] bento.tsx:69 + messages/ar.json:139-143 + messages/en.json:139-143 — MiniSite 4th theme swatch caption mislabels its color in BOTH locales
   476→  1519→  Evidence: `const SITE_PALETTE = ['#0071E3', '#34A853', '#EA4335', '#4285F4']` vs palette names `["كحلي","زمردي","مرجاني","عنبري"]` / `["Cobalt","Emerald","Coral","Amber"]` — swatch 4 is #4285F4 (blue) but reads "عنبري"/"Amber".
   477→  1520→  Impact: clicking the 4th swatch turns the whole mini-storefront blue while the live caption ("الهوية الحالية: عنبري") claims Amber — factually wrong UI copy in ar+en; the caption drifted during the amber phase and the hex was reverted without re-aligning names (swatches 1-3 happen to be valid again).
   478→  1521→  Recommended fix: rename palette[3] → AR "سماوي" / EN "Sky" (keep #4285F4), both catalogs; optionally also re-check swatch-1 name ("كحلي"/"Cobalt" is fine for #0071E3).
   479→  1522→[P2] console-scene.tsx (whole file; mount path hero-console.tsx:197,262) — no WebGL context-creation-failure / context-loss guard, unlike the other two canvases
   480→  1523→  Evidence: hero-console gates only on `!('WebGLRenderingContext' in window)` (API presence, not creation success); console-scene's Canvas has no ContextLossGuard and no feature-detection fallback — grep confirms ContextLossGuard exists in hero-canvas.tsx:531 and capability-scene.tsx:541 but NOT in console-scene.tsx.
   481→  1524→  Impact: on a GPU-blocklisted/driver-crashed browser that still exposes WebGLRenderingContext, THREE.WebGLRenderer throws during R3F render → the nearest boundary ([locale]/error.tsx) replaces the ENTIRE page for a decorative element. A mid-session context loss would similarly freeze/throw inside the hero. Edge-case blast radius is disproportionate.
   482→  1525→  Recommended fix: mirror hero-canvas's pattern — probe `document.createElement('canvas').getContext('webgl2') ?? …('webgl')` in the showSvg condition (or a tiny useHasWebGL hook) and add the ContextLossGuard component to the Canvas children.
   483→  1526→[P3] hero.tsx:66 — `focus-visible:ring-offset-elyra-deep` is an unresolvable Tailwind utility (silent no-op)
   484→  1527→  Evidence: `className="… focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-elyra-deep"` on OrbitBadge; @theme inline defines --color-elyra-dark/--color-background-deep but NO --color-elyra-deep (only the hand-written `.bg-elyra-deep` class at globals.css:685 exists, which Tailwind utilities don't derive from).
   485→  1528→  Impact: the class never compiles → the badge's focus-ring offset keeps the default color (white-ish) on the #08080A hero, producing a wrong-colored halo around the focus ring. Ring itself still visible (not a blocker).
   486→  1529→  Recommended fix: either add `--color-elyra-deep: var(--background-deep);` to @theme inline, or change the class to `focus-visible:ring-offset-elyra-dark` (matches the hero CTA pattern used 20 lines below).
   487→  1530→[P3] hero-console.tsx:263-266 — dead cursor affordance on the scene label chip
   488→  1531→  Evidence: the chip carries `data-cursor="rotate" data-cursor-label={t('placeholder')}` but also `pointer-events-none`, so pointerover can never target it (custom-cursor matches via closest() from e.target).
   489→  1532→  Impact: the "rotate" cursor label promised over the console scene never activates; attribute pair is dead code.
   490→  1533→  Recommended fix: move data-cursor/data-cursor-label onto the hit-testable container (#hero-console-scene div) or drop the attributes.
   491→  1534→[P3] calculator.tsx:189 — aria-label + aria-labelledby conflict on the calculator section
   492→  1535→  Evidence: `<section id="calculator" … aria-labelledby="calc-title" aria-label={t('kicker')}>` — when both are present aria-label wins.
   493→  1536→  Impact: the landmark's accessible name becomes the short kicker instead of the full h2 title; the labelledby wiring is inert.
   494→  1537→  Recommended fix: delete the `aria-label={t('kicker')}` attribute (keep labelledby).
   495→  1538→[P3] contact/page.tsx:97 — section landmark labelled "channels" also contains the form + social proof
   496→  1539→  Evidence: `<section className="bg-background py-20…" aria-labelledby="channels-title">` wraps the 2-col grid (channels list AND form column with its own h2 "form.title").
   497→  1540→  Impact: SR users navigating by landmarks hear a "قنوات مباشرة" region that also holds the classic form — the region name under-describes half its content.
   498→  1541→  Recommended fix: split into two sections (each labelled by its own h2) or relabel the outer one (e.g. a "تواصل" heading id).
   499→  1542→[P3] contact-form.tsx:108-115 — prefill template is seeded once; stale on soft re-navigation
   500→  1543→  Evidence: `useState(() => ({ …, message: buildTemplate(initialService, prefillIdea) }))` — buildTemplate runs only in the lazy initializer; props changes never re-seed.
   501→  1544→  Impact: latent today (all prefill producers live on other routes), but a client-side locale switch on /contact?service=… keeps the seeded message in the OLD language, and any future same-route searchParam change would keep the stale template. User edits being preserved is correct; only the untouched-template case drifts.
   502→  1545→  Recommended fix: if ever needed, re-seed in a [prefillService, prefillIdea, locale]-keyed effect guarded by "textarea still equals previous template" (same edit-protection as onToggleService).
   503→  1546→[P3] calculator.tsx:518 + contact-form.tsx:227 — honeypot `-left-[9999px]` physical offset (documented RTL quirk)
   504→  1547→  Evidence: `className="pointer-events-none absolute -left-[9999px] h-px w-px overflow-hidden"`.
   505→  1548→  Impact: pre-existing + documented (UI-5 note: /contact body scrollWidth reads 11295 under RTL; not visually scrollable) — listed for completeness.
   506→  1549→  Recommended fix: switch to the sr-only clip pattern or `position: fixed; inset-inline-start: -9999px` to kill the phantom scrollWidth.
   507→  1550→[P3] deconstructed-card.tsx:163 — hardcoded Latin fallback metric
   508→  1551→  Evidence: `const metricMain = metrics?.[0] ?? '+140%'`.
   509→  1552→  Impact: unreachable while catalogs ship workSection.project1.metrics (verified present) — but if the key ever goes missing the showcase silently renders an English string on the AR site.
   510→  1553→  Recommended fix: fall back to `''` or omit the metric line when absent.
   511→  1554→[P3] about/page.tsx:124 — unreachable avatar-gradient fallback (dead code)
   512→  1555→  Evidence: `AVATAR_GRADIENTS[i % AVATAR_GRADIENTS.length] ?? 'from-primary to-g-blue'` — modulo over a 4-entry array always yields a defined value.
   513→  1556→  Impact: dead branch only; minor noise.
   514→  1557→  Recommended fix: drop the `?? …` (or keep as a deliberate guard with a comment).
   515→  1558→[P3] home compact interactive controls below 44px touch targets (documented scope decision)
   516→  1559→  Evidence: hero-console preset chips `px-3 py-1.5 text-sm` (~33px), MiniAgent input/button h-9 (36px), simulator scenario toggles h-10 (40px), calculator option chips py-3 (~44px ok) but integration chips py-1.5 (~33px).
   517→  1560→  Impact: sub-44px targets on the home conversion path (console chips are the hero's primary intent input). Batch 1 item 4 deliberately scoped the 44px rule to navbar; the remainder was left compact by design (4-I3 risk note 3).
   518→  1561→  Recommended fix: if the rule is extended site-wide, bump chips to min-h-11 / py-2.5 in a single pass; otherwise record the scope decision as final.
   519→  1562→POSITIVE VERIFICATIONS (no action): DamascusClock placeholder+'--:--:--' setState-in-effect pattern is hydration-safe; useSyncExternalStore server-snapshot pattern (reduced-motion, sound, global-error language) prevents hydration mismatches across all animated components; universal :lang(ar) letter-spacing reset neutralizes every tracking-* leak I found (kicker, calculator reference pill, simulator headings); dir="ltr" islands correct (clock, console/payload/log, orbit seal, URLs, phone values, formula bar); Next-16 Promise params/searchParams awaited everywhere; per-page generateMetadata via buildPageMetadata with canonical+hreflang+x-default and pinned OG cards; JSON-LD on home (no fake SearchAction); heading hierarchies clean on every route (sr-only h2 on /work fixes the old jump); [...rest] hard-404 + per-segment loading.tsx rationale verified; all internal nav links resolve to real routes; lazy wrappers genuinely code-split (dynamic import + IO-gated mount + matched placeholders); eslint 0/0 on all 33 files; R7 lessons correctly applied (data-bg-layer on all hero decorative absolutes, h1 flex-col, testimonial Parallax not a direct spotlight child).
   520→  1563→VERDICT: 0 P0, 1 P1, 2 P2, 9 P3 — architecture is sound (hydration, a11y, i18n, SEO, perf discipline all verified strong); the one real debt is an incomplete amber→blue revert sweep concentrated in fallback/glow layers, plus two content/robustness gaps; everything else is polish.
   521→  1564→ADDENDUM (L1-C, post-report self-correction): My [P2] console-scene WebGL-guard finding overclaimed the blast radius. Cross-checking @react-three/fiber v9.7 source (events-156d8d12.esm.js:15700-15743, 15928-15936) confirms L1-D's verification: Canvas root.configure() is async and un-awaited — a WebGLRenderer constructor failure rejects silently (unhandled promise rejection in console; children never mount), it does NOT propagate into React's render path, so [locale]/error.tsx never trips. Corrected impact: on browsers where getContext('webgl2'/'webgl') fails despite WebGLRenderingContext existing (blocklisted GPU/driver crash), the hero-console scene area is a ~280px empty dark box with a console error — a degraded-UX edge case, not a page crash. Corrected severity: P3 (probe context creation in the showSvg gate and/or add ContextLossGuard for parity with the other two canvases). Amended verdict: 0 P0, 1 P1, 2 P2, 10 P3 (the P2 slot reverts to the two content/consistency findings; the WebGL guard joins the P3 bucket).
   522→  1565→
   523→  1566→---
   524→  1567→Task ID: 2-a
   525→  1568→Agent: Palette & Catalogs Fix Specialist (Group 2 — Fix)
   526→  1569→Task: LOOP 1 fixes — amber→blue sweep, bento caption, dead keys, touch targets
   527→  1570→
   528→  1571→Work Log:
   529→  1572→- Read worklog tail (L1-A/B/C/D audit reports) + all 10 scoped files (and every amber-adjacent region) before editing; verified the 8 "dead" keys with namespace-aware greps first (navbar consumes nav.websites/automation/work/about/contact — never nav.services; calculator uses its own calculator.back/next — not common.*; zero src/ references to getStarted/explore/close/reduceMotionNote/canvasFallback).
   530→  1573→- AMBER→BLUE SWEEP (P1 — 16 audited instances across 6 files, +1 bonus straggler = 17 total):
   531→  1574→  - hero-console.tsx:307 stroke rgba(217,119,6,0.3)→rgba(0,113,227,0.3); :315 fill rgba(217,119,6,0.15)→rgba(0,113,227,0.15); :316 stroke rgba(217,119,6,0.6)→rgba(0,113,227,0.6) — the mobile/reduced-motion ConsoleSvgDiagram now reads like the blue WebGL constellation it replaces; green center node (rgba(52,168,83,…)) intentionally untouched as the #34A853 counterpoint.
   532→  1575→  - automation-simulator.tsx:441 edgeDone stroke rgba(217,119,6,0.45)→rgba(66,133,244,0.45) (settled sibling of the #4285F4 elyra-edge active gradient); :511 packet-dot glow shadow-[0_0_12px_rgba(217,119,6,0.95)]→…rgba(0,113,227,0.95)] (dot is bg-primary); :544 active-node glow →rgba(0,113,227,0.55)] (node is border-primary); :553 framer-motion ping ring ['0 0 0 0 rgba(217,119,6,0.5)'→'0 0 0 12px rgba(217,119,6,0)'] → rgba(0,113,227,…) both stops (ring is border-primary/50); :743 payload-flash glow →rgba(0,113,227,0.28)] (panel flashes border-primary/60).
   533→  1576→  - bento.tsx:418 reduced-motion static ring rgba(217,119,6,0.35)→rgba(0,113,227,0.35) (node is border-primary); :447 MiniFlow traveling-dot glow →rgba(0,113,227,0.9)] (dot is bg-primary); :600 MiniCube ambient radial rgba(217,119,6,0.28)→rgba(0,113,227,0.28).
   534→  1577→  - before-after.tsx:938 old-sheet selected-cell ring #b45309→#1d4ed8 (the pre-amber value). BONUS straggler beyond the audit list: :440 text-amber-400 (★★★★★ rating stars in the site-new mock) → text-g-blue (#4285F4 token — blue-family, matches the AI-badge accent convention).
   535→  1578→  - page-hero.tsx:37 every inner-page hero ambient radial rgba(245,158,11,0.18)→rgba(66,133,244,0.18) (soft g-blue stacked over .hero-fallback).
   536→  1579→  - opengraph-image.tsx:44 social-card radial rgba(245, 158, 11, 0.35)→rgba(0, 113, 227, 0.35).
   537→  1580→  - not-found.tsx:48 chip hover border rgba(245,158,11,.5)→rgba(0,113,227,.5); :60 page radial rgba(245,158,11,0.16)→rgba(0,113,227,0.16).
   538→  1581→- STALE "PALETTE LAW: NO BLUE" COMMENTS reworded (5): opengraph-image.tsx:11-13 → "quad-dot brand language (blue/green/red/yellow — Google-family palette; primary #0071E3, accent #4285F4)… (#0F172A + brand-blue glow)"; opengraph-image.tsx:37-38 inline "(amber, was blue)" → "(brand blue #0071E3)"; not-found.tsx:134-140 → "Plain <a> hrefs are safe: routing has localeDetection:false, so unmatched paths deterministically serve the default Arabic locale — no locale-detect/redirect dance"; logo.tsx:10-15 → "intentional Google quad-dot brand mark (blue/green/red/yellow: #4285F4/#34A853/#EA4335/#FBBC05 — deliberately outside the token system)"; bento.tsx:71-79 contrast-derivation comment — removed an accidental duplicated line AND reworded stale "amber being the worst offender" → "the lighter swatches being the worst offenders".
   539→  1582→- BENTO PALETTE CAPTION (P2): messages/{ar,en}.json bento.websites.mini.palette[3] "عنبري"/"Amber" → "سماوي"/"Sky" (hex #4285F4 unchanged; swatch names 1-3 كحلي/زمردي/مرجاني · Cobalt/Emerald/Coral re-verified correct for #0071E3/#34A853/#EA4335). Live caption "الهوية الحالية: سماوي" / "Current identity: Sky" now tells the truth about the blue swatch.
   540→  1583→- DEAD CATALOG KEYS (P3): removed exactly these 8 keys from BOTH ar.json and en.json (usage first verified zero): nav.services, hero.canvasFallback, common.back, common.next, common.close, common.getStarted, common.explore, common.reduceMotionNote. Explicitly PRESERVED: common.cursor.magnet ("") — intentionally empty, consumed by custom-cursor.tsx; common.backToHome — used by [locale]/not-found.tsx. Surgical line-level JSON edits (no reformat); both files JSON.parse-validated.
   541→  1584→- TOUCH TARGETS ≥44px (P3): hero-console.tsx preset chips +min-h-11 (flex-wrap row absorbs); automation-simulator.tsx scenario toggles h-10→min-h-11; bento.tsx MiniFlow run chip +min-h-11, MiniAgent ask row +min-h-11, MiniAgent idea input h-9→min-h-11, MiniAgent convert CTA h-9→min-h-11. All paddings/fonts/colors otherwise untouched.
   542→  1585→- NOT resized (deliberate, recorded): bento.tsx MiniSite color swatches (size-6 = 24px) — decorative pointer toy, aria-hidden + tabIndex={-1} (FIX 2-c/12), never keyboard-focusable; 44px dots would wreck the compact swatch+caption row. hero-console scene label chip (px-3 py-1.5) is pointer-events-none — non-interactive.
   543→  1586→- Verification: Grep across ALL of src/ for 217,119,6|217, 119, 6|b45309|245,158,11|245, 158, 11|d97706|92400e|78350f|f59e0b|fbbf24|fde68a|amber- → ZERO matches; case-insensitive "amber" anywhere in src/ + messages/ → ZERO matches. Intentional quad-dot values #4285F4/#EA4335/#FBBC05/#34A853 untouched everywhere. node scripts/check-i18n-parity.js → "Parity OK: 635 keys matched across ar.json and en.json · ICU placeholders verified on 574 string keys · array lengths verified on 38 array keys" (643−8=635 exactly as predicted). bunx eslint on all 8 touched TSX files → exit 0 (0 errors, 0 warnings). Every edited region re-read via git diff — all hunks landed exactly as intended. No tsc/build/browser; dev server untouched; git scope = exactly my 10 files.
   544→  1587→
   545→  1588→Stage Summary:
   546→  1589→- Amber→blue sweep COMPLETE and grep-verified to zero: 17 value edits (16 audited + 1 Tailwind-class straggler the audits missed) + 5 stale "no-blue-law" comment rewords across hero-console, automation-simulator, bento, before-after, page-hero, opengraph-image, not-found, logo. Blue mapping harmonized per element: rgba(0,113,227,…) wherever the element carries primary/border-primary/bg-primary (packet dot, node glows, ping ring, payload flash, MiniFlow ring/dot, cube ambient, hero-console SVG, OG radial, 404 hover/radial), rgba(66,133,244,…) where g-blue anchors the visual (done-edge stroke matching the elyra-edge gradient stops, page-hero ambient) — matching the audit's guidance to reuse each file's existing blue glow alphas.
   547→  1590→- Bento caption fixed in both locales ("سماوي"/"Sky"); catalogs now 635/635 keys with the 8 dead keys removed; parity + ICU + array-length gates all green; cursor.magnet and backToHome preserved.
   548→  1591→- Touch targets: hero-console preset chips, simulator scenario toggles, bento MiniFlow/MiniAgent controls (ask row, input, convert CTA) all ≥44px; MiniSite swatches intentionally compact (decorative/unfocusable — scope decision stands).
   549→  1592→- FOR COORDINATOR (outside my file scope — not edited): (1) calculator.tsx integration chips (~33px, py-1.5) remain the only sub-44px interactive control on the home conversion path — calculator.tsx belongs to another agent's scope this round; (2) hero-console.tsx:263-266 dead data-cursor="rotate" affordance (pointer-events-none chip) and the false "Drag to rotate" console-scene promise (L1-D P2) were NOT in my fix list — untouched, still open; (3) git status shows agent 2-b concurrently editing prisma/schema.prisma + src/app/api/leads/route.ts — no overlap with my files.
   550→  1593→---
   551→  1594→Task ID: 2-c
   552→  1595→Agent: Backend & Security Fix Specialist (Group 2 — Fix)
   553→  1596→Task: LOOP 1 fixes — reference persistence, quota refund, rate-limit guard, headers, retention policy, deployment hardening
   554→  1597→
   555→  1598→Work Log:
   556→  1599→- Fix 1 (L1-B P2 — reference persistence): prisma/schema.prisma — added `reference String @unique` to Lead (+comment: 'c'+9 base-36, generated pre-create, P2002 regenerates) and documented the `service:'contact'` sentinel for contact-form leads on the service field; the PRIVACY NOTE now points at the shipped purge mode instead of a policy that didn't exist. src/app/api/leads/route.ts — `randomReference()` moved BEFORE `db.lead.create`, `reference` included in create data; create wrapped in a P2002-aware retry (isUniqueViolation = `Prisma.PrismaClientKnownRequestError && code==='P2002'`; max 3 attempts, then the existing generic 500); 201 response shape unchanged; contract doc updated.
   557→  1600→- Deployment sequence: (a) row count via a /tmp bun script (kept OUT of the repo) = 0 rows (coordinator had cleaned earlier) → no deleteMany needed; (b) `bun run db:push` → success, Prisma client regenerated (v6.19.2); (c) smoke test `curl -X POST /api/leads` with a valid contact-form payload + matching Origin header (curl sends no Sec-Fetch-Site, so the CSRF layer passes header-less clients) → 201 {"reference":"c2dc9xjtdk"}; NO manual restart was needed — editing next.config.ts made Next 16 dev self-restart ("Found a change in next.config.ts. Restarting the server…"), which loaded the regenerated client (no "Unknown argument reference" ever appeared in dev.log); (d) /tmp verification script: row persisted WITH reference === the 201 value (service:'contact' sentinel + message stored); second smoke (calculator payload) → 201 {"reference":"cx1fuclsbz"} persisted with server-recomputed estimate 5800–9200 USD; (e) MANDATORY CLEANUP done — deleteMany({}) after verification → FINAL_COUNT: 0; (f) dev.log tail clean of prisma issues.
   558→  1601→- Fix 2 (L1-B P3 — quota refund): rate-limit.ts gained `refundRateLimit(key, bucket)` — removes the NEWEST timestamp (module is synchronous on a single-threaded runtime; documented the bounded one-hit over-refund edge if a same-key request interleaves); route.ts catch-around-create calls `refundRateLimit(ip, 'strict')` before the 500 return (the lenient hit intentionally stays counted). Unit-tested in /tmp: 5 strict hits → 6th blocked → refund → allowed again; refund on unknown key is a safe no-op; lenient bucket untouched (11/11 PASS).
   559→  1602→- Fix 3 (L1-A P3 — inline size guard): rate-limit.ts sweep() split into sweepExpired(now) + evictOverCap() (MAX_KEYS=5_000 / EVICT_BATCH=1_000 as named constants, behavior preserved); rateLimit() now runs the cleanup INLINE when a brand-new key would be inserted while the Map is at/over the cap (existing===undefined && hits.size>=MAX_KEYS) — Map stays bounded between the 60s sweeps (worst overshoot: 1 key). Behavioral test: 6000-key flood → no throw, all allowed.
   560→  1603→- Fix 4 (L1-A/L1-B P3): n8n-webhook.ts deliver() dropped the never-read `secret` parameter (transport-only doc comment added — signing stays in attemptDelivery); call site now `deliver(config.url, body, headers)`. Grep: no other callers exist.
   561→  1604→- Fix 5 (L1-A P3): robots.ts Disallow ['/api','/admin'] → ['/api'] + comment explaining why /admin stays out (nonexistent path = free recon hint). Live robots.txt verified: /admin gone.
   562→  1605→- Fix 6 (L1-A P3): home-json-ld.tsx — JSON.stringify(...).replace(/</g,'\\u003c') on the dangerouslySetInnerHTML payload; proof via bun: {'x':'</script><img src=x>'} → {"x":"\u003c/script>\u003c<img src=x>"} (zero raw '<', still valid JSON). Current build-time data contains no '<' so today's output is byte-identical (live JSON-LD verified on the 200 homepage).
   563→  1606→- Fix 7 (L1-A P2): next.config.ts securityHeaders += Cross-Origin-Opener-Policy: same-origin and Cross-Origin-Resource-Policy: same-origin (comment: no-ops for the app's own functionality — no popups, same-origin assets). Live-verified present on curl -I /.
   564→  1607→- Fix 8 (L1-A P2 — TRUST_PROXY posture): Dockerfile runtime ENV block += TRUST_PROXY=true (+rationale comment; header run-comment updated); README docker run example += -e TRUST_PROXY=true plus an Arabic explanation of the single-global-bucket lockout it prevents.
   565→  1608→- Fix 9 (L1-A P2 — PII retention): scripts/clean-leads.ts extended with --purge-days=<N> (age-based deleteMany on createdAt, logs the count; --dry-run composes; a MALFORMED value now hard-errors instead of silently falling back to the full wipe — footgun found live while testing: `--purge-days=abc` previously executed the FULL wipe because unknown flags were ignored). README Deployment gained "الاحتفاظ بالبيانات (Data retention)" (recommended 90-day purge on cron/systemd timer; PII inventory: ipAddress/userAgent/email/whatsapp/message) and the sandbox-Caddyfile warning subsection. End-to-end tested: lead created → backdated 100 days via /tmp script → --purge-days=90 --dry-run counted 1 → real purge deleted 1 → count 0.
   566→  1609→- Fix 10 (L1-A P2): NEW deploy/Caddyfile.example — production-safe Caddy site block (automatic TLS, `encode zstd gzip`, reverse_proxy 127.0.0.1:3000 with X-Forwarded-For/X-Real-IP OVERWRITE via {remote_host}, optional commented-out header backstop), header comment "Production example — the root Caddyfile is sandbox-only", cross-referenced from the README warning.
   567→  1610→- Fix 11 (L1-B P3): package.json scripts += "typecheck": "tsc --noEmit" (README commands list switched from `bunx tsc --noEmit` to `bun run typecheck`); components.json hooks alias "@/hooks" → "@/lib" (LS-verified: all 5 hooks live in src/lib — use-rtl/use-reduced-motion/use-magnetic/use-near-viewport/use-cursor-velocity; src/hooks does not exist).
   568→  1611→- Fix 12 (L1-A P3): route.ts body-size gate documented (framing assumption: both gates key off Content-Length/Transfer-Encoding; the shipped HTTP/1.1 standalone server always emits one; length-less h2 DATA would bypass — unreachable here, never expose the app over h2/h2c). Also fixed the stale README 201 row in passing (claimed "first 8 chars of the record id" — wrong since final-board R1; now documents the random persisted reference).
   569→  1612→- Verification: `bunx eslint` on all 7 touched TS files (route.ts, rate-limit.ts, n8n-webhook.ts, robots.ts, home-json-ld.tsx, next.config.ts, clean-leads.ts) → 0 errors 0 warnings. Negative-path smokes: invalid payload → 400 with translated field errors; honeypot → indistinguishable fake 201 with NO row (count stayed 0); cross-site Origin → 403. P2002 detector unit-tested against real Prisma 6.19.2 classes (P2002 ✓ / P2021 ✗ / generic Error ✗). Final state: GET / 200 · /contact 200 · robots.txt 200 · sitemap.xml 200 · DB 0 rows · dev.log tail clean. No tsc/build/browser per protocol; dev server never manually restarted (Next self-restarted on the next.config.ts change and picked up the regenerated Prisma client). Git scope: exactly my 12 files + new deploy/Caddyfile.example; temp scripts lived in /tmp and were deleted after use.
   570→  1613→
   571→  1614→Stage Summary:
   572→  1615→- All 13 fix-list items DONE: reference persisted with unique-index retry (e2e-proven for BOTH lead sources, estimate recomputed server-side); strict-quota refund on write failure; rate-limit inline size guard + new refund API; deliver() secret param dropped; /admin unadvertised; JSON-LD '<' escaping; COOP/CORP headers live; TRUST_PROXY=true in Dockerfile + README; --purge-days retention mode + README policy + sandbox-Caddyfile warning; production deploy/Caddyfile.example; typecheck script + hooks alias fix; body-size framing assumption documented.
   573→  1616→- DB ended at exactly 0 lead rows; all smoke-test state was disposable; dev server healthy at hand-off (200s, no new errors in dev.log).
   574→  1617→- Deferred / notes for coordinator: (1) CSP script-src 'unsafe-inline' REMAINS by documented tradeoff (static prerender incompatibility) — hash/nonce CSP is future work per L1-A; (2) the L1-A P1 (GitHub token embedded in the .git/config remote URL) is an ops/environment item outside code scope — NOT touched; owner should `git remote set-url origin https://github.com/...` and rotate the token; (3) transient GET / 500s observed mid-session were agent 2-d's calculator.tsx mid-edit parse error (their scope) — recovered to 200 by itself; (4) agent 2-d's log line "agent 2-b concurrently editing prisma/schema.prisma + src/app/api/leads/route.ts" was ME mid-session (misID) — final file states contain exactly my edits (md5-verified); (5) the production Docker build remains untestable in this sandbox (pre-existing Dockerfile note stands).
   575→  1618→
   576→  1619→---
   577→  1620→Task ID: 2-d
   578→  1621→Agent: Frontend Polish & CSS Fix Specialist (Group 2 — Fix)
   579→  1622→Task: LOOP 1 fixes — SEO alt unification, a11y polish, honeypot RTL, dead CSS purge, parity script hardening
   580→  1623→
   581→  1624→Work Log:
   582→  1625→- Read worklog tail (all four L1-A/B/C/D audit reports in full) + every in-scope file before editing. Re-verified each dead-CSS class via Grep across src/ (zero TSX references for elyra-grain / elyra-cursor base / -chip / --visible / glass-light / blend-difference / prose-ar-max; elyra-cursor-dot/-ring only in custom-cursor.tsx + CSS).
   583→  1626→- Fix 1 (L1-B P3, OG alt): created shared `OG_IMAGE_ALT = 'Elyra — Stunning Websites · n8n Automation · Digital Studio'` in src/lib/site-config.ts:34 (kept seo.ts's richer variant); consumed it in src/lib/seo.ts:53, src/app/[locale]/layout.tsx:91, src/app/[locale]/page.tsx:44 (all three previously diverged). layout.tsx:98-100: `metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000')` → `new URL(SITE_URL)` (the already-imported constant).
   584→  1627→- Fix 2 (L1-D P3, live-clock): removed `timeZoneName: 'short'` from the Intl.DateTimeFormat options (live-clock.tsx:25) — the formatter's embedded zone duplicated the manual "(GMT+3)" tzLabel. Manual GMT±N label logic untouched for ar/en.
   585→  1628→- Fix 3 (L1-D P3, footer): five social anchors (:157/:169/:181/:193/:205) `size-9` → `size-11` (44px targets, navbar/CTA standard) + compensating `gap-3` → `gap-2.5` on the icon row (L1-D's own recommendation — row grows only 32px inside the connect column; verified grid math: at ≥1280px the 260px row fits the container's 64px side padding).
   586→  1629→- Fix 4 (L1-D P3, sheet): added `aria-describedby={undefined}` to SheetPrimitive.Content in sheet.tsx (declared BEFORE {...props} so consumers can override). Verified against @radix-ui/react-dialog dist: the DescriptionWarning only fires when the DOM attribute exists but its target doesn't — the explicit undefined suppresses the dev warning; navbar (only consumer) needs no change.
   587→  1630→- Fix 5 (L1-C P3, calculator): deleted `aria-label={t('kicker')}` from the section (aria-label was overriding aria-labelledby="calc-title"); integration chips (~33px) gained `min-h-11` (44px, items-center keeps content vertically centered, flex-wrap row absorbs height); honeypot `absolute -left-[9999px]` → `fixed -start-[9999px]`.
   588→  1631→- Fix 6 (L1-C P3, contact landmarks): outer wrapper section→plain div (layout classes kept); channels column div→`<section aria-labelledby="channels-title">`; form column div→`<section aria-labelledby="contact-form-title">` with `id="contact-form-title"` added to its existing visible h2 (t('form.title') — no new i18n keys needed).
   589→  1632→- Fix 7 (L1-C P3 ×2, contact-form): honeypot same physical→logical+fixed fix; prefill re-seed guard added — `lastTemplateRef` (initialized to the seeded template) + useEffect that rebuilds the template from the visitor's CURRENT intent (chip state + prefillIdea) and reseeds the message ONLY while it still equals the last generated template (or is empty — same edit-protection as onToggleService; user edits always preserved). buildTemplate wrapped in useCallback([t]) so the locale switch re-keys the effect via t's identity; deps = [prefillService, prefillIdea, locale, service, buildTemplate] (satisfies the error-level exhaustive-deps WITHOUT a disable — an eslint-disable there also trips react-compiler's "rules disabled" warning, found empirically). onToggleService refactored to compute reseed outside the updater and keep lastTemplateRef in sync; traced: mount run is a no-op (prev===next), chip toggles no-op through the ref sync, locale switch reseeds untouched templates only.
   590→  1633→- Fix 8 (L1-C P3, deconstructed-card): `metrics?.[0] ?? '+140%'` → `?? ''` + metric <p> conditionally rendered in ResultsContent — no hardcoded Latin fallback on the AR site; icon layer intact.
   591→  1634→- Fix 9 (L1-C P3, about): dropped unreachable `?? 'from-primary to-g-blue'` on AVATAR_GRADIENTS modulo (about/page.tsx:126).
   592→  1635→- Fix 10 (L1-C P3, hero): OrbitBadge `focus-visible:ring-offset-elyra-deep` → `ring-offset-elyra-dark` (matches the hero CTA pattern 20 lines below; --color-elyra-deep doesn't exist in @theme).
   593→  1636→- Fix 11 (L1-D P2, globals.css): deleted 118 lines / added 29 comment lines (net −89; file 1212→1123): .glass-light, .blend-difference, .prose-ar-max + orphaned --ar-max-prose var, the ENTIRE Phase-2 sensory block (.elyra-grain, .elyra-cursor base, Phase-2 dot/ring, .elyra-cursor-chip, .elyra-cursor--visible, duplicate cursor:none @media nest), the three dead print selectors, and the R7-b positioner resets (width/height/background/border) that existed only to neutralize Phase-2 — after reading custom-cursor.tsx I confirmed the R7-b -core rules fully specify every visual (7px dot, 34px ring w/ border+label styles) and the positioners' shrink-to-fit resolves to 0×0 (only abspos children), so no reset was load-bearing; mix-blend-mode/opacity/transition/position stay on the positioner rule. R7-b header + print comments reworded to reflect single ownership. Tombstone comments mark each deletion site. Everything else byte-preserved (verified via git diff hunks): :lang(ar) letter-spacing reset, reduced-motion kill-switch, spotlight lifting rule, marquee/intro/manifesto CSS, z-ladder intro 80 / grain 90 / cursor 200, R7-b print block.
   594→  1637→- Fix 12 (L1-B P3, parity script): added (a) empty-string check — FAIL on any "" outside an allowlist; NOTE: the prescribed allowlist ['common.cursor.magnet'] was extended with 'stats.hours.suffix' + 'pages.about.numbers.years.suffix' because BOTH are intentionally-empty numeric stat suffixes in the two catalogs (verified against consumers; a single-key allowlist would have failed the gate the task required to stay green); (b) advisory identical ar/en STRING values — count + up to 10 example keys, exit 0 (current: 19 strings + 23 numbers = the 42 legitimate values L1-B verified; numbers excluded as expected-equal). Unused-key scan deliberately NOT implemented — rationale documented in a comment (t()-literal scans defeat dynamic keys like t(`services.${id}.title`)). All existing checks + output format kept.
   595→  1638→- Verification: scoped `bunx eslint` on all 14 touched files → 0 errors, 0 warnings. `node scripts/check-i18n-parity.js` → exit 0 ("Parity OK: 635 keys" — agent 2-a's 8 dead-key deletions landed mid-session; all my referenced keys verified present). Tailwind v4.1.18 compile probe via the tailwindcss API confirms `-start-[9999px]` → `inset-inline-start: calc(9999px * -1)` (project already used -start-6/start-[8%]). Post-deletion Greps: dead classes appear only in tombstone comments; elyra-cursor-dot/-ring only in custom-cursor.tsx + the R7-b section. No tsc/build/browser/dev-server touched. Git scope: exactly the 16 files in my exclusive list (messages/*.json untouched — zero new i18n keys needed).
   596→  1639→- NOTE for 2-b/coordinator: the transient GET / 500s 2-b observed mid-session were my calculator.tsx mid-edit state (a JSX comment briefly placed above `return (` — invalid attribute position); fixed within the same edit window, final file passes eslint 0/0. No other agent's files were touched by me.
   597→  1640→
   598→  1641→Stage Summary:
   599→  1642→- All 13 fix-list items DONE: OG alt unified via OG_IMAGE_ALT + SITE_URL reuse (single source of truth in site-config.ts); footer clock no longer double-renders the timezone; footer social icons at 44px; sheet dialog Radix warning silenced; calculator landmark named by its h2 + 44px integration chips + RTL-safe fixed/logical honeypots in BOTH forms; /contact split into two properly-named section landmarks (no new catalog keys); deconstructed-card/about dead fallbacks removed; hero badge focus ring now resolves; globals.css purged of 89 net lines of Phase-2 dead CSS with the R7-b layer confirmed self-sufficient; parity script hardened with the empty-string gate (3-key allowlist) + advisory identical-value warning.
   600→  1643→- Verification: eslint 0/0 on all touched files; parity gate green with new checks active; dead-class greps zero; git diff reviewed hunk-by-hunk for every file (surgical, no whole-file rewrites).
   601→
   602→[showing lines 1044-1643 of 1645; use offset=1644 with limit to continue reading]