  1245→Stage Summary:
  1246→- Hero transformed from centered generic SaaS → asymmetric editorial-cinematic composition with masked word reveals, live Damascus clock, outlined watermark, vertical rail, orbit seal badge, service marquee edge, blend-difference cursor, film grain, magnetic CTAs, domain-warped silk WebGL backdrop, and a scroll-lit manifesto statement.
  1247→- Key architectural lessons: (1) .elyra-spotlight's content-lift rule forces position:relative on ALL non-bg-layer children — any new decorative absolute in spotlight sections MUST carry data-bg-layer; (2) mask-reveal line wrappers need flex-col parents to avoid negative-margin collapse; (3) additive shader intensity must be pixel-verified (VLM undersells subtle luminance; measure means + blue-dominance).
  1248→- Commits: this round's work committed as R7.
  1249→
  1250→---
  1251→Task ID: L1-A
  1252→Agent: Security Audit Specialist (Group 1 — Analysis)
  1253→Task: LOOP 1 deep security audit of API/leads pipeline, rate limiting, webhook, config, headers, robots/sitemap
  1254→
  1255→Work Log:
  1256→- Read worklog tail (project conventions: no concurrent tsc with dev server, eslint at error level, analyst-only role, worklog is the only writable file).
  1257→- Read IN FULL: src/app/api/leads/route.ts (482 L), src/lib/rate-limit.ts (87 L), src/lib/n8n-webhook.ts (198 L), src/lib/lead-fields.ts (85 L), src/lib/api-i18n.ts (35 L), src/proxy.ts (13 L), src/app/robots.ts (14 L), src/app/sitemap.ts (46 L), src/app/[locale]/opengraph-image.tsx (189 L), src/app/[locale]/apple-icon.tsx (86 L), src/app/icon.tsx (86 L), next.config.ts (82 L), .env.example (32 L), src/components/pages/contact-form.tsx (321 L).
  1258→- Cross-referenced the full request lifecycle: contact-form.tsx / calculator.tsx fetch → route.ts POST → header gates → CLIENT_ECHO strip → Zod strict parse → honeypot → strict bucket → Prisma create → fire-and-forget webhook → response. Also read supporting files: src/lib/db.ts, src/lib/calculator.ts (computeEstimate — bounded, no ReDoS), src/lib/seo.ts, src/lib/site-config.ts, prisma/schema.prisma, scripts/clean-leads.ts, the root reverse-proxy file, Dockerfile, package.json, messages/{ar,en}.json apiErrors block (all keys present), src/app/[locale]/contact/page.tsx (searchParams path), src/components/seo/home-json-ld.tsx, examples/websocket/server.ts, README (Deployment + webhook recipe sections).
  1259→- Environment checks: `ls -la .env*` (.env exists, 50 B, untracked; .env.example tracked). `git ls-files | rg env` → only .env.example. Git history audit of .env* with VALUES REDACTED (key names only, never printed): commit 6d367b6 "Initial commit" ADDED .env (single key: DATABASE_URL — a local SQLite path, not a credential); commit b659e9b deleted it; current .env untracked & ignored (.gitignore:35 `.env*`). `git remote -v` → origin URL embeds an `x-access-token:<token>@github.com/...` credential (token value auto-redacted by tooling; existence noted, never printed).
  1260→- Grep checks: `dangerouslySetInnerHTML` → exactly one usage (home-json-ld.tsx, static build-time data only). `N8N_WEBHOOK|NEXT_PUBLIC_` in src → N8N_* only in server-only src/lib/n8n-webhook.ts; NEXT_PUBLIC_ only SITE_URL (seo.ts:5, layout.tsx:99). No other API route exists (glob src/app/api → leads/route.ts only; exports POST + OPTIONS, no GET → no lead-read endpoint).
  1261→- Deps skim: zod resolved to 4.3.5 (bun.lock:1153); all packages mainstream, no red flags. dev.log / db/*.db gitignored. tool-results/ untracked-but-not-ignored (was purged from tracking once before, commit 887c4dc).
  1262→- Explicit mandated verifications: (a) rate-limit Map cleanup EXISTS — rate-limit.ts:36-54 sweep every 60 s + 5 000-key hard cap dropping oldest 1 000; (b) ALL string lengths bounded — lead-fields.ts (name 2–100, email ≤254, whatsapp 5–30 + charset regex, message 10–5000, honeypot ≤200) + route.ts strictObject + integrations .max(6); (c) webhook error paths log to SERVER console only (n8n-webhook.ts:112,189-193) and route.ts:463-470 returns a generic translated 500 — no URLs/stacks/Prisma details reach the client; (d) NO secret in client-visible code — N8N_WEBHOOK_URL/SECRET referenced only in the server-side webhook module imported solely by the API route; (e) searchParams rendering SAFE — contact/page.tsx:51-60 whitelists service, strips Cc/Cf, clamps idea to 300 chars, and rendering goes through next-intl ICU interpolation + React text nodes (auto-escaped); no XSS sink on any user-controlled path.
  1263→
  1264→Stage Summary:
  1265→FINDINGS:
  1266→
  1267→[P1] .git/config (environment; repo remote) — Live GitHub access token embedded in the origin remote URL
  1268→  Evidence: `git remote -v` → `https://x-access-token:<redacted>@github.com/ashamsaldin303-ctrl/nextjs-fullstack-project.git` (credential detected in the stored remote URL; value never printed).
  1269→  Impact: Any process, backup, CI log, or pasted `git remote -v` output that reads .git/config yields a usable GitHub credential for that repo (push/fetch scope unverified). It is a plaintext secret at rest on disk.
  1270→  Recommended fix: `git remote set-url origin https://github.com/ashamsaldin303-ctrl/nextjs-fullstack-project.git` and authenticate via credential helper or SSH deploy key instead; rotate the token if it was ever copied anywhere.
  1271→
  1272→[P2] prisma/schema.prisma:15-17 + scripts/clean-leads.ts:20 + README Deployment — Lead PII (ipAddress, userAgent, email, whatsapp, message) retained indefinitely; promised retention policy does not exist
  1273→  Evidence: schema comment says "Define a retention policy before real production use (e.g., purge Lead rows older than 90 days) — see README 'Deployment'"; README has NO retention/purge text (rg 'retention|purge|90 days' → 0 hits) and clean-leads.ts only does `deleteMany({})` as a pre-launch full wipe; nothing is scheduled.
  1274→  Impact: Privacy/compliance exposure (IP + UA + full contact text are personal data under GDPR-like regimes); SQLite grows forever; a leaked DB file exposes the complete contact history.
  1275→  Recommended fix: Add an age-based purge job (cron/GitHub Action running `prisma.lead.deleteMany({ where: { createdAt: { lt: <cutoff> } } })`, or null out ipAddress/userAgent after 30–90 days keeping the business fields), and document the policy in README Deployment where the schema comment points.
  1276→
  1277→[P2] Root reverse-proxy file :6-18 — Shipped proxy config contains an intentional open-proxy/SSRF primitive (?XTransformPort)
  1278→  Evidence: `reverse_proxy localhost:{query.XTransformPort}` proxies any request carrying `?XTransformPort=<port>` to an arbitrary localhost port; header block explicitly warns "SANDBOX-ONLY … NEVER deploy this publicly".
  1279→  Impact: An operator copy-pasting the repo's only provided proxy config into production exposes an SSRF relay into every localhost service on the host (databases, n8n, admin panels). The app's own TRUST_PROXY/XFF-overwrite guidance points users at this exact file.
  1280→  Recommended fix: Move the sandbox file to `sandbox/` (or gate the transform behind a build flag) and add a production-safe `deploy/` variant (TLS block, no XTransformPort, XFF overwrite only); mention the split in README Deployment.
  1281→
  1282→[P2] route.ts:149-155 + rate-limit.ts:23-25 + Dockerfile:52-61 + README docker-run — Documented production deployment runs with TRUST_PROXY unset → single GLOBAL rate-limit bucket → trivial total lockout of the only conversion endpoint
  1283→  Evidence: `if (process.env.TRUST_PROXY !== 'true') return 'anonymous'` — with TRUST_PROXY unset/false every caller shares one bucket (30 lenient / 5 strict per MINUTE, globally). The Dockerfile ENV block does not set TRUST_PROXY and the README `docker run` example passes only DATABASE_URL/N8N_* vars; TRUST_PROXY appears in no deployment doc except a comment in the root proxy file (rg confirms: that comment + .env.example:31=false).
  1284→  Impact: ~30 junk POSTs/min from one curl loop keeps the shared lenient bucket permanently exhausted → every legitimate visitor gets 429 on BOTH forms site-wide, continuously. Availability of the lead pipeline hinges on an env var the canonical deploy recipe never sets.
  1285→  Recommended fix: Set `TRUST_PROXY=true` in the Dockerfile/README docker-run example (deployment is by definition behind the overwriting proxy), or reconsider the fail-closed default for the standalone Docker target; at minimum add TRUST_PROXY to the README run command with one explanatory line.
  1286→
  1287→[P2] next.config.ts:26-38 (and 41-55) — Production CSP keeps 'unsafe-inline' in script-src; no COOP/CORP headers
  1288→  Evidence: `"script-src 'self' 'unsafe-inline'"` with the documented rationale (static prerender embeds inline bootstrap; nonce CSP incompatible). securityHeaders list has no Cross-Origin-Opener-Policy / Cross-Origin-Resource-Policy. (HSTS is emitted while the sandbox serves plain HTTP — harmless, browsers ignore it there; correct once TLS terminates at the proxy.)
  1289→  Impact: Any future HTML-injection bug escalates to full XSS with no CSP backstop (unsafe-inline voids script-src protection); missing COOP leaves the site without window.opener isolation. Current exploitability is LOW (React auto-escaping everywhere, no user-content HTML sink), so this is a hardening gap, not an active hole.
  1290→  Recommended fix: Add `Cross-Origin-Opener-Policy: same-origin` and `Cross-Origin-Resource-Policy: same-origin` now; evaluate hash-based CSP for the stable inline bootstrap chunks, or make the shell dynamic to enable nonce CSP. Keep the documented tradeoff note updated with whatever is chosen.
  1291→
  1292→[P3] route.ts:287-305 — Body-size gate depends on content-length/transfer-encoding presence; length-less h2 forwarding would bypass it
  1293→  Evidence: 413 fires only when `content-length > 64 KB` or `transfer-encoding` is present; a request with NEITHER header (possible over HTTP/2 DATA frames) skips both gates and `req.json()` buffers the full body before Zod runs.
  1294→  Impact: Unreachable in the shipped stack (Node standalone = HTTP/1.1; the fronting proxy speaks h1 to upstream with CL/chunked), but a future h2c/h2-direct exposure turns this into a memory-DoS primitive.
  1295→  Recommended fix: Document "never expose the app over h2 directly / proxy must forward length framing", or wrap the parse with a size-checked stream read (e.g. read the body via a capped reader before JSON.parse).
  1296→
  1297→[P3] git history (commits 6d367b6 → b659e9b) — .env was committed then deleted; history still contains it
  1298→  Evidence: `git log --all --name-status -- .env` → `A .env` in 6d367b6, `D .env` in b659e9b. Key names verified WITH VALUES REDACTED: it contained exactly one key, DATABASE_URL (local SQLite path — not a credential).
  1299→  Impact: Minimal today (no secret value), but the pattern is a footgun and the file remains retrievable from history if the repo goes public.
  1300→  Recommended fix: No action needed for a file path; if the repo will be public, rewrite history (git filter-repo/BFG) for hygiene and keep `.env*` ignored (already .gitignore:35).
  1301→
  1302→[P3] n8n-webhook.ts:94-99 / :179 — deliver() takes a `secret` parameter it never uses
  1303→  Evidence: `async function deliver(url, secret, body, headers)` — the body of deliver() never reads `secret`; called as `deliver(config.url, config.secret, body, headers)`.
  1304→  Impact: No vulnerability (server-side only), but it needlessly propagates the HMAC secret through a call stack that doesn't need it — the kind of parameter that later gets logged by mistake.
  1305→  Recommended fix: Drop the `secret` parameter from deliver() and its call site.
  1306→
  1307→[P3] rate-limit.ts:36-54 — Transient Map growth possible between throttled sweeps
  1308→  Evidence: `sweep()` executes at most once per 60 s (`if (now - lastSweep < SWEEP_INTERVAL_MS) return`); within that interval every NEW distinct key inserts without any size check; the 5 000-key cap only applies when the sweep body actually runs.
  1309→  Impact: Requires TRUST_PROXY=true AND direct reachability past the overwriting proxy (i.e., misdeployment) — an attacker minting fresh XFF keys can grow the Map unbounded for up to 60 s before the cap fires. Steady state is capped; realistic risk is low.
  1310→  Recommended fix: Cheap hardening — when inserting a key that didn't previously exist, also check `hits.size > 10_000` and run the eviction inline (or skip inserting the new key entirely).
  1311→
  1312→[P3] robots.ts:9 — Disallow list advertises a nonexistent /admin path
  1313→  Evidence: `disallow: ['/api', '/admin']` — no admin route exists anywhere in the app (single API route, no GET, no admin pages).
  1314→  Impact: Cosmetic recon hint that an admin surface might exist; /api disallow is likewise cosmetic (endpoint is POST-only). No real leak — sitemap contains only the 12 public locale×path URLs.
  1315→  Recommended fix: Optional — drop `/admin` until an admin surface actually exists (counter-argument: keeping it pre-blocks a future panel from indexing; either choice is defensible, just be aware it advertises the path name).
  1316→
  1317→[P3] src/components/seo/home-json-ld.tsx:14-55 — JSON-LD embedded via dangerouslySetInnerHTML without `</script>` escaping
  1318→  Evidence: `dangerouslySetInnerHTML={{ __html: JSON.stringify({...}) }}` — JSON.stringify does NOT escape `</script>`, so any value containing it would break out of the script element. All current inputs are build-time constants (SITE_URL env, SITE_CONTACT/SITE_SOCIAL literals).
  1319→  Impact: Safe as shipped (no user-controlled data reaches it), but the pattern breaks silently the day a dynamic value is added.
  1320→  Recommended fix: Wrap with `.replace(/</g, '\\u003c')` on the stringified output (standard JSON-LD hardening).
  1321→
  1322→[P3] Repo tree — Internal agent guides/prompts and a wildcard-CORS example are tracked in git
  1323→  Evidence: tracked: `upload/elyra-fullstack-prompt-01..05.md`, `upload/FULLSTACK_AGENT_GUIDE (2).md`, `upload/elyra-phase1-audit.md`, `download/README.md`, `examples/websocket/{server,frontend}.ts` (server.ts:8-11 uses `cors: { origin: "*" }`, no auth — inert example, not imported by the app, absent from the Docker runtime image). Also `tool-results/` is currently untracked but NOT ignored (same artifact class was purged before in commit 887c4dc).
  1324→  Impact: Combined with the P1 remote credential: if this repo is or ever becomes public, internal architecture/audit documentation is exposed; the example could be copy-pasted as-is into a real service.
  1325→  Recommended fix: Verify repo visibility is private; move internal docs out of the tracked tree (or a private docs repo); add `tool-results/` to .gitignore; label examples/ clearly as non-production.
  1326→
  1327→Verified-safe (no action): CSRF layering (Sec-Fetch-Site cross-site/same-site 403 + Origin/Host match + 415 content-type gate blocks form-post spam before the origin check); honeypot returns an indistinguishable fake 201 before the strict bucket (bots can't fingerprint or burn quota); server-side estimate recompute with client-echo field stripping; webhook URL is env-only (no SSRF via user input), HTTPS-enforced in production, secret-length-gated, HMAC-signed with fresh timestamp/nonce per attempt, 5 s AbortController timeout, one network-only retry, failures server-log-only and never alter the 201; Prisma usage is a single typed create (no raw SQL, no read endpoint); production Prisma logging is error-only (no PII query logs); rate limiter is synchronous on a single-threaded runtime (no sliding-window race), blocked requests don't extend the window, Retry-After computed from the oldest hit; reference IDs use crypto.randomInt (collision-proof, no row-id leak); client forms surface only translated catalog strings; API error responses never echo values, stacks, or internal paths (unknown-field NAMES are echoed as JSON keys — inert); zod 4.3.5 schemas are strict, bounded, and ReDoS-free (all regexes linear/anchored); OG/apple icons render static content on cached metadata routes (no per-request render cost); Dockerfile runs non-root with no .env in the runtime image and secrets via -e.
  1328→
  1329→VERDICT: 0 P0, 1 P1, 4 P2, 7 P3 — The leads pipeline itself is genuinely well-engineered (strict bounded validation, layered CSRF, honeypot, signed webhook, clean error hygiene); the real risks are operational: a plaintext GitHub token in the git remote, an undocumented PII retention gap, a sandbox proxy config carrying an open-proxy primitive, and a default deployment posture (TRUST_PROXY unset) that lets one trivial script globally 429 the only conversion endpoint.
  1330→
  1331→---
  1332→Task ID: L1-B
  1333→Agent: Backend/Data/i18n Audit Specialist (Group 1 — Analysis)
  1334→Task: LOOP 1 deep audit of Prisma/DB, business logic, i18n parity & catalogs, configs
  1335→
  1336→Work Log:
  1337→- Read worklog tail (last ~300 lines) for conventions: blue-palette revert is CURRENT law, i18n single-writer discipline, no concurrent tsc with dev server, parity script is the i18n gate.
  1338→- Read IN FULL: prisma/schema.prisma, src/lib/{db,calculator,seo,site-config,sound,utils,hero-scroll,api-i18n,rate-limit,lead-fields,n8n-webhook}.ts, src/i18n/{routing,request,navigation}.ts, src/proxy.ts (Next 16 middleware), src/app/api/leads/route.ts, src/components/seo/home-json-ld.tsx, src/app/{sitemap,robots}.ts, src/app/[locale]/{layout,page,opengraph-image,not-found}.tsx, src/app/not-found.tsx, src/app/[locale]/[...rest]/page.tsx, scripts/{check-i18n-parity.js,clean-leads}.ts, messages/ar.json + messages/en.json (both complete, 1291 lines each), tsconfig.json, eslint.config.mjs, package.json, next.config.ts, postcss.config.mjs, components.json, .env.example. Also read contact/page.tsx prefill contract + calculator.tsx consumption + scoped sections of navbar/footer/custom-cursor/automation-simulator/hero-console.
  1339→- Ran `node scripts/check-i18n-parity.js` (verified read-only first): → "Parity OK: 643 keys matched across ar.json and en.json / ICU placeholders verified on 582 string keys · array lengths verified on 38 array keys", exit 0.
  1340→- Ran `bunx eslint` scoped on all 19 audited lib/api/seo/i18n files → 0 errors, 0 warnings.
  1341→- Business-logic verification: exhaustively re-implemented computeEstimate in node and swept the ENTIRE input space (3 services × 20 pages × 2 languages × 2 threeD × 64 integration subsets × 2 automation levels = 30,720 combos): 0 invariant violations (min≤max, weeksMin≤weeksMax, weeksMax≥weeksMin+2 everywhere); boundary checks pages NaN/−5/999 clamp correctly; max estimate 14,600–23,400 USD / 11–20 weeks (sane).
  1342→- ICU validation with the project's own intl-messageformat: formatted all message templates in both locales — Arabic pagesValue plural renders correct zero/one/two/few/many/other forms (0→"لا صفحات", 1→"صفحة واحدة", 2→"صفحتان", 3→"3 صفحات", 11→"11 صفحة", 100→"100 صفحات"); step/weeks/successReference/rights/completed/prefill.serviceIdea all format correctly.
  1343→- Zod 4.3.5 runtime probe: confirmed `unrecognized_keys` issues carry `keys: string[]` + empty path (fieldErrors handles them) and discriminated-union missing-discriminator issues carry path ['source'] → maps to fields.source (matches audit-1b live observation).
  1344→- Catalog deep scans (read-only node scripts): 42 identical ar/en values — ALL legitimate (numbers, contact values, brand names, CRM acronym, 2 empty numeric suffixes, intentional cursor.magnet ""); zero Arabic text inside en.json except by-design nav.switchLocale="عربي"; zero untranslated English sentences in ar.json; zero unbalanced ICU braces; max EN/AR length ratio 2.36 (below the 3× layout-risk threshold).
  1345→- Key-usage cross-reference (namespace-aware t() literal scan across src/): all consumed keys resolve in both catalogs; found 8 never-referenced catalog keys (P3 below). Verified hero/manifesto/nav/footer/contact-form/services/work/about/common-cursor/metadata namespaces line-by-line against their consuming components.
  1346→- Verified reference generator stats: 36^9 space, 200k draws → 0 collisions; format ^c[0-9a-z]{9}$.
  1347→- DB usage map: only db.lead.create (route) + standalone scripts/clean-leads.ts (dry-run supported); no app read paths. prisma/ has NO migrations dir (db push workflow; db:migrate/db:reset scripts exist but would baseline fresh).
  1348→- Verified palette revert completeness via grep: --primary #0071E3 / --g-blue #4285F4 confirmed current; exactly 3 amber remnants found (opengraph-image.tsx:44, not-found.tsx:48,60) + 2 stale "no blue" palette-law comments.
  1349→- Verified proxy.ts matcher semantics (/opengraph-image, /apple-icon, /en/* all resolve; /api excluded from intl middleware); sitemap's 12 URLs (6 paths × 2 locales) all exist as routes; fixed LAST_MODIFIED 2026-08-26 is not future-dated (today 2026-08-27).
  1350→- Verified sound.ts SSR-safety (no module-level window access, lazy AudioContext on gesture, onended teardown, storage-event cross-tab sync), hero-scroll.ts bind/unbind identity guard, api-i18n.ts safe key-fallback.
  1351→- No `any` leaks in src/lib/** or src/app/api/** (grep-verified).
  1352→
  1353→Stage Summary:
  1354→FINDINGS:
  1355→
  1356→[P2] src/app/api/leads/route.ts:430 + prisma/schema.prisma — Customer reference is never persisted
  1357→  Evidence: `const reference = randomReference()` is generated AFTER `db.lead.create(...)`, included only in the 201 response + webhook payload; the Lead model has no `reference` column (only id/name/email/whatsapp/message/service/pages/... — schema.prisma:22-46). The success UI shows "رقمك المرجعي: {reference}" (calculator.form.successReference), and .env.example ships with N8N_WEBHOOK_URL empty (webhook disabled by default).
  1358→  Impact: The reference shown to every customer is untraceable — the team cannot map "c5tr8p13xb" to a stored row (CRM lookup fails; with the webhook disabled the reference exists NOWHERE except the user's toast). Support/repeat-contact workflows break.
  1359→  Recommended fix: Add `reference String @unique` to the Lead model; generate the reference BEFORE `db.lead.create` and include it in `data` (keep the 'c'+9 base36 format); optionally catch P2002 unique violation and regenerate (odds ~1e-14, one retry is plenty). While touching the schema, document the `service:'contact'` sentinel (route.ts:246) in a schema comment.
  1360→
  1361→[P2] src/app/[locale]/opengraph-image.tsx:11-13,44 + src/app/not-found.tsx:48,60 — Blue-palette revert leftovers: amber remnants + comments asserting the abolished "no blue" law
  1362→  Evidence: opengraph-image.tsx:11-12 comment claims "quad-dot brand language (amber/gold/red/green — palette law: no blue)" while :80/:156 draw #4285F4 blue dots and :44 still has the amber-era radial `rgba(245, 158, 11, 0.35)`; root not-found.tsx:48 chip hover border `rgba(245,158,11,.5)` + :60 radial `rgba(245,158,11,0.16)` sit next to the blue CTA #0071E3 (:125) and focus ring #4285F4 (:49). Current global law is blue (--primary #0071E3, --g-blue #4285F4 — globals.css:71,86; user round R1-R6 reverted the amber palette).
  1363→  Impact: The social-shared OG card and the last-resort 404 mix amber glow/hover with blue brand accents (off-brand vs the restored identity); the stale "palette law: no blue" comments actively mislead the next editor into "fixing" the blue dots back to amber.
  1364→  Recommended fix: Decide the accent once: either swap the three rgba(245,158,11,*) to the blue family (e.g. rgba(0,113,227,0.35)/.5/.16) for full consistency, or keep a deliberate warm secondary — then update BOTH comments to state the actual current decision. (1-line CSS + 2 comment edits per file.)
  1365→
  1366→[P3] messages/ar.json + en.json — 8 dead catalog keys (present in both locales, consumed by nothing)
  1367→  Evidence: namespace-aware t()-literal scan + direct grep across src/ shows zero references to: `nav.services` (navbar consumes websites/automation/work/about/contact/home/cta/ariaLabel/openMenu/closeMenu only), `common.back`, `common.next` (calculator uses its own calculator.back/next), `common.close`, `common.getStarted`, `common.explore`, `common.reduceMotionNote`, `hero.canvasFallback`.
  1368→  Impact: Catalog noise; copy edits to never-rendered strings waste review time; inflates the parity count (643 vs 635 live keys).
  1369→  Recommended fix: Delete the 8 keys from BOTH catalogs in one edit (parity stays green), or wire them if a surface was planned (e.g. common.reduceMotionNote for a reduced-motion notice).
  1370→
  1371→[P3] scripts/check-i18n-parity.js:1-111 — Parity gate has three blind spots
  1372→  Evidence: The script verifies presence/type/array-length/placeholder-set only. It cannot distinguish intentional empties from accidents (`common.cursor.magnet` = "" in both files passes silently — intentional per custom-cursor.tsx:42, but nothing encodes that), never flags ar==en values ( untranslated-copy risk — currently all 42 identical values are legitimate numbers/brand strings), and has no usage-vs-catalog check (the 8 dead keys above exist today).
  1373→  Impact: False confidence — an accidentally blanked or untranslated string ships silently.
  1374→  Recommended fix: Extend the script: (a) empty-string check with allowlist ['common.cursor.magnet'], (b) warn-list of identical ar/en string values, (c) optional unused-key scan (same t()-literal heuristic used in this audit).
  1375→
  1376→[P3] src/lib/n8n-webhook.ts:94-99 — `deliver()` declares an unused `secret` parameter
  1377→  Evidence: `async function deliver(url: string, secret: string, body: string, headers: ...)` — `secret` is never read inside the function body (signing happens in attemptDelivery); caller passes config.secret (:179).
  1378→  Impact: Reader confusion — the signature implies the transport layer authenticates with the secret, which it does not. (Passes lint only because of after-used arg semantics.)
  1379→  Recommended fix: Drop the parameter (and the argument at :179) or rename to `_secret`.
  1380→
  1381→[P3] src/lib/seo.ts:50 vs src/app/[locale]/layout.tsx:88 — Inconsistent OG image alt text
  1382→  Evidence: seo.ts declares alt "Elyra — Stunning Websites · n8n Automation · Digital Studio"; layout.tsx:88 and [locale]/page.tsx:41 declare "Elyra — Stunning Websites & n8n Automation Systems" for the SAME image.
  1383→  Impact: Scrapers see two different alt texts for one asset; maintenance drift.
  1384→  Recommended fix: Export a single OG_ALT constant (e.g. from site-config.ts) and use it in all three places.
  1385→
  1386→[P3] src/app/[locale]/layout.tsx:98-100 — SITE_URL re-derived instead of reusing the import
  1387→  Evidence: Line 8 imports SITE_URL from '@/lib/seo' (used for ogImage), but metadataBase is built from a duplicate expression `new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000')`.
  1388→  Impact: Two sources of truth for the same value (drift risk if the fallback ever changes in seo.ts).
  1389→  Recommended fix: `metadataBase: new URL(SITE_URL)`.
  1390→
  1391→[P3] src/app/not-found.tsx:135-138 — Stale comment: "the proxy locale-detects and redirects"
  1392→  Evidence: Comment says plain <a> hrefs work because "the proxy locale-detects and redirects on the subsequent full navigation" — but src/i18n/routing.ts:10 sets `localeDetection: false` (deterministic; /work serves the default Arabic page directly, no detection, no redirect).
  1393→  Impact: Future maintainers may build locale-sensitive behavior on a false invariant.
  1394→  Recommended fix: Reword to "paths serve the default Arabic locale (localeDetection is disabled — see routing.ts)".
  1395→
  1396→[P3] package.json:5-15 — No typecheck script
  1397→  Evidence: scripts = dev/build/start/lint/db:*; the worklog shows every agent manually runs `bunx tsc --noEmit` as the actual type gate.
  1398→  Impact: The project's primary safety gate is tribal knowledge, not a script (CI- unfriendly).
  1399→  Recommended fix: Add `"typecheck": "tsc --noEmit"`.
  1400→
  1401→[P3] components.json:18 — shadcn `hooks` alias points to a nonexistent directory
  1402→  Evidence: `"hooks": "@/hooks"` while src/hooks/ does not exist (LS-verified).
  1403→  Impact: A future `bunx shadcn@latest add` that installs a hook would fail until the dir is created.
  1404→  Recommended fix: Create src/hooks/ (empty) or point the alias at an existing dir.
  1405→
  1406→[P3] src/app/api/leads/route.ts:386-426 — Strict quota is burned before a write that may still fail
  1407→  Evidence: Step 6 checks+records the 5/min strict bucket, THEN `db.lead.create` runs inside try/catch — a storage failure (500) still consumed a strict slot.
  1408→  Impact: A user whose lead failed to persist loses one of only 5 submissions/min through no fault of their own (rare: SQLite write failures are exceptional).
  1409→  Recommended fix: Acceptable as-is (simplicity); if desired, on catch remove the just-recorded strict timestamp or move the burn after a successful create with a pre-check for early 429.
  1410→
  1411→[P3] src/i18n/navigation.ts:4 — `redirect` and `getPathname` exports are unused
  1412→  Evidence: createNavigation destructures 5 exports; grep shows only Link/usePathname/useRouter consumed anywhere in src/.
  1413→  Impact: None (standard boilerplate surface); noted for the dead-code ledger.
  1414→  Recommended fix: Keep (next-intl convention) or trim to the three used exports.
  1415→
  1416→VERIFIED-CLEAN (for the record — checked and NOT broken): db.ts globalThis singleton (hot-reload-safe, correct Prisma pattern; dev 'query' logging acknowledged as dev-only diagnostic with PII note); route error handling (500 generic + translated message, details only to server log, webhook strictly after commit and failure-isolated); rate-limit sliding window + sweep + 5k cap; honeypot fake-201 indistinguishability; CSV-injection neutralizer + per-attempt HMAC re-signing; chunked-transfer 413 gate; calculator rounding invariant (breakdown lines sum to headline, verified exhaustively); formatMoney ar/en outputs ("‏5,200 US$" / "$5,200" — Latin digits by design); sitemap ↔ routes ↔ hreflang ↔ canonical consistency (12 URLs, as-needed prefixes, x-default→ar); JSON-LD validity (Organization+WebSite @graph, no phantom SearchAction); api-i18n safe fallback + compile-time shape check on ar.apiErrors; sound.ts/hero-scroll.ts/utils.ts SSR-safety & teardown; tsconfig strict+noUncheckedIndexedAccess+noImplicitOverride; dependency alignment (next 16.1.1 / react 19 / next-intl 4.3.4 / eslint-config-next 16.1.1 / zod 4.3.5 / react-compiler plugin wired); next.config standalone+reactCompiler+CSP split dev/prod; proxy matcher coverage for icon/OG/apple-icon routes; i18n catalogs: full key-tree parity, valid ICU in both locales, zero language leakage, zero accidental empties, no 3× truncation-risk strings.
  1417→
  1418→VERDICT: 0 P0, 0 P1, 2 P2, 10 P3 — Backend, data and i18n core is production-grade; the only real gaps are the unpersisted customer reference (data-model completeness) and 3 amber palette remnants + stale comments from the blue revert, everything else is polish.
  1419→
  1420→---
  1421→Task ID: L1-D
  1422→Agent: WebGL/Sensory/Layout/Hooks Audit Specialist (Group 1 — Analysis)
  1423→Task: LOOP 1 deep audit of WebGL scenes, sensory layer, layout/shared components, hooks, globals.css
  1424→
  1425→Work Log:
  1426→- Read worklog tail (R7 lessons: GLSL1 attribute/varying convention, eslint-disable react-hooks/immutability on uniform mutation, no concurrent tsc with dev server, single-browser-session, .elyra-spotlight content-lift / data-bg-layer exemption, z-ladder 80/90/100/200).
  1427→- Read EVERY scope file in full: hero-canvas.tsx (599 ln, TS + all 5 GLSL blocks), capability-scene.tsx (604), console-scene.tsx (434), three-d-section.tsx, custom-cursor.tsx, grain-overlay.tsx, sound-toggle.tsx, navbar.tsx, footer.tsx, language-switcher.tsx, live-clock.tsx, scroll-progress.tsx, logo.tsx, cta.tsx, page-hero.tsx, reveal.tsx, section-heading.tsx, service-prose.tsx, home-json-ld.tsx, all 7 use-*/lib hooks (use-reduced-motion, use-cursor-velocity, use-magnetic, use-near-viewport, use-rtl, hero-scroll, sound), all 7 shadcn ui files, globals.css (all 1213 lines). Plus consumers: hero.tsx, hero-console.tsx (full), [locale]/layout.tsx, methodology.tsx (bindHeroScroll wiring), testimonials.tsx (spotlight interplay), site-config.ts.
  1428→- Ran structural GLSL check via node (/tmp): delimiter balance + GLSL1-only (no #version/in/out/texture()) PASS on all 4 hero-canvas shaders; JS uniform objects match GLSL declarations EXACTLY (Particles: uTime float/uMouse vec3(Vector3)/uPixelRatio float/uScroll float; Silk: uTime/uMouse vec2(Vector2)/uColorA-C vec3(Color)/uScroll); fbm call sites = 3 (budget respected; 5 octaves).
  1429→- Verified R3F v9.7 internals in node_modules: commitUpdate reconstructs primitives when the `object` prop changes (console-scene tube swap on preset change is SAFE without key changes); removeChild never disposes primitives (manual tube disposal is required + correct); events hijack BOTH e.target and e.currentTarget with {hasPointerCapture,setPointerCapture,releasePointerCapture} — decisive for P1-1 below; captured pointers keep receiving move events off-object (capability drag works); Canvas has an internal ErrorBoundary for children but WebGL-creation failure rejects an un-awaited async configure() (silent blank, not a route crash).
  1430→- Scoped `bunx eslint` on all 33 scope files → 0 errors, 0 warnings (two batches). i18n parity script → 643/643 keys; common.cursor.* (magnet/rotate/preview/drag/zoom/inspect/external) present in ar+en with magnet="" by design.
  1431→- Grep verification: dead CSS classes (elyra-grain, elyra-cursor base/chip/--visible, glass-light, blend-difference, prose-ar-max) have ZERO TSX references; amber rgba(245,158,11)/rgba(217,119,6) remnants found in 6 files; sonner 2.0.7 toaster z-index 999999999 (above cursor 200 — coherent ladder: sheet/nav 50 < progress 60 < intro 80 < grain 90 < skip 100 < cursor 200 < toasts); body > header print selector valid (providers render no DOM); footer.localTime = "بتوقيتك المحلي" (visitor-tz semantics correct).
  1432→
  1433→Stage Summary:
  1434→FINDINGS:
  1435→
  1436→[P1] capability-scene.tsx:462 — Mouse parallax can never activate (R3F hijacks e.currentTarget)
  1437→  Evidence: `const rect = (e.currentTarget as HTMLElement).getBoundingClientRect?.()` … `if (rect) { mouse.current.tx = … }` — in @react-three/fiber v9 the raycast event overrides BOTH target and currentTarget with `{ hasPointerCapture, setPointerCapture, releasePointerCapture }` (verified in node_modules/@react-three/fiber/dist/events-156d8d12.esm.js:765-773); that object has NO getBoundingClientRect, so `rect` is always undefined and the only writes to mouse.current.tx/ty (lines 464-465) never execute. The useFrame parallax (lines 416-421) then lerps toward (0,0) forever.
  1438→  Impact: The documented "mouse parallax lerp (now on a parent group so blob+satellites+halo drift together)" is a dead feature — the scene never responds to hover position (only auto-spin + drag still work).
  1439→  Recommended fix: Drop manual rect math entirely — read the R3F-maintained NDC pointer inside useFrame: `const { gl, pointer } = state; parallax.current.position.x += (pointer.x * viewport.width / 4 - parallax.current.position.x) * 0.04` (state.pointer is updated by R3F's event pass; no listener needed). Delete lines 462-466 and the mouse ref.
  1440→
  1441→[P2] page-hero.tsx:37 + hero-console.tsx:307/315/316 (+ cross-scope: opengraph-image.tsx:44, not-found.tsx:48/60, automation-simulator.tsx:441/511/544/553/743, bento.tsx:418/447/600) — Amber palette remnants survive the blue revert
  1442→  Evidence: page-hero: `radial-gradient(60% 60% at 50% 0%, rgba(245,158,11,0.18), transparent 70%)` (amber behind EVERY inner-page hero); hero-console SVG fallback (mobile + reduced-motion!): `stroke="rgba(217,119,6,0.3)"`, `fill="rgba(217,119,6,0.15)"`; opengraph-image: `rgba(245, 158, 11, 0.35)` on the SOCIAL SHARE image; simulator/bento: five rgba(217,119,6,…) glow shadows. R1-R6 claimed "zero amber hex left" — the rgba() forms were missed. (L1-C independently found 3 of these; my sweep adds hero-console + simulator + bento = 16 instances in 6 files.)
  1443→  Impact: Off-palette amber glows/nodes against the restored blue brand — visible on all inner-page heroes, the mobile hero-console diagram, the simulator/bento glows, the root 404, and externally on og:image social cards.
  1444→  Recommended fix: Swap rgba(245,158,11,x)→rgba(66,133,244,x) (#4285F4) and rgba(217,119,6,x)→rgba(0,113,227,x) (#0071E3) in all 16 spots (keep the green #34A853 counterpoints, e.g. hero-console center node).
  1445→
  1446→[P2] globals.css:750-827 (+170-175, 204-207, 653-656, 835-841) — ~95 lines of dead CSS from the unmounted Phase-2 sensory layer + unused utilities
  1447→  Evidence: Grep across src/ returns ZERO TSX references for `.elyra-grain` (750), `.elyra-cursor` base (764), `.elyra-cursor-chip` (792), `.elyra-cursor--visible` (813), `.glass-light` (170), `.blend-difference` (204), `.prose-ar-max` (654); the print block still lists `.elyra-grain/.elyra-cursor/.elyra-cursor-chip` (836-838); the `cursor:none` rule at 820-827 duplicates R7-b's 1182-1187. The Phase-2 `.elyra-cursor-dot/-ring` rules (775-789) survive only because R7-b re-declares the same class names with explicit resets (1087-1100) — fragile shared-name coupling.
  1448→  Impact: ~8% of the stylesheet is inert; every future cursor tweak must keep neutralizing rules nobody remembers. R7 deleted film-grain.tsx/magnetic-cursor.tsx but left their styles.
  1449→  Recommended fix: Delete lines 750-827 entirely (keep 820-827's reduced-motion nesting only if you want belt-and-braces, else rely on the R7-b !important rule), the three dead print selectors, and `.glass-light`/`.blend-difference`/`.prose-ar-max` blocks.
  1450→
  1451→[P2] console-scene.tsx:16-17 + hero-console.tsx:265-266 — "Drag to rotate" affordance is false
  1452→  Evidence: Header comment: "Drag to rotate (clamped). No wheel zoom / no scroll hijack (§4)." — but console-scene.tsx contains NO pointer handlers anywhere (verified: git log -S "onPointerDown" on the file returns nothing — drag never existed); the only motion is the useFrame rotation drift (lines 274-279). Meanwhile hero-console renders `data-cursor="rotate"` + label chip and the Canvas has `cursor: 'grab'` (line 423).
  1453→  Impact: Users are told "اسحب للتدوير 360°" and see a grab cursor, attempt to drag, and nothing responds — a broken promise on the hero's conversion payoff scene.
  1454→  Recommended fix: Either add the interaction (drag delta on the group with clamped rotation, mirroring capability-scene's throttled pattern) or fix the affordance: drop data-cursor="rotate" + change the chip label + remove `cursor: 'grab'`, and reword the header comment.
  1455→
  1456→[P2] hero-canvas.tsx:39 + 583-598 — No mobile tier: 4500 curl-noise particles + 15-vnoise silk fragment ship to phones
  1457→  Evidence: `const PARTICLE_COUNT = 4500` (fixed); SILK_FRAGMENT runs 3× fbm×5 octaves = 15 value-noise samples (~60 sin) per pixel; hero.tsx gates only on `!reduced && load3D` — no viewport/mobile matchMedia (unlike hero-console, which replaces WebGL with SVG below 768px); the only mobile concession is `dpr={[1,2]}`.
  1458→  Impact: Low-end phones render ~1.3M px × heavy fragment math + 4500-particle vertex curl (6 snoise/vertex) at up to 60fps — jank/battery risk; prior rounds never reduced the count.
  1459→  Recommended fix: Add a coarse-pointer or <768px tier: PARTICLE_COUNT 4500→1800 and dpr [1,1.5] (or a uniform-driven fbm octave drop to 4 is harder — count+dpr is enough); alternatively reuse hero-console's `matchMedia('(max-width: 767px)')` to skip the silk plane on phones (particles alone read fine over the CSS fallback).
  1460→
  1461→[P3] live-clock.tsx:21-27 + 58-63 — English users see the timezone twice
  1462→  Evidence: `new Intl.DateTimeFormat(…, { …, timeZoneName: 'short' })` already yields "15:16 GMT+3" (verified via node), then the component appends the manually built `{tzLabel}` → "15:16 GMT+3 (GMT+3)". Arabic shows "١٥:١٦ غ (GMT+3)" (no visual dup, cryptic abbreviation).
  1463→  Impact: Duplicated tz string in EN footer; mildly confusing.
  1464→  Recommended fix: Remove `timeZoneName: 'short'` from the formatter (keep the explicit GMT±N label — it's locale-stable), or drop tzLabel and keep the Intl one.
  1465→
  1466→[P3] logo.tsx:11-13 — Stale palette comment contradicts the code
  1467→  Evidence: "a 4-color brand dot in the amber/green family (palette law: no blue)" — the circles below are #4285F4/#EA4335/#FBBC05/#34A853 (the Google quad, correct after the blue revert).
  1468→  Impact: Misleads the next palette sweep.
  1469→  Recommended fix: Reword to "Google-brand quad-dot (#4285F4/#EA4335/#FBBC05/#34A853) — intentionally outside the token system".
  1470→
  1471→[P3] console-scene.tsx:404-434 + hero-console.tsx:197 — WebGL guard checks API presence, not context creation
  1472→  Evidence: `!('WebGLRenderingContext' in window)` gates the SVG fallback; hero-canvas and capability-scene both do full `createElement('canvas').getContext('webgl2' ?? 'webgl')` creation probes. If the API exists but creation fails (driver blocklist), R3F v9's configure() rejects an un-awaited async promise (verified in fiber dist) → silent 280px empty box, no SVG fallback, no crash.
  1473→  Impact: Rare edge case (blocked GPUs / hardened browsers) renders an empty hero-console scene.
  1474→  Recommended fix: Reuse the same rAF-deferred context-creation probe in ConsoleScene (return null → let the parent fall through to the SVG diagram), or lift the probe result into hero-console's showSvg condition.
  1475→
  1476→[P3] sheet.tsx:73-79 + navbar.tsx:124-196 — Mobile sheet has no Description/aria-describedby
  1477→  Evidence: SheetDescription is exported but never used; the navbar sheet passes none, and the stock built-in close was removed (documented LOW-10). Radix Dialog logs a dev warning for dialogs without Description or `aria-describedby={undefined}`.
  1478→  Impact: Dev-console noise; minor a11y (dialog purpose not described to SR).
  1479→  Recommended fix: Pass `aria-describedby={undefined}` on SheetContent in navbar (one-line), or add a visually-hidden SheetDescription with the nav summary.
  1480→
  1481→[P3] footer.tsx:154/166/178/190/202 — Social icon buttons are 36px touch targets
  1482→  Evidence: `size-9` (36px) on all 5 social anchors; the Batch-1 item-4 44px pass covered the navbar only (size-11), footer was out of its scope.
  1483→  Impact: Below WCAG 2.5.8 (24px minimum, AA) — passes the minimum, but below the 44px target the project standardized on for navbar/CTAs (G1-c finding class).
  1484→  Recommended fix: size-9→size-11 with compensating gap-3→gap-2.5, or wrap in a hit-area expander.
  1485→
  1486→[P3] hero-canvas.tsx:492 — Silk plane edges visible on super-ultrawide viewports
  1487→  Evidence: `planeGeometry args={[64, 32]}` (±32 wide) at z=-9 with fov 70/z=6 covers visible width 21×aspect → fully covered up to aspect ≈ 3.05; 32:9 monitors (3.56) / 5120×1440 show uncovered side bands (the additive layer just ends).
  1488→  Impact: Cosmetic seam on extreme aspect ratios only.
  1489→  Recommended fix: Widen to args={[80, 32]} (cost-free — one fullscreen quad regardless) or clamp via the existing vignette.
  1490→
  1491→[P3] three-d-section.tsx:70-72 — aria-label on a role-less div + non-keyboard drag
  1492→  Evidence: `aria-label={t('hint')}` sits on a plain div (ignored by most AT without a role); the capability scene's drag interaction has no keyboard equivalent and the visible hint text duplicates the label.
  1493→  Impact: Minor a11y semantics gap on the 3D showcase.
  1494→  Recommended fix: `role="img"` + keep the visible hint as the accessible description (drop aria-label), or add tabIndex={0} + arrow-key rotation (10 lines, mirrors simulator stage pattern).
  1495→
  1496→VERIFIED-CLEAN (for the record — checked and NOT broken): hero-canvas GLSL (GLSL1-only, uniform sets match exactly, 3×fbm budget, CustomBlending ONE,ONE/ZERO,ONE preserves the CSS stage, aspect-correct mouse projection, scroll-dolly via state.camera, context-loss guard, full disposal via JSX, rAF-deferred WebGL probe); hero visibility pause lives correctly in hero.tsx (IO + visibilitychange threaded into active); hero-console's 800ms payoff timer is unmount-safe and double-submit-guarded; console-scene primitive tube swap is SAFE in R3F v9 (commitUpdate reconstructs on object change — index keys fine) and its manual disposal is required + correct (primitives are never auto-disposed), StrictMode double-dispose semantics documented; capability-scene drag works off-object (R3F captured-pointer dispatch verified) and pointer capture uses R3F's hijacked target.setPointerCapture correctly; custom-cursor (single rAF loop with proper teardown, delegation + relatedTarget check, all 7 label keys present in both catalogs, is-hidden initial, touch kill-switch, elyra-cursor-active class cleanup, z-200 under toasts); grain-overlay truly server-rendered, %23-encoded data-URI valid, reduced-motion collapses to the base 0 0 keyframe; sound engine (gesture-gated AudioContext, storage-backed uSES with stable primitive snapshots, node teardown via onended, pointer-only delegation with cleanup); all 7 hooks (uSES patterns canonical — module MQL singletons, primitive snapshots, cleanup in every branch; use-magnetic's transition suppression/settle/restore logic correct incl. capture-flag-matched scroll listener; Parallax IO arm/disarm; useNearViewport one-shot disconnect); language-switcher scroll restore (StrictMode-safe consume-inside-timeout + 5s expiry); navbar sheet a11y (aria-expanded, Radix focus/Escape, SheetClose-wrapped links); footer (wa.me number format correct, rel noopener everywhere, mt-auto sticky-footer with body flex-col); home-json-ld (@graph Organization+WebSite valid, home-only); all 7 shadcn files stock + the one documented sheet modification; globals.css z-ladder coherent incl. sonner 999999999, spotlight data-bg-layer exemptions present on all 4 hero decorative absolutes, @theme/inline TW4 syntax correct, universal :lang(ar) letter-spacing reset sound, print `body > header` matches (providers render no DOM).
  1497→
  1498→VERDICT: 0 P0, 1 P1, 4 P2, 7 P3 — The WebGL/sensory/hooks architecture is genuinely production-grade (every disposal/rAF/uSES discipline verified clean); the real work is one dead parallax feature (R3F event-hijack gotcha), finishing the blue-revert sweep (16 amber rgba remnants in 6 files), deleting ~95 lines of Phase-2 dead CSS, and the false drag affordance on the console scene.
  1499→
  1500→---
  1501→Task ID: L1-C
  1502→Agent: App Pages & Home Components Audit Specialist (Group 1 — Analysis)
  1503→Task: LOOP 1 deep audit of app-router pages, home sections, page components
  1504→
  1505→Work Log:
  1506→- Read worklog tail (R7 + R1-R6 blue-revert + 3-convergence + I1-I4 entries) for conventions: data-bg-layer exemption from .elyra-spotlight's content-lift rule, flex-col parents for mask-reveal wrappers, Math.random only in lazy useState, blue palette is the CURRENT mandate (#0071E3/#4285F4 family), 44px touch targets scoped to navbar by decision.
  1507→- Read ALL scoped files completely: pages layer ([locale] layout/page/error/not-found, global-error, root not-found, [...rest], about/contact/services-websites/services-automation/work pages + all 5 loading.tsx), all 19 home components (hero, hero-console, intro-overlay, kinetic-heading, manifesto, trust-bar, bento, featured-work, testimonials, before-after, deconstructed-card, calculator(+lazy), automation-simulator(+lazy), console-scene, ring-gauge, methodology(+lazy), three-d-section), pages components (contact-form, work-grid).
  1508→- Cross-verified supporting infra: globals.css (universal :lang(ar) letter-spacing reset :649, reduced-motion kill-switch :246, spotlight lifting rule :722, hero-marquee/hr-word/intro/manifesto CSS), messages/ar.json+en.json (stats/about numbers values are real Numbers — no NaN risk; hero.console/workSection.scenes/simulator/bento keys all present; parity script → "Parity OK: 643 keys"), shared components (section-heading, reveal/parallax, page-hero), lib hooks (use-rtl, use-near-viewport, use-reduced-motion, use-cursor-velocity, use-magnetic, sound, calculator, seo, site-config), sitemap.ts, proxy.ts, eslint.config.mjs.
  1509→- Verified with Grep across src/: amber-remnant sweep (217,119,6/#b45309/245,158,11 found in 5 files + page-hero), Math.random/Date.now/new Date confined to effects/callbacks/lazy-initializers in scoped files, physical-direction utilities (only deliberate ltr:right/rtl:left hero rail + documented honeypot -left-[9999px]), --color-elyra-deep token existence (absent — only hand-written .bg-elyra-deep class), ContextLossGuard presence per WebGL scene (hero-canvas ✓, capability-scene ✓, console-scene ✗), testimonial Parallax is NOT a direct spotlight child (no R7 lifting-rule bug).
  1510→- Ran scoped `bunx eslint` on all 33 scoped files in 4 batches → 0 errors, 0 warnings. Ran read-only parity script (643/643). No tsc/build/browser/DB writes (analyst-only contract).
  1511→
  1512→Stage Summary:
  1513→FINDINGS:
  1514→[P1] hero-console.tsx:307,315,316 + automation-simulator.tsx:441,511,544,553,743 + bento.tsx:418,447,600 + before-after.tsx:938 + shared/page-hero.tsx:37 — Incomplete blue-palette revert: 13 amber remnants survived the R1-R6 sweep
  1515→  Evidence: hero-console ConsoleSvgDiagram `stroke="rgba(217,119,6,0.3)"` / `fill="rgba(217,119,6,0.15)"` / `stroke="rgba(217,119,6,0.6)"`; simulator `edgeDone ? 'rgba(217,119,6,0.45)'`, `shadow-[0_0_12px_rgba(217,119,6,0.95)]`, `shadow-[0_0_28px_rgba(217,119,6,0.55)]`, `animate={{ boxShadow: ['0 0 0 0 rgba(217,119,6,0.5)'…` , `shadow-[0_0_32px_rgba(217,119,6,0.28)]`; bento `boxShadow: '0 0 0 4px rgba(217,119,6,0.35)'`, `shadow-[0_0_6px_rgba(217,119,6,0.9)]`, `radial-gradient(130px 90px … rgba(217,119,6,0.28)`; before-after `boxShadow: 'inset 0 0 0 1.5px #b45309'`; page-hero `radial-gradient(60% 60% at 50% 0%, rgba(245,158,11,0.18)…)`.
  1516→  Impact: WORST at hero-console: `showSvg = mounted && (isMobile || reduced || !('WebGLRenderingContext' in window))` means EVERY phone (<768px) and every reduced-motion user who submits a hero prompt sees an AMBER node diagram, while desktop users see the blue WebGL constellation (console-scene PRESET colors were reverted). Simulator shows blue `border-primary` nodes with amber glow shadows on the same element (home + /services/automation). All 5 inner-page heroes carry a faint amber top glow stacked over the blue .hero-fallback. Violates the user's explicit blue-revert directive; prior convergence round classed palette integrity as top severity.
  1517→  Recommended fix: one sweep restoring the pre-amber values — hero-console SVG → rgba(0,113,227,0.3/0.15/0.6); simulator glows → rgba(0,113,227,…) family (match elyra-edge #4285F4 stops); bento MiniFlow/MiniCube glows → rgba(0,113,227,…); before-after cell ring #b45309 → #1d4ed8 (pre-amber value); page-hero radial → rgba(0,113,227,0.18). Then re-grep `217,119,6|b45309|245,158,11` in src/ → must be zero.
  1518→[P2] bento.tsx:69 + messages/ar.json:139-143 + messages/en.json:139-143 — MiniSite 4th theme swatch caption mislabels its color in BOTH locales
  1519→  Evidence: `const SITE_PALETTE = ['#0071E3', '#34A853', '#EA4335', '#4285F4']` vs palette names `["كحلي","زمردي","مرجاني","عنبري"]` / `["Cobalt","Emerald","Coral","Amber"]` — swatch 4 is #4285F4 (blue) but reads "عنبري"/"Amber".
  1520→  Impact: clicking the 4th swatch turns the whole mini-storefront blue while the live caption ("الهوية الحالية: عنبري") claims Amber — factually wrong UI copy in ar+en; the caption drifted during the amber phase and the hex was reverted without re-aligning names (swatches 1-3 happen to be valid again).
  1521→  Recommended fix: rename palette[3] → AR "سماوي" / EN "Sky" (keep #4285F4), both catalogs; optionally also re-check swatch-1 name ("كحلي"/"Cobalt" is fine for #0071E3).
  1522→[P2] console-scene.tsx (whole file; mount path hero-console.tsx:197,262) — no WebGL context-creation-failure / context-loss guard, unlike the other two canvases
  1523→  Evidence: hero-console gates only on `!('WebGLRenderingContext' in window)` (API presence, not creation success); console-scene's Canvas has no ContextLossGuard and no feature-detection fallback — grep confirms ContextLossGuard exists in hero-canvas.tsx:531 and capability-scene.tsx:541 but NOT in console-scene.tsx.
  1524→  Impact: on a GPU-blocklisted/driver-crashed browser that still exposes WebGLRenderingContext, THREE.WebGLRenderer throws during R3F render → the nearest boundary ([locale]/error.tsx) replaces the ENTIRE page for a decorative element. A mid-session context loss would similarly freeze/throw inside the hero. Edge-case blast radius is disproportionate.
  1525→  Recommended fix: mirror hero-canvas's pattern — probe `document.createElement('canvas').getContext('webgl2') ?? …('webgl')` in the showSvg condition (or a tiny useHasWebGL hook) and add the ContextLossGuard component to the Canvas children.
  1526→[P3] hero.tsx:66 — `focus-visible:ring-offset-elyra-deep` is an unresolvable Tailwind utility (silent no-op)
  1527→  Evidence: `className="… focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-elyra-deep"` on OrbitBadge; @theme inline defines --color-elyra-dark/--color-background-deep but NO --color-elyra-deep (only the hand-written `.bg-elyra-deep` class at globals.css:685 exists, which Tailwind utilities don't derive from).
  1528→  Impact: the class never compiles → the badge's focus-ring offset keeps the default color (white-ish) on the #08080A hero, producing a wrong-colored halo around the focus ring. Ring itself still visible (not a blocker).
  1529→  Recommended fix: either add `--color-elyra-deep: var(--background-deep);` to @theme inline, or change the class to `focus-visible:ring-offset-elyra-dark` (matches the hero CTA pattern used 20 lines below).
  1530→[P3] hero-console.tsx:263-266 — dead cursor affordance on the scene label chip
  1531→  Evidence: the chip carries `data-cursor="rotate" data-cursor-label={t('placeholder')}` but also `pointer-events-none`, so pointerover can never target it (custom-cursor matches via closest() from e.target).
  1532→  Impact: the "rotate" cursor label promised over the console scene never activates; attribute pair is dead code.
  1533→  Recommended fix: move data-cursor/data-cursor-label onto the hit-testable container (#hero-console-scene div) or drop the attributes.
  1534→[P3] calculator.tsx:189 — aria-label + aria-labelledby conflict on the calculator section
  1535→  Evidence: `<section id="calculator" … aria-labelledby="calc-title" aria-label={t('kicker')}>` — when both are present aria-label wins.
  1536→  Impact: the landmark's accessible name becomes the short kicker instead of the full h2 title; the labelledby wiring is inert.
  1537→  Recommended fix: delete the `aria-label={t('kicker')}` attribute (keep labelledby).
  1538→[P3] contact/page.tsx:97 — section landmark labelled "channels" also contains the form + social proof
  1539→  Evidence: `<section className="bg-background py-20…" aria-labelledby="channels-title">` wraps the 2-col grid (channels list AND form column with its own h2 "form.title").
  1540→  Impact: SR users navigating by landmarks hear a "قنوات مباشرة" region that also holds the classic form — the region name under-describes half its content.
  1541→  Recommended fix: split into two sections (each labelled by its own h2) or relabel the outer one (e.g. a "تواصل" heading id).
  1542→[P3] contact-form.tsx:108-115 — prefill template is seeded once; stale on soft re-navigation
  1543→  Evidence: `useState(() => ({ …, message: buildTemplate(initialService, prefillIdea) }))` — buildTemplate runs only in the lazy initializer; props changes never re-seed.
  1544→  Impact: latent today (all prefill producers live on other routes), but a client-side locale switch on /contact?service=… keeps the seeded message in the OLD language, and any future same-route searchParam change would keep the stale template. User edits being preserved is correct; only the untouched-template case drifts.
  1545→  Recommended fix: if ever needed, re-seed in a [prefillService, prefillIdea, locale]-keyed effect guarded by "textarea still equals previous template" (same edit-protection as onToggleService).
  1546→[P3] calculator.tsx:518 + contact-form.tsx:227 — honeypot `-left-[9999px]` physical offset (documented RTL quirk)
  1547→  Evidence: `className="pointer-events-none absolute -left-[9999px] h-px w-px overflow-hidden"`.
  1548→  Impact: pre-existing + documented (UI-5 note: /contact body scrollWidth reads 11295 under RTL; not visually scrollable) — listed for completeness.
  1549→  Recommended fix: switch to the sr-only clip pattern or `position: fixed; inset-inline-start: -9999px` to kill the phantom scrollWidth.
  1550→[P3] deconstructed-card.tsx:163 — hardcoded Latin fallback metric
  1551→  Evidence: `const metricMain = metrics?.[0] ?? '+140%'`.
  1552→  Impact: unreachable while catalogs ship workSection.project1.metrics (verified present) — but if the key ever goes missing the showcase silently renders an English string on the AR site.
  1553→  Recommended fix: fall back to `''` or omit the metric line when absent.
  1554→[P3] about/page.tsx:124 — unreachable avatar-gradient fallback (dead code)
  1555→  Evidence: `AVATAR_GRADIENTS[i % AVATAR_GRADIENTS.length] ?? 'from-primary to-g-blue'` — modulo over a 4-entry array always yields a defined value.
  1556→  Impact: dead branch only; minor noise.
  1557→  Recommended fix: drop the `?? …` (or keep as a deliberate guard with a comment).
  1558→[P3] home compact interactive controls below 44px touch targets (documented scope decision)
  1559→  Evidence: hero-console preset chips `px-3 py-1.5 text-sm` (~33px), MiniAgent input/button h-9 (36px), simulator scenario toggles h-10 (40px), calculator option chips py-3 (~44px ok) but integration chips py-1.5 (~33px).
  1560→  Impact: sub-44px targets on the home conversion path (console chips are the hero's primary intent input). Batch 1 item 4 deliberately scoped the 44px rule to navbar; the remainder was left compact by design (4-I3 risk note 3).
  1561→  Recommended fix: if the rule is extended site-wide, bump chips to min-h-11 / py-2.5 in a single pass; otherwise record the scope decision as final.
  1562→POSITIVE VERIFICATIONS (no action): DamascusClock placeholder+'--:--:--' setState-in-effect pattern is hydration-safe; useSyncExternalStore server-snapshot pattern (reduced-motion, sound, global-error language) prevents hydration mismatches across all animated components; universal :lang(ar) letter-spacing reset neutralizes every tracking-* leak I found (kicker, calculator reference pill, simulator headings); dir="ltr" islands correct (clock, console/payload/log, orbit seal, URLs, phone values, formula bar); Next-16 Promise params/searchParams awaited everywhere; per-page generateMetadata via buildPageMetadata with canonical+hreflang+x-default and pinned OG cards; JSON-LD on home (no fake SearchAction); heading hierarchies clean on every route (sr-only h2 on /work fixes the old jump); [...rest] hard-404 + per-segment loading.tsx rationale verified; all internal nav links resolve to real routes; lazy wrappers genuinely code-split (dynamic import + IO-gated mount + matched placeholders); eslint 0/0 on all 33 files; R7 lessons correctly applied (data-bg-layer on all hero decorative absolutes, h1 flex-col, testimonial Parallax not a direct spotlight child).
  1563→VERDICT: 0 P0, 1 P1, 2 P2, 9 P3 — architecture is sound (hydration, a11y, i18n, SEO, perf discipline all verified strong); the one real debt is an incomplete amber→blue revert sweep concentrated in fallback/glow layers, plus two content/robustness gaps; everything else is polish.
  1564→ADDENDUM (L1-C, post-report self-correction): My [P2] console-scene WebGL-guard finding overclaimed the blast radius. Cross-checking @react-three/fiber v9.7 source (events-156d8d12.esm.js:15700-15743, 15928-15936) confirms L1-D's verification: Canvas root.configure() is async and un-awaited — a WebGLRenderer constructor failure rejects silently (unhandled promise rejection in console; children never mount), it does NOT propagate into React's render path, so [locale]/error.tsx never trips. Corrected impact: on browsers where getContext('webgl2'/'webgl') fails despite WebGLRenderingContext existing (blocklisted GPU/driver crash), the hero-console scene area is a ~280px empty dark box with a console error — a degraded-UX edge case, not a page crash. Corrected severity: P3 (probe context creation in the showSvg gate and/or add ContextLossGuard for parity with the other two canvases). Amended verdict: 0 P0, 1 P1, 2 P2, 10 P3 (the P2 slot reverts to the two content/consistency findings; the WebGL guard joins the P3 bucket).

[showing lines 1245-1564 of 1565; use offset=1565 with limit to continue reading]