44:- Deferred / notes for coordinator: (1) same-route prefillService changes (hypothetical — no producer today) re-seed the message from the visitor's CURRENT chip selection, not the new param (full prop-sync incl. chip state was judged out of the audit's scope); (2) the empty-allowlist extension beyond the prescribed single key is documented in the script and here — if stats.hours.suffix/pages.about.numbers.years.suffix ever gain real content the allowlist should shrink; (3) L1-D's remaining P3s in OTHER agents' scopes (logo comment, console-scene drag affordance/WebGL probe, three-d-section aria, page-hero amber radial, hero-console/simulator/bento amber rgba sweep, capability parallax P1) were NOT touched — outside my file list.
49:Task: LOOP 1 fixes — capability parallax revival, console drag implementation, WebGL probe gate, mobile tier, a11y
52:- Read worklog tail (L1-A/B/C/D audits + 2-a/2-c/2-d fix reports; conventions: GLSL1 style, eslint-disable react-hooks/immutability for uniforms, R3F v9 e.currentTarget pointer-capture shim, no tsc/build/browser, surgical edits). Read all 6 scope files in full BEFORE editing.
53:- FIX 1 (L1-D P1 — capability-scene.tsx, THE key fix): deleted the dead parallax plumbing — `const mouse = useRef({tx,ty})` (was :306) and the whole `e.currentTarget.getBoundingClientRect?.()` block (was :462-466) that could never run under R3F v9's event-hijack shim (no getBoundingClientRect on the pointer-capture proxy). The useFrame parallax (now :444-446) reads R3F's maintained NDC pointer directly: `const tx = (state.pointer.x * viewport.width) / 4` / same for ty — the file's own ÷4 amplitude and 0.04 easing preserved, still lerping the `parallax` parent group so blob+satellites+halo drift together. state.pointer updates on every canvas pointermove (R3F event pass), so no listener and no rect read remain. Grep-verified: zero remaining consumers of the deleted mouse ref.
54:- FIX 2 (L1-C/L1-D P2 — console-scene.tsx, drag was promised but never existed): implemented the clamped drag by mirroring capability-scene's proven throttled pattern at the DOM wrapper level — ConsoleScene now returns a wrapper div (`absolute inset-0`) with onPointerDown (setDragging + record start + `e.currentTarget.setPointerCapture?.(e.pointerId)`), onPointerMove (FIX(2-c/4) ~60Hz timestamp throttle, early-return BEFORE updating `last` so deltas accumulate correctly; yaw free `+= dx*0.008`, pitch `THREE.MathUtils.clamp(rotation.x + dy*0.008, ±MAX_TILT)` with new `const MAX_TILT = 0.5` ≈ ±29° — the "clamped" promise, console can never flip upside down), onPointerUp/onPointerCancel (setDragging(false) + guarded `hasPointerCapture`/`releasePointerCapture`). Wrapper-level DOM handlers chosen over R3F group handlers so the WHOLE scene box is a drag surface (R3F raycast events would only START a drag on a node/sprite hit; this scene's ray targets are sparse — presses on empty space would stay dead). The group mutation channel: Nodes registers its rotation group UP to ConsoleScene via a `registerGroup` callback prop (see react-compiler note below); the existing useFrame auto-drift pauses while dragging (capability-consistent) and its drift tilt is now clamped to the same ±MAX_TILT so drift alone can never flip the console either (was unbounded += 0.02/s → upside-down after ~2.6min). Canvas cursor:'grab' KEPT — moved to the wrapper as `dragging ? 'grabbing' : 'grab'` (now truthful); Canvas keeps `style={{position:'absolute', inset:0}}` minus the cursor. Added ContextLossGuard (mirror of hero-canvas:531 / capability-scene:541 — preventDefault + one-line warn) inside the Canvas (L1-C P2 parity). Header comment rewritten to describe what's actually implemented.
55:- FIX 3 (L1-C/L1-D P3 — WebGL gate + truthful rotate cursor): NEW src/lib/use-webgl.ts — `probeWebGL()` (synchronous one-shot, memoized at module level so all consumers share ONE context-creation probe; `createElement('canvas').getContext('webgl2') ?? getContext('webgl')` in try/catch; server → false WITHOUT pinning the cache) + `useWebGLSupport()` hook (rAF-deferred exactly like hero-canvas's glAvailable pattern — no synchronous context creation in the effect body; SSR-safe false start). hero-console.tsx:197 gate strengthened `!('WebGLRenderingContext' in window)` → `!webglSupported` (from the new hook) — driver-blocklisted browsers now get the SVG diagram instead of R3F's un-awaited async configure() rejection → silent empty 280px box. Cursor wiring: removed the dead `data-cursor="rotate" data-cursor-label={t('placeholder')}` attrs from the pointer-events-none label chip (kept the chip as the visible preset-name hint) and put `data-cursor={showSvg ? undefined : 'rotate'}` on the hit-testable `#hero-console-scene` wrapper that now receives the drag. DEVIATION (documented): dropped the explicit data-cursor-label={t('placeholder')} when moving the affordance — t('placeholder') is the search-input placeholder ("صف مشروعك بكلمة واحدة…"), NOT a rotate label; making it live would have mislabeled the cursor ring. With only data-cursor="rotate" present, the custom-cursor engine resolves the label from its own common.cursor.rotate context map = "اسحب للتدوير 360°" / "Drag to rotate 360°" — the exact text the audit quoted as the promised affordance (same mechanism three-d-section already relies on).
56:- FIX 4 (L1-D P2+P3 — hero-canvas.tsx mobile tier + ultrawide): `PARTICLE_COUNT = 4500` → `PARTICLE_COUNT_DESKTOP = 4500` / `PARTICLE_COUNT_MOBILE = 1800`. New `useMobileTier()` hook — the EXACT useSyncExternalStore pattern of use-reduced-motion.ts: module-level MQL singleton for the combined query `'(max-width: 767px), (pointer: coarse)'` (comma = OR → coarse pointer OR narrow viewport), subscribe/getSnapshot/getServerSnapshot (false on server — HeroCanvas is a client-only dynamic import anyway). Reactive WITHOUT canvas re-creation: tier flip swaps `dpr={[1,1.5]}` ↔ `dpr={[1,2]}` (R3F re-applies the pixel ratio in place, no GL context recreation) and remounts `<Particles key={particleCount} count={particleCount}>` so the lazy-useState buffers resize (buffers are sized once per mount by design — Math.random purity); the Canvas/GL context itself survives. Particles signature `{mouse}` → `{mouse, count}`; desktop path byte-identical (4500 + dpr [1,2]). Silk plane `args={[64, 32]}` → `args={[80, 32]}` (covers 32:9 ultrawides: visible half-width at the dolly's farthest z ≈ 39.4 < 40; cost-free for a 1-segment fullscreen quad; height/fov untouched so the ±10.5 vertical melt math is unchanged).
57:- FIX 5 (L1-D P3 — three-d-section.tsx accessible interactive visual): wrapper gained `role="img"` (aria-label was on a role-less div — ignored by most AT) + `tabIndex={!reduced ? 0 : undefined}` + focus ring mirroring the hero CTA pattern (`focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-elyra-dark` — compiles, unlike the old ring-offset-elyra-deep). Keyboard path: onKeyDown (ArrowLeft/Right/Up/Down → `sceneRef.current?.nudge(±16, 0)` / `(0, ±16)`; 16 "drag pixels" ≈ 0.16 rad ≈ 9° per press; preventDefault so the page doesn't scroll while rotating; guard no-ops before the lazy scene mounts). The nudge channel: capability-scene exports `CapabilitySceneHandle {nudge(dx,dy)}` and accepts `ref` via React 19 ref-as-prop + useImperativeHandle — verified END-TO-END against installed sources that the ref survives next/dynamic → React.lazy (next 16.1.3 loadable.js spreads all props incl. ref onto React.lazy; react-dom 19.2.3 jsx-runtime keeps ref IN props, tag-16 lazy beginWork passes pendingProps — ref included — to the resolved function component; @types/react 19.2.8 documents the `ref?: Ref<T>` in-props pattern). three-d-section imports the handle TYPE-ONLY (`import type { CapabilitySceneHandle }`) so the lazy chunk boundary is untouched. nudge applies the SAME ±0.01 rad/px mapping as the pointer drag to the SAME spinner group (blob+satellites+rings rotate identically to an equivalent drag).
59:- Verification: `bunx eslint` on all 6 files → 0 errors, 0 warnings (after the redesign above). git diff reviewed hunk-by-hunk for every file: surgical, no whole-file rewrites, ZERO GLSL lines touched (explicit diff scan for uniform/varying/attribute/gl_/snoise lines → only my own comment mentions curlNoise). Import resolution: @/lib/use-webgl exists under the `@/* → ./src/*` alias; type-only import into three-d-section verified erased (import type). Dev server (hot-reload, never restarted): `GET / 200` (compiles hero-console + three-d-section + use-webgl) and `GET /services/websites 200` (compiles three-d-section's tree) with `✓ Compiled` and ZERO new error lines in dev.log mentioning my files; lazy leaf chunks (hero-canvas/console-scene/capability-scene) parse-verified via eslint's full TS parser (0 problems). node scripts/check-i18n-parity.js → exit 0, 635/635 keys (no new i18n keys needed — cursor label comes from the engine's existing common.cursor.rotate). No tsc/build/browser/DB writes; dev server untouched; git scope = exactly my 5 files + new src/lib/use-webgl.ts.
62:- All 5 fix-list items DONE: capability mouse parallax REVIVED (state.pointer NDC read replaces the dead rect math — the documented "blob+satellites+halo drift together" hover response now actually works); console drag IMPLEMENTED (throttled + pointer-capture + clamped pitch ±0.5 rad + drift-pause + grab/grabbing cursor + ContextLossGuard — the "اسحب للتدوير 360°" affordance is now truthful, and the whole scene box is a drag surface, not just node hits); WebGL gate hardened (new memoized rAF-deferred context-creation probe hook; blocklisted GPUs get the SVG fallback); hero mobile tier shipped (coarse-pointer/<768px → 1800 particles + dpr ≤1.5, reactive without canvas re-creation, desktop byte-identical) + silk plane widened to 80 for 32:9 ultrawides; three-d-section is an accessible interactive visual (role="img" + real focus ring + full arrow-key rotation path through a React 19 imperative handle verified through the dynamic/lazy ref plumbing).
63:- Design decisions worth keeping: (1) register-callback-instead-of-ref-prop pattern for R3F group sharing across the Canvas boundary — keeps react-compiler quiet in BOTH components with zero disables (reusable next time an R3F scene needs an external mutation channel); (2) data-cursor-label intentionally NOT moved with data-cursor="rotate" onto #hero-console-scene — the engine's common.cursor.rotate fallback renders the correct bilingual label, whereas the old t('placeholder') label would have been the input placeholder text; (3) console drag throttling/clamp constants mirror capability-scene (16ms / 0.01 rad/px) at 0.008 rad/px + MAX_TILT 0.5 rad for the lighter scene.
64:- Deferred / notes for coordinator: (1) hero-canvas.tsx + capability-scene.tsx still carry their own inline rAF WebGL probes — probeWebGL() is exported and memoized so they can adopt it in a later round (deliberately not swapped now to keep the desktop path byte-identical per the fix spec); (2) keyboard nudge renders only while the capability frameloop is active (offscreen = paused) — inherent to the frameloop gating, no user-reachable path focuses an offscreen scene; (3) three-d-section keeps role="img" (per the fix spec) — semantically the frame is a static image with a bonus keyboard affordance; a future round could revisit role="application"/figure semantics if AT feedback warrants; (4) eslint currently 0/0 across all six files — the register-callback pattern is load-bearing for that, don't "simplify" it back to ref props.
72:- Read worklog tail in full (L1-A/B/C/D audits + 2-a/2-c/2-d/2-b fix reports). Coordinator E2E report not yet present in the worklog (the drag-postponement fix is visible in the hero-console.tsx diff — left to L2-B per protocol).
73:- AMBER SWEEP (2-a): reviewed every hunk of `git diff` on automation-simulator / bento / before-after / page-hero / opengraph-image / not-found / logo (+ light check of hero-console's 2-a share). All 17 value edits landed as reported (rgba(217,119,6,*)→rgba(0,113,227,*) where the element carries primary; rgba(245,158,11,*)→rgba(66,133,244,*) where g-blue anchors; #b45309→#1d4ed8; text-amber-400→text-g-blue). Grep `217,119,6|b45309|245,158,11|d97706|92400e|78350f|f59e0b|fbbf24|fde68a|amber-|Amber` (case-insensitive "amber" too) across src/ → ZERO. Quad-dot #4285F4/#EA4335/#FBBC05/#34A853 intact in logo/icon/apple-icon/OG/not-found/intro-overlay/work-grid. Stale "no-blue law" comments reworded in opengraph-image, not-found (localeDetection wording), logo, bento (duplicated line removed).
114:  Evidence: success handler does `setValues({..., message: ''})` + `setService(null)`; the new re-seed effect (deps include `service`) then runs: prevTemplate = lastTemplateRef (e.g. the serviceIdea template), nextTemplate = buildTemplate(null, prefillIdea) = ideaOnly template → mismatch → guard `v.message.trim() === ''` is TRUE on the just-reset empty message → message is re-seeded to the ideaOnly template. Trace (primary hero-console → /contact?service=X&idea=Y flow): land (message=serviceIdea, lastTemplate=serviceIdea) → submit → 201 → reset → effect → message = ideaOnly(Y). A locale switch after success with ?idea= present triggers the same via the buildTemplate identity change. Pre-fix behavior: form stayed fully cleared after success.
125:- Read worklog tail (L1-A/B/C/D audits, 2-a/2-c/2-d fix reports, 2-b WebGL fix report). NOTE: the coordinator's LOOP-2 E2E verification notes are NOT in worklog.md — the file ends at 2-b's report; the drag-postponement fix exists only as code + inline comments in hero-console.tsx/console-scene.tsx (audit-trail gap recorded below).
126:- Reviewed EVERY hunk of `git diff` on the 5 WebGL files + read the new src/lib/use-webgl.ts in full; then fresh-eyes full reads of console-scene.tsx (575 ln), capability-scene.tsx (675 ln), hero-console.tsx (382 ln), three-d-section.tsx, hero-canvas.tsx (tier/GLSL regions).
128:- FIX 1 (parallax, L1-D P1) VERIFIED: `mouse` ref + `e.currentTarget.getBoundingClientRect` block fully deleted (grep: zero remaining consumers; the only getBoundingClientRect hits are explanatory comments); useFrame reads `state.pointer.x·viewport.width/4` / `state.pointer.y·viewport.height/4` with the original 0.04 easing; parallax group wraps spinner (blob+satellites+rings) AND the halo `<points>` — blob+satellites+halo still drift together. NaN-safety verified against installed R3F 9.7.0 (events `compute`: `state.pointer.set(event.offsetX/size.width·2−1, …)` — pointer only ever set from real events on a sized canvas; Vector2 init (0,0); viewport finite). Fix is live-possible now.
131:- FIX 4 (mobile tier) VERIFIED: constants 4500/1800; useMobileTier is the exact use-reduced-motion.ts pattern (module MQL singleton for '(max-width: 767px), (pointer: coarse)' — comma=OR — subscribe/getSnapshot/getServerSnapshot=false); dpr swaps [1,1.5]↔[1,2]; `<Particles key={particleCount}>` remounts so the lazy-useState buffers resize; desktop path byte-equivalent (4500 + dpr [1,2] + constant key). Verified against installed R3F 9.7.0 source that the dpr prop change does NOT recreate the canvas: CanvasImpl's dependency-less useIsomorphicLayoutEffect re-runs `root.configure({...dpr...})` every render; configure's renderer setup is `if (!state.gl)` (one time) and dpr goes through `state.setDpr` → `gl.setPixelRatio` in place.
132:- FIX 5 (three-d-section a11y) VERIFIED: role="img" + aria-label + `tabIndex={!reduced ? 0 : undefined}` + focus-visible ring (ring-offset-elyra-dark — compiles, unlike the old elyra-deep); Arrow keys → `nudge(±16,0)/(0,±16)`; preventDefault placed AFTER the switch (default case returns early) so only arrow keys are swallowed — no collateral scroll suppression; `sceneRef.current?.nudge` null-guard no-ops before the lazy scene mounts (arrows scroll normally then — correct). Type-only import (`import type { CapabilitySceneHandle}`) verified erased → lazy chunk boundary intact. Ref-plumbing mechanism RE-VERIFIED against installed sources (citations): next 16.1.3 lazy-dynamic/loadable.js renders `<Lazy {...props} />` with ref included in the spread; react 19.2.3 jsx-runtime keeps `ref` IN props (only `key` is stripped); react-dom-client beginWork `case 16` (LazyComponent) passes `workInProgress.pendingProps` — ref included — to the resolved function component via updateFunctionComponent. nudge() applies the SAME ±0.01 rad/px mapping as the pointer drag (capability-scene lines 493-494) to the SAME spinner group. Services pages unaffected: imperativeHandleEffect in react-dom no-ops when ref is undefined (verified in react-dom-client source), and three-d-section is the ONLY CapabilityScene consumer.
133:- Coordinator drag-postponement fix VERIFIED (hero-console.tsx:99-148 + console-scene.tsx:499-540): (a) first-intent-wins holds — each postponement re-assigns navTimer.current (`window.setTimeout(navigate, 400)`), so `if (navTimer.current) return` blocks later intents while postponing; (b) unmount cleanup (hero-console:110-115) clears the CURRENT handle — the reschedule is synchronous inside the fired callback, so the handle is never stale; (c) drag-forever = an infinite but strictly single-timer chain (one live timeout at a time — no accumulation/leak) that dies at unmount; (d) after release the pending ≤400ms timer fires with dragActiveRef=false → router.push — nav lands within ~400ms; (e) reduced path (250ms) unaffected — reduced users get the SVG diagram, dragActiveRef can never be set; (f) no stale closure — `navigate` closes over qs/router captured at intent time (correct semantics: the intent's own params; goToContact is rebuilt on router/reduced change); (g) ordering confirmed — onPointerDown calls `onDragStateChange?.(true)` BEFORE `e.currentTarget.setPointerCapture?.(...)` (a throwing capture still leaves the flag set, and a failed capture still delivers pointerup → endDrag), and endDrag (both up+cancel) reports false. reportDragState is a stable useCallback mutating only a local ref — react-compiler clean.
134:- Cursor affordance: `data-cursor={showSvg ? undefined : 'rotate'}` now sits on the hit-testable #hero-console-scene wrapper; verified the custom-cursor engine resolves the label from common.cursor.rotate ("اسحب للتدوير 360°"/"Drag to rotate 360°" — present in BOTH catalogs) when no explicit data-cursor-label exists; the dead pointer-events-none chip attrs are gone.
140:- ✓ FIX 1 (L1-D P1) capability-scene parallax revival — dead rect-math removed, state.pointer NDC read with ÷4/0.04, group composition preserved, NaN-safe.
143:- ✓ FIX 4 (L1-D P2+P3) mobile tier — uSES MQL tier, dpr swap in-place (R3F source-verified), keyed remount, desktop byte-equivalent, ZERO GLSL changes (5/5 blocks byte-identical), plane 80×32 with untouched melt/vignette math.
148:[P3] src/components/home/console-scene.tsx:463-574 (+ hero-console.tsx:281-309) — dragActiveRef can strand true if ConsoleScene unmounts mid-drag
149:  Evidence: ConsoleScene calls `onDragStateChange?.(true)` in onPointerDown and `?.(false)` only in endDrag (pointerup/pointercancel). hero-console renders `{showSvg ? <ConsoleSvgDiagram/> : <><ConsoleScene …/>…</>}` — when showSvg flips false→true the WebGL branch (and its pointer handlers) unmounts with no cleanup reporting false. Flip sources: isMobile MQL crossing 768px (window resize mid-drag), reduced-motion OS toggle mid-drag (webglSupported cannot flip true→false after resolving).
158:  Impact: audit-trail gap only — LOOP 2 has no record of the coordinator's browser verification or the rationale/400ms choice; future rounds auditing "who changed hero-console's goToContact" will find no provenance.
183:- P3-A: console-scene.tsx — added unmount cleanup `useEffect(() => () => onDragStateChange?.(false), [onDragStateChange])`; dragActiveRef can no longer strand true when ConsoleScene unmounts mid-drag (768px tier crossing / reduced-motion toggle while dragging). Idempotent (parent reportDragState writes only a ref); stable callback → cleanup fires exactly at unmount.
193:Task: Apply the 11 verified R3/R5 home-UI fixes (a11y affordances, keyboard paths, perf coalescing, contrast, SR labels).
198:- FIX 2 [P3] bento.tsx MiniCube keyboard path — stage div (bento.tsx:636-643) now carries role="img" + aria-label (moved from the cube so it names the FOCUSABLE element; children of role="img" are presentational) + tabIndex={0} + onKeyDown + focus-visible ring (ring-ring offset-2 offset-elyra-dark — three-d-section convention, flat 2D ring instead of a 3D-rotated one on the cube). onKeyDown (588-614): ArrowLeft/Right/Up/Down nudge the same `rot` state the drag writes, ±0.15 rad (≈8.6°) yaw / ±0.1 rad (≈5.7°) pitch (KEY_YAW_DEG/KEY_PITCH_DEG at 502-507 — one firm ≈17px drag at the drag's 0.5°/px mapping; same unclamped rotation space — the pointer drag clamps nothing), delta signs mirror same-direction drags (right→+yaw, down→−pitch), preventDefault AFTER the switch (three-d-section pattern — only handled arrows swallow scrolling), each press = an interaction (stopSpin + armIdle, like onDown/onUp). DEVIATION from task parenthetical: tabIndex NOT gated on reduced motion — MiniCube drags still work for reduced users (only autonomous spin + 0.2s easing are disabled; component doc comment), and reduced users get instant un-eased nudges (transition:'none' when reduced). Label kept as `${t('hint')} — ${t('idle')}` — no suitable existing key mentions arrow keys (bento.threeD.mini has only hint/idle; hero.console.sceneLabel is hero-specific copy), task fallback applied (no new keys).
199:- FIX 3 [P3] before-after.tsx:1018-1061 — drag pointermove rAF-coalesced (GlowCard pattern): pendingXRef stores latest clientX, schedulePosFromPending schedules ONE frame applying setFromClientX(pendingXRef.current), rAF cancelled on unmount (1034-1039) AND drag-end; onPointerUp cancels the queued frame and applies the final position synchronously (final-position guarantee preserved); pointerdown still applies immediately; clamp logic (2-98) + cursor + RTL START-edge math untouched.
212:- A11y: no false drag affordance on deconstructed-card; MiniCube operable by keyboard (arrows, focus ring, named stage); calculator success announces context via focus move (not body-drop); Toaster + live-clock speak Arabic; decorative glyphs silenced.
213:- Perf: before-after drag now one setState per frame (was per-pointermove); cursor-velocity ignores touch.
221:Task: Implement the 10 verified WebGL fixes from R3/R4 (LOOP-3 Group 2)
224:- Read worklog tail (L1 audits → L2 verifications → L3-0). Verified actual capability-scene path: src/components/three/capability-scene.tsx (not home/). Verified hero.console.sceneLabel exists in BOTH catalogs at messages/*.json:75 (added by coordinator — I did not touch messages/*).
225:- FIX 1 [P2] touch-action: console-scene.tsx:637-645 — drag wrapper style gains `touchAction: 'none'` (+ rationale comment; iPads ≥768px mount this scene and native panning would claim the gesture → pointercancel → dead "اسحب للتدوير 360°"). capability-scene.tsx:691-698 — same on its drag wrapper. Repo precedent before-after.tsx:1113.
226:- FIX 2 [P3] console-scene.tsx:363-381 — split the [tubes, glowTexture] disposal effect into TWO: tubes effect deps [tubes] (363-372), glowTexture effect deps [glowTexture] (379-381; stable useMemo-[] identity ⇒ cleanup fires exactly once at unmount, never mid-mount on preset switch). Chose [glowTexture] over [] because exhaustive-deps is ERROR level here — instruction explicitly allowed either. Stale "exactly once" comments updated (124-129, 359-362, 374-378) to state the real semantics.
227:- FIX 3 [P3] hero-console.tsx:163-178 — scrollIntoView behavior: reduced ? 'auto' : 'smooth'; `reduced` added to scrollSceneIntoView's useCallback deps ([reduced]).
228:- FIX 4 [P3] console keyboard path: console-scene.tsx:582-618 — stable useCallback onKeyDown: ArrowLeft/Right ±0.13 rad free yaw, ArrowUp/Down ±0.07 rad pitch clamped ±MAX_TILT (same THREE.MathUtils.clamp as the drag pitch path), groupRef null-guard early-return, preventDefault AFTER the switch (default returns early) so only the 4 handled arrows swallow scroll. Wrapper (620-646): role="img" + tabIndex={0} + aria-label={sceneLabel} (new optional prop, 494-505) + focus ring. hero-console.tsx:299-304 passes sceneLabel={t('sceneLabel')}. data-cursor="rotate" untouched.
229:  DEVIATION (documented): the focus ring is `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-inset` instead of three-d-section's verbatim `ring-offset-2 ring-offset-elyra-dark` — this wrapper is inset-0 inside hero-console's overflow-hidden frame, so an outward ring+offset would be clipped invisible; inset draws the same 2px ring token just inside the scene edge. Tailwind v4.1.18 compile-probed both `ring-inset` and `inset-ring-*` (both compile; used the legacy modifier form).
230:- FIX 5 [P3] NEW FILE src/lib/use-mobile-tier.ts (60 lines, 'use client') — useMobileTier extracted verbatim from hero-canvas (MQL singleton '(max-width: 767px), (pointer: coarse)' + useSyncExternalStore + false server snapshot, comments adapted to be consumer-generic). hero-canvas.tsx:7 imports it, local copy deleted (597-600 note left). capability-scene.tsx:15 imports it; 643 `const mobileTier = useMobileTier()`; 701 `dpr={[1, mobileTier ? 1.5 : 2]}`.
231:- FIX 6 [P3] uPixelRatio live sync: hero-canvas.tsx:449-455 — Particles useFrame sets `uniforms.uPixelRatio.value = state.viewport.dpr` every frame (memo initial value kept as first-frame guess). capability-scene.tsx:465-470 — haloUniforms.uPixelRatio live-synced the same way; initial value tier-clamped via new Centerpiece prop `dprMax` (320/328-332, memo deps [dprMax] at 405-415, passed at 711-715). Note: react-hooks/immutability reports ONE finding per memoized value, so the second uniform mutation needs no eslint-disable (removed 2 unused-directive warnings to reach 0/0).
232:- FIX 7 [P3] clock rewind: console-scene.tsx:308-331 — `const tRef = useRef(0)`; useFrame does `tRef.current += delta; const t = tRef.current` (drives emissive pulse phases + traveling-pulse arcs). capability-scene.tsx:449-459 — same tRef pattern (drives blob uTime, halo uTime, satellite orbit angles). Grep-audited: ZERO remaining clock.elapsedTime/getElapsedTime in either file (only explanatory comments). Audit also covered hero-canvas (already immune via uTime += delta).
233:- FIX 8 [P3] shared WebGL probe: hero-canvas.tsx:614-630 and capability-scene.tsx:662-676 — inline createElement('canvas').getContext probes replaced with `if (!probeWebGL()) setGlAvailable(false)` inside the SAME rAF callbacks. Timing semantics preserved byte-for-byte (glAvailable starts true, flips false at first rAF only on failure — no first-paint delay, no new flash; useWebGLSupport()'s false-first semantics were deliberately NOT adopted to keep observable behavior identical). probeWebGL is module-memoized ⇒ one real probe per page load shared across hero-canvas + capability-scene + hero-console. use-webgl.ts itself untouched (0-line diff).
234:- FIX 9 [P3] reversed smoothstep: hero-canvas.tsx:241-244 (PARTICLE_FRAGMENT) and capability-scene.tsx:309-312 (HALO_FRAGMENT) — `smoothstep(0.5, 0.08, d)` → `1.0 - smoothstep(0.08, 0.5, d)` (+GLSL comment). Mechanical GLSL block diff vs HEAD: these are the ONLY shader deltas in both files — 4/5 blocks byte-identical in hero-canvas (3311/1729/176/5273 chars), 4/5 in capability-scene (2148/1580/1554/614 chars). Grep confirmed no other reversed-edge smoothstep anywhere.
235:- FIX 10 [P3] dt-compensated lerps (1 - Math.exp(-k·delta), clamps safely at delta 0/huge): hero-canvas.tsx Particles mouse ×0.04→k=2.454 (460), uScroll ×0.08→k=4.983 (468), tilt ×0.05→k=3.079 (473); SilkBackdrop mouse ×0.05→k=3.079 (524), uScroll ×0.08→k=4.983 (533); ScrollDolly camera ×0.05→k=3.079 (575). capability-scene.tsx parallax ×0.04→k=2.454 (488). k constants as given in the task brief; each carries a "dt-compensated (was ×N/frame)" comment.
237:- Gates: `bunx eslint` on all 6 files (console-scene, capability-scene, hero-canvas, hero-console, use-mobile-tier, use-webgl) → 0 errors / 0 warnings / exit 0 (zero output; react-compiler at warn included). `bunx tsc --noEmit` → 0 errors / exit 0. i18n parity → 638/638, exit 0 (grew from 635 via coordinator's LOOP-3 catalog additions, not mine). No dev server / build / browser run. No files outside the exclusive set touched.
240:- All 10 fixes implemented: FIX 1 touch-action ×2, FIX 2 disposal split, FIX 3 reduced-motion scrollIntoView, FIX 4 console keyboard path + sceneLabel, FIX 5 use-mobile-tier extraction + capability dpr tier, FIX 6 uPixelRatio live sync ×2 (tier-aware initial in capability), FIX 7 clock→tRef ×2 (all useFrames audited), FIX 8 shared probeWebGL ×2 (identical timing semantics), FIX 9 reversed smoothstep ×2 (mechanically verified only shader delta), FIX 10 dt-compensated lerps ×7.
241:- One documented deviation: inset focus ring (clipping rationale above). One implementation choice within the brief's allowance: glowTexture disposal deps [glowTexture] not [] (exhaustive-deps=error).
285:- V-1 P3: capability-scene.tsx — split the shared disposal effect [blobGeo,blobMat,haloGeo,haloMat] into 4 per-resource effects; the dprMax-keyed haloUniforms→haloMat memo changes identity on a mid-session tier flip, and the shared cleanup would have disposed the still-live blob/halo GPU resources (three.js self-heals, but it was the same mid-mount-dispose bug class console-scene just got fixed for).
286:- V-1 P3: console-scene.tsx — keyboard steps now pause the auto-drift: onKeyDown sets the same `dragging` flag the pointer drag sets + re-arms a 900ms idle timer (each key repeat re-arms; unmount cleanup clears). Deliberately NOT routed through onDragStateChange — keyboard rotation must never postpone the pending payoff navigation.
287:- V-1 P3: k-constants corrected to exact -60·ln(1-f) values: 2.449 (was 0.04), 3.078 (was 0.05), 5.003 (was 0.08) across hero-canvas (6 sites) + capability-scene (1 site); comments synced.